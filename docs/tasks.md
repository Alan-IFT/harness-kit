# Task Board — Harness Kit

> Maintained by PM Orchestrator. Each task lifecycle stage is logged here.

## Active tasks

| ID | Slug | Stage | Mode | Started | Doc folder |
|---|---|---|---|---|---|
| T-24 | operator-obligation-home | 1 — requirement analysis | full | 2026-08-02 | `docs/features/operator-obligation-home/` |

## Completed tasks

| ID | Slug | Outcome | Completed | Doc folder |
|---|---|---|---|---|
| T-23 | review-write-path | Delivered into unreleased v0.46.0 (1 rollback) — **the two review roles no longer name an output they cannot produce**. From entropy finding **EP-001**: both reviewers declare `Read, Glob, Grep`, yet both `## What you produce` sections named a file they must author and instructed in-place correction; the reconciliation existed in **one** of the three contracts as a parenthetical conditioned on a fact that file's own frontmatter already fixes, the code reviewer had **no equivalent clause**, and the orchestrator — the role that actually writes — carried **no mention of the duty**. It had already fired: T-22 transcribed three documents, one missing its declared opening line and carrying a forbidden `## Round 2` section. **OQ-1 decided as direction 2+3: no capability changed.** The reviewers become named **authors**, the PM the named **writer** that transcribes verbatim and authors no part. The architect tested the analyst's two-conjunct override and it failed on conjunct 2 (`agents/supervisor.md:283` — this repo's only live prose confinement ships with a false statement of its own basis); **the gate adjudicated the direction independently and upheld it**, publishing a four-point case *for* the write grant and a three-point rebuttal, decisive leg = asymmetric detectability (the duty's failure leaves an artifact; the invariant's failure leaves none). **Gate r1 BLOCKED ON DESIGN (2 CRITICAL)**: the AC-10 uniqueness constraint was *unsatisfiable* against statements the same design mandated — it would have passed on paper at stage 2 and failed at stage 6 — and a dispatched stage does not load the working-tree contract, so AC-8/AC-9 carried an undischargeable precondition. r2 replaced it with a scoped **write-act uniqueness rule** (exactly one per contract; P7/G3/G4/K3/K4 rewritten as anaphors) that the gate re-mutated and confirmed still bites, and settled the acceptance bar as a **controlled reproduction** — **no human ruling required**, because stage 6 runs before delivery/commit/release, making the released-build reading unsatisfiable *by construction* for every agent-contract task here. The architect made the test **stricter on every axis except the one that cannot execute**. **The arrangement was measured, not asserted**: five `general-purpose` probe dispatches; the load-bearing **variant B** — a writer holding *only* the reviewer's in-band header, the exact main-session shape T-22 failed in — persisted the body **byte-identical** (`fd5ff7f0…`) and added nothing, and variants A and B produced **byte-identical files despite different instruction sets**; the **must-fail control** on the authentic 0.44.0 build was **routed back unwritten**. Bound published: the control is pre-T-18, so it validates the apparatus while necessity rests on AC-10 (1 write-act statement → 0 in every mutant). Stage 4 corrected three inherited figures **against itself** (base 293 not 294; C1 frees 5 not 6; the errors cancelled) and found `02`'s audit search returns **zero** hits under any `.harness/` path because `rg` skips dot-directories — dropping exactly the twin class the condition existed to cover. **K7's null-prediction re-read bit**: `code-reviewer.md`'s own fenced example contradicted its schema, so a body written as its contract *demonstrates* would have been routed back; CR upheld the fix and **corrected the developer's scope claim**. **QA adjudicated a striking result away rather than harvesting it** — a probe's "my `tools:` line was violated" claim was an artifact of pasting a contract as prompt text (15 documents, ≥13 dispatches, 0 counter-instances). 7 QA defects, none blocking; residuals published unsoftened incl. P6(b)'s 5/5 two-way readability (wedge-shaped, safe reading taken by both writers) and three uninstantiated class members. No new check (**32 held**, md5-stable ×4), no version stamp moved, baseline sha256-identical, no `.ps1` surface (operator list stays **25**). `pm-orchestrator` 296/300. verify_all **32/0/0 exit 0**. **The change is edited, not in force — commit is the unstarted link.** Queued: settle H1-vs-marker ordering across all seven contracts (`qa-tester.md:15` vs `:92-93` is live). | 2026-08-02 | `docs/features/_archived/review-write-path/` |
| T-22 | stage-model-tiering | **DELIVERED AS A DECLINE (2 rollbacks)** — ASSESS-FIRST outcome. The row asked to wire the per-agent `model` / reasoning-effort declarations the agent format already supports; T-21's cost attribution (78% of spend is cache traffic, ~128k cached tokens read per call against ~1,272 output) had since downgraded tiering from primary lever to secondary, so the honest question became what tiering still buys after context reduction. **Nothing was wired; no `agents/*.md` edited.** Deliverable = the assessment + one `## stage-model-tiering` record (model-swap **declined**, reasoning-effort **deferred**), `+23 −0`, byte-identical to design §6 (`cmp` IDENTICAL, digest `072fe740…`). **Delegated share published as a band 6.7%–45% at LOW confidence with its decisive component — the sub-agent share of cache traffic — declared NOT DERIVABLE in-repo and the reason given**; the external report's ~$36 figure used only as a floor, never as "what sub-agents cost" (the named trap, avoided). Decision rests on two **rate-free** legs: no rollback-attribution instrument (QA computed ≈30 tasks/arm at α-only, **≈61 at 80% power**, against a ten-task history) and no per-consumer reversibility (plugin-native agents; QA broke a planted local override four ways — it yields a bare-named agent nothing dispatches). Addressable set reduces to RA+SA, the two longest-propagation roles; the three verification roles held per constraint; supervisor evaluated as best structural candidate and rejected on magnitude. Break-even surface **0.02–0.83 rollbacks/task (0.6%–44%)**, all 16 cells reproduced exactly by QA. **The architect falsified stage 1's central claim** (the break-even is NOT independent of the delegated share) and found the correction *strengthened* the decline, then published **six movements toward BUILD** against itself incl. tripling its own ceiling. **Gate r1 BLOCKED ON DESIGN (4 MAJOR)** — every one decline-favourable and refuted inside the design's own rationale; gate r2 assembled the **compound** BUILD case the architect never had and confirmed survival. Gate drafted a `NEEDS-HUMAN` on whether a decline may override a live build row, **PM declined to escalate** on `STREAM_LOG.md:88` + the dispatch's verbatim pre-authorisation + operator-authored `BATCH_PLAN.md:42` ("Do not skip to T-22."), and **the gate verified all three and withdrew the point**. **CR r1 MAJOR**: a freeze-proof mutation row unproducible by its declared method; the **developer confirmed it against its own interest** and took the costlier fix (a real re-run in a scratch git repo whose per-file control pins to the live tree). **QA falsified a clause the gate had weighed and passed** — 19/19 rollbacks *are* stage-attributed in `PM_LOG.md`, twice under explicit ledger headings — and still ruled the decline sound, because the load-bearing claim is that nothing **measures** a per-stage rate (RES-QA1, MAJOR non-blocking, reserved for the operator; one-verb repair specified). QA also measured a new class nobody had run: an **in-hunk line-count-preserving edit** defeats `git status`, `numstat` *and* `wc -l` at once — only a digest moves. Whole-tree scan over **499 tracked files**: exactly 4 written post-dispatch, none under `agents/`, `skills/`, `.harness/scripts/`. `agents/` proved to be **in** the dirty set — three stages had certified it clean off a **truncated session-context `git status`**. No new check (**32 held**), no version/count flip, baseline preserved, 0 tests added. verify_all.sh **32/0/0 exit 0** (×6 across two stages, no flakes); no PS surface created. **Out-of-band: `gate-reviewer` and `code-reviewer` have no `Write` tool and cannot author their own stage docs — PM transcribed three documents verbatim; needs its own row.** `BATCH_PLAN.md:37` deliberately left `in-progress` — closing it is the operator's act. | 2026-08-02 | `docs/features/_archived/stage-model-tiering/` |
| T-20 | harvest-wrapped-insight | Delivered into unreleased v0.46.0 (3 rollbacks + 1 document-correction return) — **a wrapped `## Insight` bullet no longer loses its continuation lines**: the harvester emitted only bullet-marker lines, so an insight authored as wrapped text lost every evidence pointer **silently, at exit 0**, with the console echo reprinting the truncated first line (T-15 lost 4/4 this way). **Rotation was worse than the brief predicted** — it derived its "header" as every non-bullet line in the whole index and re-emitted that block first, so a stored continuation line was **hoisted above every entry**, not orphaned; the analyst found this at stage 1 and the gate confirmed it independently, making harvest + index-read + header-derivation one atomic change. Harvest, rotation, the stored-index read and `I.4` now derive **every figure from one classification of one file** (`I.4`'s separate `grep -c` cap arm removed, so one check can no longer hold two notions of "entry"); the cap counts **entries**; an unaccounted line refuses at **exit 3** naming document + 1-based line + text **before any create/write/append/move** (verified with rotation pending); a tally prints on every terminating path. **Section discovery is a whole-document, fence-aware walk** — every matching heading opens a section, a heading inside a fenced block is neither opener nor terminator with fence state tracked **once**, a fence open at EOF refuses, and every quoted heading skipped is **counted and printed** (the report is a *precondition* of the fence rule, not decoration — without it the narrowing is inferable only from an entry that never arrived). Gate r1 **BLOCKED ON REQUIREMENTS** (3 FAIL): declined the "closer block" widening because its rule order made a post-break bullet *ignorable*, re-creating the defect class **inside the fix**; **reversed the PM's own in-scope ruling** on the template cap repair on a measurement the PM had not made (the shipped template ships a 9-line header, so `F.4` already WARNs today — aggravation, not a flipped verdict); and caught that the fix would rotate the shipped template's closing `-->` out of every generated project's index. Analyst r2 re-authored the rule around a **terminal footer anchored to the last entry** with the footer clause ordered *after* entry-start, making the safety property structural. **The architect falsified the analyst's own "G-2 dissolves" claim** rather than relying on it. **QA found a MAJOR regression five stages had passed** — an intermediate fix scanned only the *first* matching heading where the pre-change awk re-armed on every one, dropping a harvested entry at exit 0 inside the design's own regression floor — and **predicted this task's own delivery would lose every insight** to a fenced `## Insight` quote, which the developer then closed and QA measured precisely enough to yield a rule. **The code reviewer published why it had missed it** as reusable method (a regression floor is verified by the *class* it covers, never the fixture it runs), which immediately produced two more findings. **The developer caught its own fix costing coverage** (a `>= N` relaxation went green against the very script it detects, because `${var##*needle}` returns its operand unchanged) and repaired it. Three agents corrected a wrong figure in the PM's own log. Driver pair `test-archive-task.{sh,ps1}` new: **186/0**, **84/102** vs the git-extracted pre-change script, corroborated at the **label level** not by totals; QA added a 1181-document randomized differential fuzz against an independently written parser (**0 violations**). Residuals published unsoftened, incl. `QA-9` (a fenced bullet inside the section) and five pre-existing channels reproduced so each bound is measured. No new check (**32 held**), no version stamp moved, `PM_LOG` compacted 673→383. verify_all.sh **32/0/0** (×3, count read from the run); PS operator-pending (**17** numbered — item 17 added, 1-16 untouched). | 2026-08-01 | `docs/features/_archived/harvest-wrapped-insight/` |
| T-18 | stage-contract-split | Delivered into unreleased v0.46.0 (3 rollbacks + 1 escalation to the human) — **every pipeline stage's output is now a typed contract plus an optional `0N_RATIONALE.md` sibling**, with round history in `PM_LOG.md` and each downstream stage consuming contracts by default, opening a rationale only on a named trigger (T2.1–T2.4, T4.1–T4.4, T5.1–T5.4, T6.1–T6.3, T7.1–T7.3). The normative **stage-doc boundary rule** — ordered six-step classification ladder + "Not units" + a self-contained "Verbatim byte-form" definition + a 14-row first-match-wins table with a terminal reachable default — is single-sourced in `.harness/rules/70-doc-size.md` and its `/harness-init` twin (byte-identical inside the inserted section; the fragment reaches generated projects via the template overlay, **not** `sync-self`, which the architect established by falsifying the analyst's premise). The contract **keeps the existing filename**, so backwards compatibility cost zero mechanism and no archive was touched. **Measured on T-15, span by span against the live archive: stage-4 ingest 1649 → 1028 = 37.7 %** (floor 30 %), stages 5/6 at 52.9 %/51.7 % — and the figure moved *down* twice (40.1 → 38.6 → 37.7) as two reviewers re-attributed spans toward the contract, the smallest published. **The grade is stated exactly and not softened: structural for multi-line forms, compliance for anything that fits on one line.** The load-bearing property carrying AC-2 — every declared shape is line-shaped, so a multi-line body has no container to hide in — was found by QA and previously unstated; the flagship 115-line block walks to `no home` under three independent walks, with the row-2 guard present *and* removed. Gate r1 **BLOCKED ON DESIGN** (9 MAJOR: the design breached its own flagship rule in its flagship edit; the rule was total as a table and non-total as a system; a false "renames no heading / no other file changes" closure) → design r2 answered F-1 with a **third option the gate had not offered** (a normative-text exception structurally unable to match code) and the gate upheld it after re-walking all fourteen rows. CR caught the last unmet AC-4 clause → dev r2; CR then **ruled its own proposed fix directionally wrong** when the developer pushed back with measurement. **QA falsified the rule twice** — first non-total as a system (`## Insight` misrouting in the **false-green** direction, silently defeating `archive-task` at exit 0), then, after the fix, the hole having moved from *which row* to *which unit* (same bytes, opposite destinations by markdown container). PM **escalated rather than dispatching a fourth stage-2 round** — the two remaining correctives were in direct tension with the clause that closed the CRITICAL — and the operator ruled: accept the narrower guarantee, publish the residuals unsoftened, record the decline (`byte-form-subpart-classification`). QA-12 fixed (partition FR-9 header sweep 4/6 → 6/6, reusing the sibling's exact wording). **First live use of the split**: the gate and the code reviewer each produced a real `0N_RATIONALE.md`. Three agents published measurements against their own interest (the smaller reduction, an unfavourable margin-0 threshold left untuned, QA correcting its own mis-instrumented count). No new check (**32 held**, run ×3 md5-identical), no stamp moved, `pm-orchestrator` 291/300, `70-doc-size.md` 174/200. verify_all.sh **32/0/0**; PS operator-pending (**16** numbered, 4 security — unchanged, no PS surface created). 11 residuals published, headed by RES-QA7/RES-QA8. **T-20 is the first task to execute under this structure.** | 2026-08-01 | `docs/features/_archived/stage-contract-split/` |
| T-16 | hook-truth-derivation | Delivered into unreleased v0.46.0 (2 rollbacks) — **the lockstep is gone**: all four derivation flows (creation, adoption, upgrade-repair, layout-migration) now obtain their hook command byte-forms from `hook-spec` via a lazily-resolved memoised **spec adapter**, and `resilient_cmd`/`Get-ResilientCmd` are **retired**. Provenance changed, **bytes did not** — AC-2 re-proved by QA against a genuinely non-circular oracle (`cb0ed57` predates `hook-spec` entirely), 8/8 cells both flows. `guard-rm` stays fail-CLOSED **structurally** (no expression in either flow constructs a command string; the adapter's only alternative to the spec's stdout is "return nothing"), verified across every degradation branch incl. a QA-invented **partial-answer** probe (6 of 8 cells resolve, guard alone does not → exit 4, no residue blessed on re-run). The `&`/`patsub_replacement` hazard is unreachable and QA **measured** the counterfactual (naive `${var//…}` writes `{{GUARD_COMMAND}}` into the middle of the live guard command). **T-15's inherited containment residual closed** (in scope, reasoned): `F.2` now tests containment, not presence — mutation M-C → exactly 2 tokens / 31-0-1, anti-vacuity PASS from an **S0 working-tree capture** (never `git show HEAD:` — HEAD is v0.44.0 and predates both `hook-spec` and T-15's anchoring), admissibility proven four ways. The analyst **falsified the upstream hand-off list** (2 omitted files) rather than inheriting it; the gate independently re-derived the ledger over a 54-file sweep, caught a non-total terminator rule, an adapter placement that would abort 3 of 4 files, and a proof chain resting on a stale artifact → design r2 (lazy resolution, `≤ IND` totality, S0-capture rule) + 2 binding conditions; the architect then found **two defects the gate missed** (a cache silently discarded by `$( )` capture — 13 spawns vs a ceiling of 9; a wrapper reduced to a pass-through) and adjudicated both gate disagreements **in its own favour, upheld**. PM routed CR MINOR #3 back over a non-blocking rating (a real cross-shell **verdict** divergence in the very check this task tightened); the developer then **declined the reviewer's own suggested "cheap fix" with a measurement** — GNU grep reads `[ \t]` in a bracket expression as `{space, backslash, t}`, so it would have shipped a false FAIL *and* a false PASS — and the reviewer withdrew the suggestion without reservation. QA (0 blocking) caught the last false claim: the fixture cited to prove the terminator fix **passes under both rules** and never discriminated; QA built the one that does. 3 prose consumers corrected; 4 stale provenance sentences retired. No new check (**32 held**), 0 pins moved, `75-safety-hook.md` md5-stable at 200/200, QA left zero footprint. verify_all.sh **32/0/0** (count read from the run, 3× no flakes); PS operator-pending (**16** numbered, 4 security — items 14a/15a/16 are **mandatory re-runs** against round-2 bytes). **Zero hand-synchronised byte-form copies remain in any hook-writing path.** | 2026-08-01 | `docs/features/hook-truth-derivation/` |
| T-15 | hook-truth-verify-scope | Delivered into unreleased v0.46.0 (1 rollback + 4 doc-only fix-forward rounds) — **the gate stopped asserting a fact about the maintainer's machine**: `F.2` selected its hooks evidence from a **gitignored** `.claude/settings.local.json`, falling back to the committed `.claude/settings.json` whose hooks object has been empty since T-12 — so a fresh clone and **every CI run** failed the release gate at FAIL severity on an environment condition, in **both** shells. ASSESS-FIRST re-ran the decline case (T-13 made the file bootstrappable, T-14 gave the health report the machine dimension) and **rejected** it on evidence: the failure was *repairable*, not absent, and CI cannot be fixed by documentation; the cheap middle path (assert-only-if-present) is **unsound** because the documented durable opt-out **is** a present file with an empty hooks object; and this dogfood-only gate protects exactly one machine (generated projects run the type-overlay gate, which has **zero** guard/hook tokens), so dropping the read costs **zero** user-facing coverage. `F.2` is now single-source over tracked content — **7 assertions, all FAIL**: 4 guard-script paths + template exists + `{{GUARD_COMMAND}}` + a `"PreToolUse"` **key**. Two corrections rode along: the hook-block assertion was **VACUOUS since inception** (unanchored whole-file `grep` satisfied by the template's own `_guard_hook` doc string → deleting the *entire* array still PASSed, both shells; QA proved it by running the **committed pre-change script** against the mutated artifact — no hook-block token at all), now anchored to the key form; and the PS twin **stopped throwing on the first problem** (accumulate-then-throw), fixing a real B-9 violation. Gate r1 caught a **CRITICAL**: the plan never forbade mutating the live fail-CLOSED guard script (renaming it seizes the whole Bash toolchain; recovery is Read+Write only) → design r2 folded C-1..C-10, r2 APPROVED. CR (0 CRITICAL/0 MAJOR) ruled all 4 self-reported discrepancies **sound**, caught that a dirty-set **difference alone** cannot prove a freeze on a tree with 37 already-dirty files (the **mtime table** carries it — corrected before it reached permanent memory), and verified the tallies against **check-ordering and indentation signatures**, not arithmetic. QA went past the plan: built a **stronger** clean state (staged the overlay *inside* the worktree, removing the stale-index concession), mutated **all seven** assertions incl. the three nobody had, ran the accumulation cases, defeated the anchor 4 ways, and **empirically confirmed** the declined residual (B1/B2 assert presence, never **containment** → a template with the placeholder in another hook + `"PreToolUse": []` PASSes under a "wiring present" label → **T-16**). Architect r4 then found its own r2 claim false (2 of 3 cross-shell deviations exist *because* T-15 anchored B2) and that **none** has a backstop in the PS gate. No new check (**32 held**), no stamp moved, `test-guard-rm` **87/0** pin held, `75-safety-hook.md` md5-identical at 200/200. verify_all.sh **32/0/0**; PS operator-pending (**11** items, 2 security). **T-16 unblocked.** | 2026-08-01 | `docs/features/_archived/hook-truth-verify-scope/` |
| T-17 | guard-cmd-chain | Delivered into unreleased v0.46.0 (4 rollbacks) — **closed a reproduced destructive-command bypass**: the guard judged only the first verb of each top-level PIPE segment, so `echo hi && rm -rf <outside root>`, `true; rm …` and `ls \| xargs rm …` all exited 0 (surfaced out-of-band by T-14). The nine-verb rule is now evaluated at **every command position** — `;` `&&` `\|\|` `&`, newlines/CRLF, subshells, brace groups, `$( )`, backticks, process substitutions, argv carriers (`xargs`/`env`/`nohup`/`nice`/`time`/`timeout`/`command`/`exec`/`find -exec`) and nested `bash -c`/`sh -c`/`pwsh -c` — via a single-pass quote/here-doc/comment-aware **position scanner** unioned with the byte-unchanged pre-change pass **plus the input string itself at every depth**, so monotonicity holds by construction and the path resolver stayed byte-unchanged. **Verb set deliberately unchanged** (`mv`/`cp`/`>` stay out — reachability fixed, not scope). Gate round 1 caught a false negative (`$( )` opened inside `"…"` never reset quote state), a **false** "monotonicity provable by inspection" headline, a branch order breaking an AC under both readings, and a coverage **overstatement**; CR caught a `\>&` false negative, then caught **its own fix's regression** (sentinel `-1` == `i-1` at `i == 0` → leading `&` appended; `pwsh -c "& Remove-Item …"` flipped to ALLOW). Suite **17 → 87 rows**, now pinned in `baseline.json` (it was the only unpinned driver); vs pre-change **50/37**; two single-fix reverts redden **disjoint** sets `{R1,R2}` / `{R4,R5}`. QA: ~1,870 adversarial+fuzz probes, **0 BLOCK→ALLOW**, 0 exit codes outside {0,2}, 0 false positives on 50 realistic commands. NFR-1's +20 ms clause **WAIVED** (Mode 2, recorded — pre-existing O(n²) `${s:$i:1}`; typical commands got *faster*). Residual surface published, incl. the newly-disclosed **leading-token verb-hiding** class (`> /tmp/log rm …`). No new check (32 held); rule doc at 200/200 lines. verify_all.sh **32/0/0**; PS operator-pending (10 items, 2 security). v0.45.0→0.46.0. | 2026-08-01 | `docs/features/_archived/guard-cmd-chain/` |
| T-14 | hook-truth-status | Delivered into unreleased v0.45.0 (3 rollbacks) — **the health report no longer lies about the guard**: `/harness-status` declared the destructive-command guard `DISABLED` (and all four events `not wired`) on a machine where it is installed and demonstrably blocking, because it read one hardcoded `.claude/settings.json`. New **`§0 Effective hook source`** resolve-once module (machine-local → committed precedence, four-state probe converging with `install-hooks`'s own predicate) that §1/§3b/§3c/§6/§7 all read — no section re-reads a settings path. Guard verdict is now an 8-row first-match-wins table quantified over the extracted path SET (`PATHS`/`GUARD_PATHS`), so wiring-absent / wiring-dangling / installed-and-wired never collapse and no unverifiable wiring is healthy; the machine dimension (never-installed vs documented opt-out vs unparseable) is owned and named; every repair line is conditional on where the wiring lives (FR-8: never print a fix that cannot reach the file just named). §3c's event enumeration now comes from T-13's `hook-spec` (7 queries, soft dependency + labelled fallback) instead of the report's own prose. Gate caught F-1 (a false-green path through rows 6-8) → design r2; CR caught a false "stale baseline" claim queued for permanent memory → dev r2; **QA caught 2 MAJORs the gate and CR both missed** — a NON-TOTAL state table (`{"hooks": []}` matched zero rows) and a fix-line table with overlapping keys that read `rm <the only healthy settings file>` in one state → dev r3. QA re-verified with a 28-input totality prober (0 unclassified, 0 moved) and a 38-root regression sweep (0 changed). No new check (32 held); no `.ps1` touched; `baseline.json` untouched (its `45` confirmed CORRECT — a no-python3 key vs a 46 python3-present run). verify_all.sh **32/0/0**; test-supervisor 46/0. **T-15 now unblocked.** Surfaced out-of-band: `guard-rm.sh` only inspects the first verb of each top-level PIPE segment (`echo hi && rm <outside>` passes) — pre-existing, needs its own task. | 2026-07-31 | `docs/features/_archived/hook-truth-status/` |
| T-13 | hook-truth-spec | Delivered v0.45.0 (4 rollbacks) — **single source of truth for the lifecycle-hook wiring**: new `hook-spec.{ps1,sh}` pair (source in `templates/common/`, byte-mirrored via new `sync-self` Mapping 9) answering `(tool, OS) → command` + `event`/`matcher`/`semantics`/`hostos`; 3 convenience hooks stay fail-open, **guard-rm stays fail-CLOSED** (verified 6 variants × 10 reps → exits 127/126/2, never 0). `install-hooks` now **bootstraps a missing `.claude/settings.local.json`** from the spec (idempotent, no backup churn, temp-then-rename, terminal confirmation re-read from disk) so a fresh clone can no longer silently lose its hooks. AC-3 byte-identity proven executably for all 8 `(tool, OS)` cells against the **live** `resilient_cmd`/`Get-ResilientCmd` oracle (awk-range/AST extraction — sourcing would `exit 1` the driver) behind a call-through anti-vacuity assertion. Gate caught 2 design FAILs (missed live count site; a transitive proof chain that did not exist); CR caught MAJOR M-1 (`n_wired > 0` not `== 4` + dropped literal confirmations); QA caught r-1/r-6 (a step-7 refusal left a **guardless** file the next run blessed at exit 0 — PM routed back over QA's non-blocking rating) and r-7 (the new gate had no anti-revert coverage). Also fixed a **pre-existing** `test-init.sh` eval-quoting defect that silently truncated the driver on unix (baseline 278 → 355, all from captures), and **corrected a fabricated tally in T-12's archive** (append-only annotation, under coordinator scope grant). No new check (32 held); skills 17 / agents 8. verify_all.sh **32/0/0**; PS operator-pending (8 binding items). v0.44.0→0.45.0. **T-16 now unblocked.** | 2026-07-31 | `docs/features/hook-truth-spec/` |
| T-12 | resilient-hooks | Delivered v0.44.0 (0 rollbacks) — **real consumer bug fix**: the per-turn `Stop hook error: bash: .harness/scripts/harness-sync.sh: No such file or directory` eliminated BY DESIGN. (A) harness convenience lifecycle hooks (Stop/sync, UserPromptSubmit/ambient-prompt, SessionStart/ambient-reset) now use a split-anchor form — runtime `cd "$CLAUDE_PROJECT_DIR"`/`Set-Location` (cwd-independent, survives subdir launch) + presence guard → missing/unreachable script no-ops silently (exit 0, no stderr) instead of erroring every turn; **guard-rm stays fail-CLOSED** (same anchor, NO `|| exit 0`). Applied to template `.tmpl` + init/adopt `{{SYNC_COMMAND}}`/`{{GUARD_COMMAND}}` derivation + dogfood + a `/harness-upgrade`+migrate **repair path** that rewrites existing brittle hooks (idempotent, `.bak`). (B) dogfood hooks moved to gitignored `.claude/settings.local.json`; committed `.claude/settings.json` now hook-less; F.2 reads guard evidence from settings.local.json (fallback), J.1 adds it as a target. **OQ-3 load-bearing call:** `cd`-anchor keeps a bare space-bounded `.harness/scripts/<name>.<ext>` token so the E.4b/D.4b congruence ERE stays 0-edit. Gate caught 2 under-counted lockstep surfaces (templates/common 8-file sync-self; test-init test_migrate); CR APPROVED-WITH-NITS → 3 brittle fixtures fixed-forward (AC-7 met); QA added Fixture Z fail-closed mutation probe. No new check (32 held); skills 17/agents 8. verify_all.sh 32/0/0; PS operator-pending. v0.43.0→0.44.0. | 2026-06-21 | `docs/features/_archived/resilient-hooks/` |
| T-11c | entropy-watch-persist | Delivered v0.43.0 (0 rollbacks) — **anti-entropy watch slice 3/3 (feature COMPLETE)**: findings persistence via a lean DECLINE filter — the supervisor scan reads `.harness/rejected-decisions.md` and excludes any finding whose normalized module/concept handle EXACTLY matches a declined record (not substring → no sibling over-suppression; not the per-run EP-NNN); `/harness-deflate` step-4 = real decline action (append T-09 record, de-dup, no new tool, no /harness-goal dispatch); fail-open. **Scoped DOWN**: the scan re-derives every sweep so OPEN/FIXED need NO store — the standalone store was DECLINED as overkill (recorded as `## entropy-findings-store`). Single-sourced key rule (DRY); supervisor 285≤300; no new file/check; no count flip. verify_all.sh 32/0/0; PS operator-pending. v0.42.0→0.43.0. | 2026-06-20 | `docs/features/_archived/entropy-watch-persist/` |
| T-11b | entropy-watch-harness | Delivered v0.42.0 (0 rollbacks) — **anti-entropy watch slice 2/3**: wired the cadenced watch into the `/harness` single-task stage-7 delivery boundary, so the reminder now fires from BOTH `/harness-stream` (T-11a) AND `/harness` — completing "harness 和 harness-stream 都自动巡检". DRY: home in pm-orchestrator stage-7 (250 lines), harness SKILL step-10 = 1 pointer (0 entropy-cadence occurrences); goal-mode guarded OUT (first-sentence guard); plain `check` (no --first-of-session). Gate conditions F-1 (supervisor names /harness as 3rd entropy dispatcher; 280 lines) + F-2 (dev-map "(and later)" dropped) folded in. Reused entropy-cadence pair + supervisor lens + references (no new script/skill/state/check). verify_all.sh 32/0/0; PS operator-pending. v0.41.0→0.42.0. Final slice T-11c (findings persistence) queued. | 2026-06-20 | `docs/features/_archived/entropy-watch-harness/` |
| T-11a | entropy-watch | Delivered v0.41.0 (1 design rollback — Gate caught supervisor I.3-cap breach + false F.1-auto-extends claim; SA r2 fixed) — **anti-entropy watch, core slice** (operator-directed): new **17th skill `/harness-deflate`** (delegator — scan→read-only supervisor entropy lens, execute→`/harness-goal`; no Edit/Bash → can't refactor unauthorized) + `references/entropy-scan.md` (single-source scan grammar) + shared `entropy-cadence.{ps1,sh}` (check/delivered/swept, N=5, fail-open) + `supervisor` entropy lens stub (279≤300, Hard-rule #1 scoped read-only exception) + `/harness-stream` pool-drain `## Entropy watch` surface (cadenced, non-blocking). Machine reminds → user authorizes → machine executes. 16→17 fan-out + F.1 add; no new check (32), agents 8. QA: cadence boundary + fail-open + load-bearing mutations all green. verify_all.sh 32/0/0; PS operator-pending. v0.40.0→0.41.0. **Follow-ups queued: T-11b (/harness surface), T-11c (findings persistence).** | 2026-06-20 | `docs/features/_archived/entropy-watch/` |
| T-10 | planning-decision-map | **DECLINED (no build, 0 rollbacks)** — ASSESS-FIRST outcome (mattpocock/skills ⑧ decision-mapping). Independent RA + Gate re-derived a 1:1 concept-mapping onto existing surfaces (map=pool, ticket=row, blocked_by=Depends on, frontier=topological frontier [same word], fog=append-only pool, sizing=T-06 smart zone, Research/Prototype=explore, Discuss/bootstrap=grill, resume=stream resume); the only candidate delta (pre-task loose-idea question map) is already owned by /harness-grill + /harness-explore → BATCH_PLAN frontier. MINIMAL considered + rejected (not a gap). Decline recorded in `.harness/rejected-decisions.md` (first real use of the T-09 memory — loop closed). No skill/check/agent/version change; counts stay 16/8/32. verify_all.sh 32/0/0. Dev/CR/QA skipped (nothing to build). | 2026-06-20 | `docs/features/_archived/planning-decision-map/` |
| T-09 | rejected-decisions-memory | Delivered v0.40.0 (0 rollbacks) — new **rejected-decisions memory** (mattpocock/skills adoption ⑨; from `.out-of-scope/`): a 4th memory-layer file `.harness/rejected-decisions.md` (dogfood, 9 seed records: 1 deferred + 8 declined — design-it-twice/ask-matt/issue-tracker/to-prd/triage/telemetry/non-fit skill families) + generic template seed; read/append habit SINGLE-SOURCED in 25-decision-policy.md (loads at decide-points), RA/SA + 15-skill-authoring telemetry entry now pure pointers; AI-GUIDE memory line + dev-map row + symmetric test-init assertion (273→276). Chose a FILE over a convention (the convention already existed in 15-skill-authoring and had failed — invisible at decide-points). No count flip (16/8/32); no new check (memory not a gate). QA mutation proved the seed assertion load-bearing. verify_all.sh 32/0/0; PS operator-pending. v0.39.0→0.40.0 | 2026-06-20 | `docs/features/_archived/rejected-decisions-memory/` |
| T-08 | two-axis-review | Delivered v0.39.0 (0 rollbacks) — folded the **two-axis review principle** (mattpocock/skills adoption ⑦; from the `review` skill) into code-reviewer: a `## Two review axes` section keeping Standards-conformance and Spec/design-fidelity as SEPARATE non-merged lenses + a masking invariant (aggregate verdict can't read APPROVED while an axis holds an open CRITICAL/MAJOR) + a Workflow per-axis-severity step + a `## Axis status` block above the verdict in the format template. RA honestly scoped to ONLY the explicit-separation delta (declined parallel sub-agents / 7th dimension / severity-rewrite); read-only + severity model + rollback routing byte-unchanged. 108→139 lines (≤300); no count flip (16/8/32); no new check. CR dogfooded the new lens (both axes clean). verify_all.sh 32/0/0; PS operator-pending. v0.38.0→0.39.0 | 2026-06-20 | `docs/features/_archived/two-axis-review/` |
| T-07 | sa-design-vocab | Delivered v0.38.0 (0 rollbacks) — gave solution-architect a `## Design vocabulary (optional lens)` section (mattpocock/skills adoption ⑥; from codebase-design): 7 deep-module leading-word handles (module / interface=everything a caller must know / depth=leverage per unit of interface / seam / adapter / leverage / locality) + deletion test + "interface is the test surface" + "one adapter=hypothetical seam, two=real". Framed as an OPTIONAL lens (3 affordance signals, zero mandate — rule 15 P4 anti-railroad; 12-section output contract byte-unchanged). design-it-twice excluded (name-only future mention); DEEPENING pointer-only. 122→144 lines (≤300); no count flip (16/8/32); no new check. Gate/CR/QA verified interface+depth source fidelity + fence-clean. verify_all.sh 32/0/0; PS operator-pending. v0.37.0→0.38.0 | 2026-06-20 | `docs/features/_archived/sa-design-vocab/` |
| T-06 | vertical-slices | Delivered v0.37.0 (0 rollbacks) — added a **tracer-bullet vertical-slice + smart-zone task-decomposition discipline** (mattpocock/skills adoption ⑤; from to-issues + ask-matt). Single-sourced as `## Task-decomposition discipline` in `skills/harness-plan/SKILL.md` (each task a thin end-to-end independently-verifiable slice, NOT a horizontal layer; size to a ~120k-token window, split/hand off before degrading); `skills/harness-batch`, `skills/harness-stream`, and the BATCH_PLAN template reference it BY NAME (compose-by-name P8 + SSOT, no `../` link). BATCH_PLAN table header + triage logic byte-unchanged; no count flip (16/8/32); no new check. Gate V1-V6 + QA 8 probes all PASS. verify_all.sh 32/0/0; PS operator-pending. v0.36.0→0.37.0 | 2026-06-20 | `docs/features/_archived/vertical-slices/` |
| T-05 | durable-brief | Delivered v0.36.0 (0 rollbacks) — folded the agent-brief **durability discipline** (mattpocock/skills adoption ④; from triage/AGENT-BRIEF.md) into requirement-analyst (new Hard rule 6: behavioral-not-procedural + no forward-looking file:line in the spec + good/bad exemplars) and pm-orchestrator (1 dispatch-contract line referencing it). Key design: the no-file:line ban is scoped FORWARD-only and EXPLICITLY exempts backward-looking EVIDENCE citations (insight-index + stage-doc evidence keep citing path-and-line per 05-insight-index.md) — single-sourced in RA, PM references it. C-1 (no literal name.ext:NNN token, I.6-safe) + C-2 (additive only) held; QA regex-scanned clean. No count flip (16/8/32). verify_all.sh 32/0/0; PS operator-pending. v0.35.0→0.36.0 | 2026-06-20 | `docs/features/_archived/durable-brief/` |
| T-04 | skill-authoring-vocab | Delivered (0 rollbacks; no version bump — dogfood-only rule) — additively enriched `.harness/rules/15-skill-authoring.md` (mattpocock/skills adoption ③) with 7 named skill-design handles from writing-great-skills: leading word (→P1), no-op test (→P2), completion criterion *(new)*, premature completion *(new)*, sediment/sprawl (→P5+cap), single source of truth (→anti-bloat), user/model-invoked context-vs-cognitive-load lens *(new)*. P1-P8 byte-stable; 80→115 lines (≤200); source credited alongside the Anthropic line. No fan-out (not distributed), no new check (32 stays). Gate verified 7-concept fidelity vs source glossary; QA all adversarial probes survived. verify_all.sh 32/0/0; PS green-by-symmetry (operator-confirm optional). | 2026-06-19 | `docs/features/_archived/skill-authoring-vocab/` |
| T-03 | harness-grill | Delivered v0.35.0 (0 rollbacks) — new **16th skill `/harness-grill`** (mattpocock/skills adoption ②): a main-loop interview front-end (one question at a time, recommended answer per question, explore-codebase-to-self-answer, reads CONTEXT.md SOFT, emits an aligned brief → feature `INPUT.md`, then STOPS — not a pipeline stage, user-invoked, no Task tool) + a standing "recommend an answer per Open Question" rule on requirement-analyst (Hard-rule-1 strip-list reconciled: ban scoped to requirement prose, labelled `Recommended:` field exempt). Composes with T-02 CONTEXT.md into a grill-with-docs-style aligner. 15→16 fan-out across 13 files / 34 sites incl verify_all C.1/G.1/G.2 hardcoded name arrays BOTH shells (the load-bearing catch). Gate independently grep-verified the ledger complete; QA mutations A/B proved G.1/G.2 load-bearing for grill; decoys (CHANGELOG/tasks history, proposals html, insight-index, status "14 assets") frozen. verify_all.sh 32/0/0; PS operator-pending. v0.34.0→0.35.0 | 2026-06-19 | `docs/features/_archived/harness-grill/` |
| T-02 | context-glossary | Delivered v0.34.0 (0 rollbacks) — new **`CONTEXT.md` domain-glossary memory layer** (mattpocock/skills adoption ①): repo-root dogfood (13 real harness-kit terms, definition + `_Avoid_`) + generic `templates/common/CONTEXT.md` seed (placeholder-free, single-brace, not byte-synced — `decision-rubric.md` dual-purpose pattern); wired as a SOFT dependency into requirement-analyst + solution-architect (read-if-present + lazy-maintain + graceful-degrade, no setup pointer, no BLOCKED — per mattpocock ADR-0001 soft/hard split); AI-GUIDE memory-layer index line; dev-map row; symmetric test-init seed assertion (270→273). **No new verify_all check (32 stays), no new placeholder** ([[feedback_design_over_guards]]). QA load-bearing mutation proved the assertion non-vacuous + runtime graceful degradation (remove seed → 270/3 → restore → 273/0). verify_all.sh 32/0/0; PS side operator-pending (deny rule). v0.33.0→0.34.0 | 2026-06-19 | `docs/features/_archived/context-glossary/` |
| T-022 | stream-defer-human | Delivered v0.33.0 (0 rollbacks) — `/harness-stream` **defers needs-human tasks instead of halting**: a task needing human input gets new status `needs-human` with its exact ask recorded, only its `Depends on` descendants blocked, the stream keeps draining everything else; at end all asks surface together (FIRST `## Needs your input` in STREAM_REPORT.md + exit msg leads with the digest). Mechanism: `BLOCKED: NEEDS-HUMAN — <q> — <unblock>` pm-orchestrator return discriminator (new Hard rule 6: never auto-decide a human-reserved point to dodge blocking), step-d `deferred-human mode` dispatch signal, ingest ambiguity records-and-drains (AskUserQuestion removed from allowed-tools), (c)/(d) bright line (safety-action REQUEST defers / guard-rm BLOCK halts; the 3 hard-safety stops unchanged). QA PASS 0 defects: verify_all 32/32 BOTH shells, test-init 270/308, test-real-project 90/90, test-supervisor 45/49, behavioral probes 6(i-vii) all survived, G.3 mutation load-bearing. Counts stay 15 skills / 32 checks; v0.32.0→0.33.0 | 2026-06-13 | `docs/features/_archived/stream-defer-human/` |
| T-021 | stream-auto-decompose | Delivered v0.32.0 (0 rollbacks) — `/harness-stream` **ingest triage**: a complex chat/ambient requirement is auto-decomposed at ingest into N small staged pool rows (shared slug prefix + `## Notes` provenance, real `Depends on` only, union ≡ original ask; fixed-point: derived rows never re-triaged); simple requirements stay 1:1; user-authored `ADD`/hand-written rows never split (hard rule amended to the union invariant). Criteria single-sourced in SKILL.md with binding pointers from both channels (Gate D-1); 4 ambient-prompt hook carriers +2 emitted lines lockstep. QA PASS: verify_all 32/32 BOTH shells, test-init 270/308, test-real-project 90/90, isolated-root hook probes, G.3 mutation probe load-bearing, AC-5 semantic probes all forced by quotable clauses. 1 pre-existing MINOR (pwsh GBK console encoding → insight + backlog). Counts stay 15 skills / 32 checks; v0.31.0→0.32.0 | 2026-06-12 | `docs/features/_archived/stream-auto-decompose/` |
| T-000 | initial-bootstrap | Delivered | 2026-05-15 | _(bootstrap, no docs/features/ folder)_ |
| T-001 | ai-safety-guardrails | Delivered v0.15.0 (1 rollback) | 2026-05-17 | `docs/features/_archived/ai-safety-guardrails/` |
| T-002 | ai-native-init | Delivered v0.16.0 (2 rollbacks: M-1/M-2/M-3 from CR; BUG-2 from QA via PM override) | 2026-05-19 | `docs/features/_archived/ai-native-init/` |
| T-003 | supervisor-agent | Delivered v0.17.0 (3 rollbacks: F-1 from Gate; BUG-1 from QA via PM override; +1 within-stage round) | 2026-05-19 | `docs/features/_archived/supervisor-agent/` |
| T-004 | i6-semantic-guard | Delivered v0.18.0 (4 rollbacks: F-1/F-2 from Gate; F-4 from Gate re-review; entry-#10 from Dev; CR-MAJOR 40-locations:25 from Code Review) + in-flight archive-task.sh L13-pattern bug fix | 2026-05-23 | `docs/features/_archived/i6-semantic-guard/` |
| T-005 | i6-test-hardening | Delivered v0.18.1 (1 rollback: M-1 from Gate Review on §9 baseline; PM-applied §9+§11 patch under user-delegated authority); 12/12 QA mutations detected; PASS:56 both shells | 2026-05-23 | `docs/features/_archived/i6-test-hardening/` |
| T-006 | harness-batch-skill | Delivered v0.19.0 (1 rollback: M-1 AI-GUIDE.md skill-count drift + m-1 manual-e2e-test drift + m-2 stop-signal clarity, all from Code Review); 10/10 QA adversarial tests survived; PS verify_all 31/31 PASS stable x3 | 2026-05-23 | `docs/features/_archived/harness-batch-skill/` |
| T-007 | scripts-relocation | Delivered v0.20.0 (1 rollback: B-1 test-init contradictory asserts + M-1 test-supervisor.sh stale ref + m-1 baseline-move coverage, from Code Review; reproduce-before-fix exposed a stale 250/212 tally + 2 reviewer-missed defects); 6/6 ACs verified both shells, 12 QA adversarial probes survived, 0 defects; verify_all 31/31 PASS both shells. Scripts moved `scripts/` → `.harness/scripts/` + `migrate-scripts-layout` helper | 2026-06-04 | `docs/features/_archived/scripts-relocation/` |
| T-008 | test-supervisor-stamps | Delivered v0.21.0 (2 rollbacks at design: Gate re-scans found scattered count claims F-1/F-2/F-5 + SA self-caught F-7, converged at exhaustive 11-claim/8-file ledger); removed 8 stale `v0.17.1`/30 fan-out asserts (7 already red), added standing verify_all **G.4** meta-check (derives version from plugin.json + count from live step tally, gates 11 doc claims); 6/6 ACs both shells, QA version-bump probe proved G.4 load-bearing, 0 defects; verify_all 32/32 PASS (PS) · 31+1 pre-existing-I.4-WARN (SH). Count 31→32 | 2026-06-05 | `docs/features/_archived/test-supervisor-stamps/` |
| T-009 | i4-cap-symmetry | Delivered v0.21.1 (0 rollbacks); fixed verify_all I.4 to count insight-index evidence/data lines (`^\s*-\s+`) identically in both shells — resolved the pre-existing PS/bash cross-shell WARN/PASS split, aligned I.4 with archive-task's rotation metric + the "≤30 evidence lines" intent; reconciled baseline.json↔manual-e2e test_init to a captured run (251/213); 7-fixture QA parity matrix all byte-identical, 0 defects; **verify_all 32/32 PASS BOTH shells, 0 WARN, 0 FAIL** (SH WARN resolved). No new check | 2026-06-05 | `docs/features/_archived/i4-cap-symmetry/` |
| T-010 | g4-version-decouple | Delivered v0.21.2 (1 rollback: **QA caught D-1 — a MAJOR same-file substring collision the Gate+CR both missed**); decoupled the 6 prose verify_all count claims from the version (G.4 count-only against the live tally) so a count-unchanged release no longer bumps 6 doc version tokens — version stays gated by G.3 + the G.4 CHANGELOG check; the v0.21.2 bump self-demonstrated (touched only stamps+CHANGELOG, 0 prose claims). D-1 fix: row-3 keys on file-unique `(32 checks)` + systematic 11-row same-file-uniqueness audit. verify_all PS 32/32 (bash G.3/G.4 via probe — pre-existing I.6 MSYS wedge flagged). No new check | 2026-06-06 | `docs/features/_archived/g4-version-decouple/` |
| T-019 | agents-cutover | Delivered v0.30.0 — **content-model redesign Leg 1 complete**: the 8 framework agents are now plugin-native (top-level `agents/`, dispatched `harness-kit:<name>`), **no longer copied into each project** → per-project agent duplication/drift class ELIMINATED by design (the way the 15 skills already work). Spike-confirmed live dispatch first (`harness-kit:supervisor`/`requirement-analyst`); operator-spec'd, implemented by `harness-kit:developer` (dogfooded the shipped agents), operator-verified. Retired 24 copies; the feared atomic cascade was only 3 gate FAILs (D.1/E.3/E.4 — E.1/E.2/I.3 absorbed missing dirs). Partition `dev-*` stay local; default init plugin-native (openly Claude-Code-native). verify_all **32/32**; test-init 249 / real-project 82 / supervisor 45 (bash; PS run green by dev). Follow-ups (generated-project template-doc refresh + Claude-native repositioning done same day): Legs 2/3, existing-project migration. v0.29.0→0.30.0. | 2026-06-10 | `docs/features/_archived/agents-cutover/` |
| T-020 | sync-hook-dangling-ref | Delivered v0.31.0 (1 rollback: CR MAJOR B8 — migrate's terminal scan validated in-memory text not disk; rework re-reads written file + M3 read-only probe). User-reported consumer Stop-hook dangle (`harness-sync.sh: No such file or directory`) made unreachable (presence-gated rewires + terminal congruence scan exit 4 in migrate/upgrade helpers; init 10b + adopt terminal asserts; adopt gains `{{SYNC_COMMAND}}`/`{{GUARD_COMMAND}}` spec), repairable (`/harness-upgrade`: re-lands script pairs, repairs literal placeholder tokens, `REWIRE-PLACEHOLDER`), diagnosable (`/harness-status` §3c 4-event congruence; template E.4b/D.4b FAIL row). RC-6 fixed: type templates + status no longer demand 7 agents in `.harness/agents/` (v0.30-correct: partition `dev-*` only). Ambient hooks OS-picked (OQ-3). Doctor-vs-extend adjudicated: NO new command. QA replayed the user scenario verbatim (rc=127→repaired rc=0, idempotent); AC-7 cross-shell `cmp` byte-identical; all suites green both shells. Ran with PM shell in main thread (sub-agents lack Task tool); stage-4 rework agent died mid-flight (stream-idle) after code landed — PM attributed + verified. | 2026-06-11 | `docs/features/_archived/sync-hook-dangling-ref/` |
| T-018 | decision-mode-skill | Delivered v0.28.0 (1 design rollback: Gate F-1 — SA ledger missed the ungated `getting-started.md` "fourteen skills" fan-out surface; fixed SA round-2; Gate round-2 APPROVED 8/8). New **15th skill `/harness-decision-mode`** (interactive Mode 1/2/3 switcher; surgical Active-mode-line edit; Mode-3 custom-rubric capture; no helper per rule 15 P6) + **Mode 3 (user-custom rubric)** in the decision policy; policy mechanism shipped GENERIC (Active mode 1) to all generated projects (AC-4: zero operator personal prefs in the template). Pipeline RA→Gate; Dev BLOCKED-on-capability (sub-agents no Bash) → operator-verified. verify_all **32/32**, test-init **249** (bash; PS 287 operator-unrun — deny rule). Surfaced OQ-1 (pre-existing install-array gap: harness-upgrade/-language missing — deferred). 14→15 skills; check 32 unchanged; v0.27.0→0.28.0. | 2026-06-10 | `docs/features/_archived/decision-mode-skill/` |
| T-017 | i6-bash-inproc-scan | Delivered (operator-completed; pipeline RA→Gate clean, Dev BLOCKED-on-capability — sub-agents have no Bash). **I.6 grep-storm fix**: hoist the per-(file×entry) `i6_build_regex` rebuild (the real ~30k-fork bottleneck, NOT the grep scan the design hypothesized) out of the file loop + keep the Dev's in-process `[[ =~ ]]` scan → **verify_all.sh 12m13s → 44.5s (~12-16×)**. No data/semantics/check-count change (still 32); PS twin untouched (already in-process). test-verify-i6 **58/58** (incl. live bash↔PS parity); verify_all **32/32**. No version bump (script-internal perf). | 2026-06-09 | `docs/features/_archived/i6-bash-inproc-scan/` |
| T-016 | i18n-special-drift-guard | Delivered v0.27.0 (0 rollbacks) — **ELIMINATED** the T-015 R-2 body-duplication via better design instead of adding a guard (per user: "不断加守卫会使项目越来越臃肿;优秀的设计才是大家都喜爱的" → [[feedback_design_over_guards]]). **Check count stays 32 (no bloat).** Single-sourced the zh policy into a non-overlaid `i18n/zh/_policy/output-language.zh.md.tmpl`; re-pointed `/harness-language` to read it; init now COMPOSES a zh project (lay English `common/` → run `/harness-language zh` to inject — "init zh = init en + /harness-language zh", reusing the tested T-014 mechanism); **deleted the 3 SPECIAL i18n/zh files** → English body now lives in ONE place (`common/`), drift impossible-by-construction. SA design caught that option-B-by-deletion alone would've broken /harness-language. Dev pre-delete `cmp` proof: composed files byte-identical to old overlay. PS run by PM (Dev+QA sandboxes blocked pwsh): verify_all 32/32, test-language 39/39, test-init 275/275 incl. new composition-integrity assertion. QA PASS, 0 blocking (1 claim-accuracy minor: the body-match test is composition-integrity not drift-catching — corrected in 07/insight). verify_all **32/32 both shells**; v0.26.0→0.27.0; EN path byte-untouched | 2026-06-09 | `docs/features/_archived/i18n-special-drift-guard/` |
| T-015 | zh-overlay-anglicize | Delivered v0.26.0 (0 rollbacks) — completes the language model: a generated zh project's AI-facing STATIC scaffolding is now English (matches the policy it declares). Reduced the i18n/zh overlay 16→**5 files**: 11 AI-facing files `git rm`'d (AI-GUIDE, rule frags 05/60/75, insight-index, workflow/dev-map/tasks seeds, 3 type 50-*) → fall through to English common/; 3 SPECIAL files (00-core/CLAUDE/copilot) = English body + byte-preserved T-013 Chinese policy (single-section); 2 human-facing files (spec/README, golden-tasks) stay Chinese. **OQ-3 design caught a trap**: deleting the SPECIAL files would have broken `/harness-language zh` (T-014 reads its canonical policy from them) → kept in-overlay via splice. +19 inverse assertions/shell (QA proved non-vacuous by reverting a deletion). QA 0 defects — generated-zh-tree inspection + `/harness-language zh` round-trip both pass. verify_all **32/32 both shells**; test-init 274/236; check 32 / skill 14 unchanged; v0.25.0→0.26.0. EN path byte-untouched | 2026-06-09 | `docs/features/_archived/zh-overlay-anglicize/` |
| T-014 | harness-language-skill | Delivered v0.25.0 (0 rollbacks) — new 14th skill `/harness-language [en|zh]`: set / switch(en↔zh) / refresh a project's output-language POLICY for already-init'd OLD projects (the language-config analogue of /harness-upgrade's stale-scripts gap). skill + `language-policy.{ps1,sh}` helper: heading-anchor locate (matches en or zh policy heading) → slice to next `##` → install TARGET lang's canonical block **extracted from template** (never embedded → I.6-safe); `.bak`, NOOP-idempotent, dry-run, `--force` insert, CONFLICT→AskUserQuestion. `test-language.{ps1,sh}` 39/39 incl. byte-identical zh→en→zh round-trip. CR APPROVED (2 minor; SKILL step-4 wording tightened). QA 13/13 adversarial, 0 defects — **cross-shell end-state byte parity confirmed** (bash vs PS helpers byte-identical, both langs, even CRLF). No I.6 self-trip (grepped scanned files clean). verify_all **32/32 both shells**; check 32 unchanged; skill 13→14; v0.24.0→0.25.0. Scope = policy config only (overlay AI-facing template anglicization still future) | 2026-06-08 | `docs/features/_archived/harness-language-skill/` |
| T-013 | lang-policy-split | Delivered v0.24.0 (1 rollback: Gate F-1 — I.6 sweep missed archived snapshot `docs/project-overview.html:314`, fixed by exemption not rewrite). Refined the v0.7.0 zh output-language policy from the blunt all-Chinese rule to a THREE-WAY split (chat→ZH, AI-facing output→EN, human-facing output→ZH) in the i18n/zh overlay + SKILL.md Q5 + READMEs + manual-e2e. New I.6 banned-line for the retired blunt-Chinese claim (4-file lockstep + I6ExpectedEntryCount 13→14), first-ever test-init zh language assertion. **A delivery-stage self-trip** (harvested insight quoted the literal banned anchor → I.6 FAIL → reworded) validated the guard live. QA 11/11 adversarial (incl. cross-shell I.6 mutation + real zh-init policy inspection), 0 defects. verify_all **32/32 both shells**; check 32 / skill 13 unchanged; (A)runtime-output vs (B)template-language split kept B as future T-014 | 2026-06-08 | `docs/features/_archived/lang-policy-split/` |
| T-012 | harness-upgrade-skill | Delivered v0.23.0 (0 rollbacks; 5 GR advisories + 1 CR-minor cleanup + 1 QA MINOR DEFECT-1 all fixed forward) — new 13th skill `/harness-upgrade`: self-bootstrapping upgrade of a stale harness project (relocate → **content-refresh depth-sensitive scripts (L31 fix)** → settings rewire → hook re-install → verify_all regenerate w/ `HARNESS:B-CUSTOM` splice/HALT matrix preserving user B.*). Skill+helper two-layer; `upgrade-project.{ps1,sh}` in sync-self set; 6 verify_all `.tmpl` gained B.* delimiters. QA 16 adversarial probes (AC-5 mutation test load-bearing; OQ-3 no silent-loss path), DEFECT-1 hook-newline parity fixed + byte-proven. verify_all **32/32 BOTH shells**; check count stays 32; skill 12→13; v0.22.0→0.23.0 | 2026-06-08 | `docs/features/_archived/harness-upgrade-skill/` |
| T-011 | ambient-stream | Delivered (0 rollbacks) + UX refinement; minimal **ambient chat-driven stream** — invoke `/harness-stream` with **no pool-id and no keyword** → `.harness/ambient.flag`-gated `UserPromptSubmit` heartbeat hook folds each chat message into the no-arg default pool `docs/batches/default/` and drains it through the pipeline (no `/loop`, serial). **Session-scoped**: a `SessionStart` → `ambient-reset` hook clears the flag each new session (no "off" keyword; re-invoke to resume). Hooks `ambient-prompt.{ps1,sh}` + `ambient-reset.{ps1,sh}` (dogfood+template, F.1). CR APPROVED (0 findings), QA PASS 0 defects (flag on/off + deep-nested/non-repo cwd + 10× stability flaky=0). Dogfood `.claude/settings.json` wiring **propose-only** (red line + harness safety layer); ships wired in template. verify_all **32/32 PASS** (bash; `.ps1` pending one-time user Windows run). No new check/placeholder/version/count change (0.22.0 / 32 / 12; [Unreleased]) | 2026-06-08 | `docs/features/_archived/ambient-stream/` |

## Notes

- T-000 is the initial repo creation; it predates the pipeline, so no per-stage docs exist.
- Subsequent tasks must follow the 7-stage pipeline.

