# Batch Plan — default

> Created: 2026-06-11 · Updated: 2026-06-19
> Default mode: full
> Stop policy: strong-signal-only

## Tasks

| ID | Slug | Goal (one sentence) | Mode | Depends on | Status |
|---|---|---|---|---|---|
| T-01 | sync-hook-dangling-ref | Eliminate by design the consumer-project failure where the Stop hook fires `bash: .harness/scripts/harness-sync.sh: No such file or directory` on every turn — root-cause which flow can produce a project whose wired hook references a missing script, make that state unreachable, and ship a repair path for already-broken projects. | full | — | done |
| T-02 | context-glossary | Add a `CONTEXT.md` domain-glossary memory layer (tight definition + `_Avoid_` format) as a dogfood file and a harness-init template asset, wired as a SOFT dependency that the requirement-analyst and solution-architect reference and lazily maintain, indexed in AI-GUIDE.md — killing verbose/inconsistent domain naming at the root with no new verify_all guard. | full | — | done |
| T-03 | harness-grill | Add a `/harness-grill` main-loop skill that interviews the user one question at a time (recommending an answer for each, exploring the codebase to self-answer where it can) and emits an aligned brief that becomes the requirement-analyst's `INPUT.md`, plus a "recommend an answer per open question" rule on the requirement-analyst — composing with `CONTEXT.md` into a grill-with-docs-style alignment front-end. | full | T-02 | done |
| T-04 | skill-authoring-vocab | Additively enrich `.harness/rules/15-skill-authoring.md` with the named skill-design vocabulary from mattpocock/skills' writing-great-skills (leading word, completion criterion, premature completion, no-op test + sediment/sprawl pruning discipline, single source of truth, and the context-load vs cognitive-load / user-vs-model-invoked lens) without rewriting existing principles. | full | — | done |
| T-05 | durable-brief | Fold the agent-brief durability discipline (behavioral not procedural, no file paths/line numbers, complete testable acceptance criteria, explicit out-of-scope, durable across refactors) into the requirement-analyst's hard rules and the pm-orchestrator dispatch-prompt contract — sharpening stage hand-offs without touching insight-index evidence citations. | full | — | done |
| T-06 | vertical-slices | Add a tracer-bullet vertical-slice decomposition discipline (each task an independently-verifiable end-to-end slice, not a horizontal layer) plus a smart-zone task-sizing heuristic (size a task to one ~120k-token reasoning window) to the harness-plan decomposition guidance and the batch/stream task-authoring guidance. | full | — | done |
| T-07 | sa-design-vocab | Give the solution-architect the codebase-design deep-module vocabulary (module, interface, depth, seam, adapter, leverage, locality) plus the deletion test and "interface is the test surface" / "one adapter = hypothetical seam, two = real" principles, as an optional design-language section — excluding the heavier design-it-twice parallel-subagent pattern this round. | full | — | done |
| T-08 | two-axis-review | Fold the two-axis review PRINCIPLE from mattpocock/skills' `review` skill into the code-reviewer agent: keep Standards-conformance (does it follow this repo's conventions) and Spec/design-fidelity (does it match the requirement+design) as SEPARATE, non-merged lenses so one axis cannot mask the other — as a lightweight review-structure principle, NOT literal parallel sub-agent dispatch. | full | — | done |
| T-09 | rejected-decisions-memory | Generalize the ad-hoc "Deliberately not adopted" pattern into a lightweight rejected-decisions memory (mattpocock/skills' `.out-of-scope/` idea) that prevents re-litigating declined requests/approaches — assess whether a dedicated file or a documented convention on existing surfaces is the lighter fit, and wire a light read/append habit. No heavy machinery. | full | — | done |
| T-10 | planning-decision-map | ASSESS-FIRST: mattpocock/skills' `decision-mapping` (fog-of-war investigation map for a pre-task idea too loose to form tasks) overlaps heavily with our existing pool/frontier/stream + harness-explore/harness-plan. Honestly scope ONLY the genuinely-new delta (a planning-phase open-question map BEFORE tasks exist); if it is fully redundant, recommend DECLINE rather than build a duplicate feature (feedback_design_over_guards / lightweight). | full | — | done (DECLINED, no build) |

| T-11a | entropy-watch | Anti-entropy watch — thinnest end-to-end slice: supervisor entropy/deep-module scan lens + shared "remind-if-due" cadence check (N≥5 delivered since last sweep, or first session drain) + `.harness/entropy-watch.state` + `/harness-stream` pool-drained `## Entropy watch` surface (reuse T-022 pattern) + new 17th skill entry + authorize→execute via `/harness-goal`. Non-blocking, no new gate. | full | — | done |
| T-11b | entropy-watch-harness | Add the `/harness` single-task stage-7 delivery surface calling the SAME shared remind-if-due check (so both harness and stream auto-remind). | full | T-11a | done |
| T-11c | entropy-watch-persist | Findings-persistence store: open findings re-surface, fixed ones drop, user-declined ones go to `.harness/rejected-decisions.md` (no re-litigation). | full | T-11a | done (decline-filter only; standalone store DECLINED as overkill) |

| T-12 | resilient-hooks | Fix the per-turn `Stop hook error: bash: .harness/scripts/harness-sync.sh: No such file or directory`: (A) make the harness sync/ambient lifecycle hooks FAIL-OPEN + `$CLAUDE_PROJECT_DIR`-anchored (missing/unreachable script no-ops silently, cwd-independent) in the template + dogfood + /harness-upgrade repair path (guard-rm stays fail-CLOSED — safety); (B) stop distributing this repo's dogfood `.claude/settings.json` (move dev hooks to gitignored `.claude/settings.local.json`). Root cause: brittle relative-path hook (T-020 only ensured the script lands, didn't make the hook resilient). | full | — | done |

| T-13 | hook-truth-spec | Establish a single executable source of truth for the harness lifecycle-hook wiring (the four events × two OS command byte-forms plus each one's fail-open / fail-closed semantics), and prove it end-to-end by having the one-shot local-environment installer regenerate a missing dogfood local-settings hook file from it idempotently — so a fresh clone bootstraps its own hooks instead of silently losing them. | full | — | done |
| T-14 | hook-truth-status | Make the project-health report read the hook wiring from wherever it legitimately lives (local settings taking precedence over committed settings) instead of assuming one fixed file, and have it own the "is the destructive-command guard actually installed on this machine" report — fixing the current false `DISABLED` verdict that appears even when the guard is installed and demonstrably blocking. | full | — | done |
| T-15 | hook-truth-verify-scope | Narrow the guard-rm verification check to assertions the repository itself can answer (guard script pairs present, distributed template carries the guard placeholder and PreToolUse block) and remove its dependence on an untracked machine-local settings file, so a clean checkout no longer fails the gate for an environment condition — keeping every remaining assertion at FAIL severity and the total check count unchanged. | full | T-14 | done |
| T-16 | hook-truth-derivation | Retire the hand-maintained byte-identical copies of the hook command strings across the project-creation, adoption, upgrade-repair and layout-migration flows by having each derive its commands from the single source of truth, so adding or changing a hook is a one-place edit instead of a multi-file lockstep. | full | T-13 | done |
| T-17 | guard-cmd-chain | Make the destructive-command guardrail evaluate every command in a compound command line rather than only the leading verb of each top-level pipe segment, so a destructive verb reached through `&&`, `;`, a logical-or operator, a subshell, or an `xargs`-style intermediary is judged by the same out-of-project-root rule that already blocks it when written on its own — closing a reproduced bypass while keeping the guard fail-closed and without widening what counts as a destructive verb. | full | — | done |
| T-18 | stage-contract-split | Split every pipeline stage's output into a structured contract section carrying only what downstream stages consume (acceptance criteria, interface shapes, constraints, change ledger) and a free-form rationale section carrying the reasoning that only the gate and reviewer read, and make each downstream stage consume the contract alone — so implementation literals have no home in either section, routing history lives in the routing log rather than accumulating inside the design, and a stage stops re-reading every predecessor in full. Structural separation, not a summary copy and not a rule asking agents to write less. | full | — | done |
| T-19 | stage-summary-header | SUPERSEDED by T-18 before dispatch — a per-document summary header is a second copy of the contract that must be hand-kept in sync with its own body, which is the duplication class this repo keeps paying for. T-18's contract section is the original, not a copy, so no synchronisation surface is created. Recorded rather than deleted so the reasoning is not re-litigated. | full | — | skipped |
| T-20 | harvest-wrapped-insight | Fix the insight harvester so a bullet that wraps onto continuation lines keeps them instead of being silently truncated to its first line at exit zero, and make any truncation detectable rather than invisible — the current behaviour discards evidence pointers while echoing text that looks correct. | full | — | done |
| T-21 | stage-cost-attribution | Establish where a pipeline run's spend actually goes before any tuning decision is made: attribute cost per stage and per role from evidence already on disk (recorded sub-agent token counts, stage document sizes, rollback counts) and reconcile that attribution against the external usage report, which currently shows the six framework roles accounting for a small fraction of the project total while observed sub-agent token counts suggest otherwise. Produce a per-role cost table and state plainly which of the two readings is right and why. Measurement only — change no model, no effort setting, no agent definition. | explore | — | done (measured directly by the stream; deliverable at docs/proposals/cost-attribution-2026-08.md — no pipeline run needed) |
| T-22 | stage-model-tiering | Wire the per-agent model and reasoning-effort declarations that the agent definition format already supports but this project has never set, so a role runs at the depth its work needs instead of every role inheriting the top tier — with the tier for each role justified by T-21's attribution and by that role's demonstrated defect-catching record, and with the three verification roles held at full depth unless evidence says otherwise. | full | T-21, T-18 | done (DECLINED, no build) |
| T-23 | review-write-path | Reconcile the review stages' declared capability with the output their own contracts require: either grant them a way to author the document they are told to produce, or state the transcription duty in the contract of the role that actually performs the write — so the obligation stops existing only as a parenthetical in one of the three contracts that implement it. | full | — | done |
| T-24 | operator-obligation-home | Give the release-gating operator obligations a single home that is meant to hold them, replacing their present residence as prose inside string values of a machine-read baseline file — so the set can be read, counted and executed without reconstructing it, and a second copy is not needed to make it usable. | full | — | in-progress |
| T-25 | per-call-context-measure | Make the context a pipeline run actually loads measurable from inside the repository, and attribute it between the orchestrating loop and the delegated stages — so context reductions can be verified against a captured number instead of projected from document sizes, and so the next optimisation is aimed at whichever side actually dominates. Measurement only: change no agent, no document, no budget. | full | — | pending |

## Notes (optional)

- T-23/T-24/T-25 were authorized by the operator on 2026-08-02 after the pool drained and the due entropy sweep returned FINDINGS-PRESENT. T-23 = EP-001, T-24 = EP-002, T-25 = the follow-up T-22's stage 1 identified and correctly declined to self-authorize. Sweep artifact: `docs/features/_supervision/entropy-2026-08-02.md`.
- Deliberately NOT queued, and why: T-17's published residual bypass surface (a known, documented boundary rather than a hidden defect) and the guard's O(n²) scan (performance, not correctness — and any length cap introduced there must fail CLOSED). Both remain in the report's follow-up table. The stale doc-size path label that the sweep recorded as "not rot, just wrong" was fixed directly by the operator rather than queued.
- T-25 was widened on 2026-08-02 after the operator asked whether the 128k-tokens-per-call figure can be reduced. Two independent paths now point at the same unknown: T-22 could not establish the delegated share of spend (6.7%–45%, confidence LOW), and a call-type breakdown suggests roughly 87% of calls (Bash 3186 + Edit 2593 + Read 2440 + Grep 877 of 10456) originate in the orchestrating loop rather than in delegated stages — whose context is also the longest-lived and cannot be cleared. If that holds, optimising stage documents and sub-agents is aiming at the smaller side. UNVERIFIED HYPOTHESES for T-25 to confirm or kill, derived from file sizes rather than measurement: a task's stage documents total ~328 KB (~82k tokens); the always-resident rules/guide/insight layer is ~100 KB (~25k tokens); an agent definition adds 7–19 KB per dispatch. Do not treat any of these as established.
- DELIBERATELY NOT QUEUED alongside T-25: a rule requiring agents to batch or parallelise tool calls. It was proposed and withdrawn — it is a prohibition that depends on compliance with nothing enforcing it (the same shape rejected before T-18 was rewritten into a structural split), and it would be aimed at a target no measurement has confirmed. Revisit only if T-25 shows tool-call frequency, rather than per-call context volume, is what dominates.
- T-24 carries a live warning from its own origin: the consolidated checklist written during the previous run to make these obligations executable had ALREADY drifted from the baseline file within a day (17 obligations reported against an actual 25, because one note's local indices were read as global item numbers). A second copy is not the fix — that document is the evidence.

- T-21/T-22 are a COST wave authorized by the operator on 2026-08-01 after an external usage report showed 30-day spend of roughly 1570 USD with essentially all of it on the top model tier (10420 calls) and 0.18 USD on the cheap tier across 21 calls. Root cause confirmed by inspection: all eight framework agent definitions declare name/description/tools and NO model, so every role inherits the main-loop model. The capability is already supported by the agent definition format — this is unused wiring, not a missing mechanism.
- T-21 exists because two measurements disagree and tuning before resolving that would optimise the wrong thing: the external report attributes about 36 USD total to the six framework roles against a project total of about 540 USD, while the stream directly observed sub-agent token counts of roughly 200k / 248k / 350k / 267k / 297k for the five preceding rows. Either the report's per-agent figure excludes sub-agent internal consumption, or the bulk of spend genuinely sits in the main loop rather than in delegated stages. The answer determines whether model tiering is a 7% lever or a majority one. Do not skip to T-22.
- T-22 constraint carried from evidence, not preference: across the five preceding rows essentially every one of the 11+ rollbacks was raised by code-reviewer, qa-tester or gate-reviewer — a partial wiring reporting success, a guardless residue blessed at exit zero, a round-two fix opening a new hole, a gate assertion vacuous since inception. Downgrading those three trades the pipeline's entire value for a small saving, against a debugging category that itself cost more than the model delta. Prefer tuning reasoning effort (same model, less depth on mechanical work) over swapping to a smaller model, and treat any downgrade of a verification role as requiring positive evidence rather than absence of objection.

- T-18/T-19 are an EFFICIENCY wave authorized by the operator on 2026-08-01, motivated by a measured complaint that each requirement takes too long. Cost anatomy from the five preceding rows, measured not estimated: each task emitted 250-420 KB of stage documents against code changes often in the low hundreds of lines, and the design document grew monotonically across the wave (47 -> 70 -> 58 -> 101 -> 110 KB). Three named causes: the design stage embeds verbatim implementation text the developer then re-emits and the reviewer re-reads (paid three times); rollback changelogs accumulate as new sections in the design document, which every downstream stage re-reads in full; and each stage re-reads all predecessors, making context cost roughly quadratic in stage count. DELIBERATELY NOT DOING: cutting stages or relaxing gates — all 11+ rollbacks this wave caught real defects (a partial wiring reporting green, a guardless residue blessed at exit 0, a round-2 fix opening a new hole, a gate assertion vacuous since inception), so the seven independent views are earning their cost. The target is bytes emitted per stage, not the number of stages. Parallelising stages was assessed and declined: the reviewer is read-only but QA genuinely consumes its findings, so the serial invariant stands.
- T-18's third clause has direct evidence: one T-13 rollback originated in a constraint the dispatching PM invented ("assertion count must stay unmoved") rather than one traceable to a rule; the developer honoured it, omitted anti-revert coverage, QA caught the omission, and the PM withdrew its own constraint. Pure process friction, zero yield. The stream operator's own dispatch briefs carry the same risk and are in scope for the rule, not exempt from it.
- T-20 originates in T-15, where the harvester truncated all four of that task's insights to their first line with every evidence pointer lost, at exit 0, with a console echo reprinting the truncated text so it looked correct; the I.4 guard cannot catch it because it counts bullet lines, not content. Verified by the stream to be condition-dependent, not universal: T-13, T-14 and T-17 wrote single-line bullets and were unaffected, so historical data is intact. T-15 repaired its own entries by hand.

- T-17 is a PRIORITY INTERLEAVE authorized by the operator on 2026-07-31, ahead of the remaining hook-truth-* rows. Origin: T-14's QA stage found it; T-14's PM correctly ruled it out of scope rather than putting a safety-critical change through a pipeline that never examined it. Independently reproduced by the stream before queuing — `rm -rf <outside-path>` blocks with exit 2, but `echo hi && rm -rf <outside-path>`, `true; rm -rf <outside-path>` and `ls | xargs rm -rf <outside-path>` all exit 0. This is a DESIGN blind spot, not a coding slip: `.harness/rules/75-safety-hook.md` documents the trigger as "first token after optional sudo", and its v0.15 out-of-scope list names mv/cp/redirect but never shell operator chaining.
- Decomposed hook-truth-* (4 rows) ← "按最新推荐方案：建立钩子事实的单一来源、断言分层归位（verify_all 只断言仓库属性、本机状态归 status）、install-hooks 自举生成 settings.local.json" (2026-07-31)
- Root cause the hook-truth-* wave addresses: the hook wiring's byte-form is duplicated across ~8 hand-synced sites (distributed template, dogfood local settings, four derivation flows, verify_all F.2/J.1, harness-status §3b/§3c). T-12 (v0.44.0) updated some and missed harness-status — which is why `/harness-status` currently reports the safety hook as DISABLED on this repo even though it is installed and blocking. Duplication, not any single check, is the defect generator; T-13/T-16 remove it, T-14/T-15 put each assertion in the layer that can answer it.
- Dependency shape: two independent chains — T-13 → T-16 (T-16 consumes the spec artifact) and T-14 → T-15 (T-15 may only shed the machine-state assertion once the health report has taken ownership of it). A failure in one chain never blocks the other.
- Known hazards for this wave (from `.harness/insight-index.md`, surface into each dispatch): (a) `harness-status` SKILL asset rows are structurally pinned by `test-supervisor.sh` doc fan-out asserts — update the asserts, don't delete them (T-14); (b) bash 5.2 `patsub_replacement` makes an unescaped `&` in a `${var//needle/repl}` replacement expand to the match — the resilient hook strings contain `& pwsh`, so use the literal `str_replace_all` helper (T-13/T-16); (c) PowerShell is not executable by agents here — the `.ps1` twins are green-by-symmetry only and need an operator PS run (parse-check + driver run) before any release tag; (d) verify_all check count must stay 32 or the version/claim gate G.4 fires (T-15).
- This batch adopts six ideas distilled from a deep read of github.com/mattpocock/skills (research artifact at `c:\Programs\_research\mattpocock-adoption-plan.html`). Mapping: T-02=① shared-language CONTEXT.md, T-03=② grill interview, T-04=③ skill-authoring vocabulary, T-05=④ durable agent-brief, T-06=⑤ tracer-bullet vertical slices + smart-zone sizing, T-07=⑥ deep-module design vocabulary.
- Second wave (Tier-3 "缓议" items, queued on user request 2026-06-20): T-08=⑦ two-axis review principle, T-09=⑨ rejected-decisions memory, T-10=⑧ planning-phase decision map (ASSESS-FIRST — decline if redundant with the existing pool/frontier/stream). These were originally flagged as marginal/heavier; the RA stage must honestly assess value+overlap and descope or recommend decline where appropriate rather than force-build.
- Source repo shallow-cloned at `c:\Programs\_research\mattpocock-skills\` for reference during implementation.
- T-03 depends on T-02 (the grill front-end composes with CONTEXT.md). T-02 and T-07 both edit `solution-architect.md`; T-03 and T-05 both edit `requirement-analyst.md` — serial execution (stream invariant) avoids file conflicts.
- Runtime quirk (carried from T-01): sub-agents have no `Task` tool, so the PM shell runs in the main thread while each stage runs as its own isolated sub-agent.
- T-01 is a prior completed run (DELIVERED v0.31.0, archived); kept for lineage and skipped by resume semantics.

## Column reference

- **ID** — batch-local identifier. **Slug** — kebab-case; becomes `docs/features/<slug>/`. **Goal** — one sentence; pm-orchestrator's task-description input. **Mode** — `full` | `plan` | `goal`. **Depends on** — comma-separated IDs or `—`. **Status** — `pending` | `in-progress` | `done` | `failed` | `blocked` | `needs-human` | `skipped`.
