> Contract portion. Rationale: 03_RATIONALE.md (absent = none written).

# 03 — Gate Review: operator-obligation-home (T-24)

Mode: **full** · deferred-human: defer, do not ask. Finding ids run `H-*` this round; round 1's
`G-*` ids are referenced only in the closure table.

## Dimension audit

| # | Dimension | Verdict | Reason |
|---|---|---|---|
| 1 | Requirement completeness | **WARN** | R-4's union rule and R-6's ordinal restoration now state exactly the property round 1's defect violated, and AC-3's per-ordinal verdict plus "a comparison performed against a mirror does not discharge this criterion" make the decisive criterion non-arithmetic; the holes are AC-1's new second sentence, which is unsatisfiable against the tracked archive under any stated reading of "live tree" (H-4), and E-6's "seven source spans", which is again short of a span that amends a figure the design transcribes (H-1). |
| 2 | Design completeness | **WARN** | Every in-scope behavior keeps a named key, §4.1 re-issues the eight from S-F correctly — I re-derived all eight ordinals independently and T13-5 is recovered, carried in force, and surfaced to the operator per the PM ruling — but AC-1's tracked-live-tree half has no verification row anywhere in §11, and K-15's allocation rule and §4.1's realisation of it disagree on where the `Test-InstallBootstrap` = 32 figure lands (H-3). |
| 3 | Reuse correctness | **PASS** | The reuse audit is accurate at every point I could check: `sync-self.sh` makes sixteen top-level `sync_file` calls over eight script pairs (`:63-93`) with `sync_dir_of_md` (`:35-56`) defined and never invoked; `decision-rubric.md` and `rejected-decisions.md` exist in both trees with no mapping; the three cited decline records exist (`rejected-decisions.md:92`, `:234`, `:298`); declining `I.4`'s INSIGHT-SCAN is right for a single-line ungated entry; and §R-reuse's new row — reuse S-F's own enumeration instead of re-deriving a decomposition — is the correct read of the round-1 failure. |
| 4 | Risk coverage | **PASS** | K-36 now names the risk that actually fired and states the right consequence — the loss is count-invariant, so no count-based check can detect it — and supplies a non-arithmetic instrument (K-46, AC-3, V-15) rather than a stronger count. K-40, K-43, K-22 and K-10 remain genuine traps a developer would otherwise hit, and §R-risk's note that K-38 now has two edits in one value rather than one is the new hazard the two-span excision creates. |
| 5 | Migration safety | **PASS** | Additive and reversible with no schema, migration or flag: rollback is `git checkout --` on seven paths plus one `rm`; §10 writes the destination before the excision; K-21 keeps every edit intra-line with V-14 pinning the file at 31 lines and V-3 proving the parse; the two-span `_qa_note_t13` excision is ordered so the retained K-45 clause survives between the spans and V-6 probes for it. |
| 6 | Boundary handling | **PASS** | B-6 — round 1's only unimplementable boundary — is now executable: K-4a fixes `<artifact state>` as the 40-hex `HEAD` sha and reduces staleness to `git diff --quiet <sha> HEAD -- <field-3 paths>`, with `+dirty` reading as not discharged. B-1 (including grep's exit 1), B-3, B-4, B-5, B-7, B-12 and B-13 are each designed and probed; the only residue is K-4a's generated-artifact clause, which is not realisable for T13-5 (H-10). |
| 7 | Test feasibility | **WARN** | V-1…V-15 are executable verbatim, and I checked V-5's ten absence tokens and V-6's nine presence anchors one by one against the post-excision note text: all ten reach `0`, all nine reach `≥1`, so AC-1's pin-file half and V-6 are jointly satisfiable **in fact**, not only by definition. Against that: V-15's expected string encodes two verdicts that contradict §4.1's own key (H-2), V-12's `pm-orchestrator 296` may read FAIL on a file nobody touched (H-5), and AC-1's tree half has no row to execute. |
| 8 | Out-of-scope clarity | **PASS** | §12 is explicit and correctly implements the PM ruling: §12.4 states that nothing is retired, that T13-5 is carried in force, and that the delivery says the travelling note has not carried it since T-13; §12.3 keeps the archive, `docs/proposals/*` and `docs/features/_supervision/*` read-only, naming S-F `:452-453` specifically because K-46 depends on it; §12.5 discloses four check-count-bearing entries rather than three. |

## Round-1 closure

| id | round-1 severity | status | evidence checked this round |
|---|---|---|---|
| G-1 | FAIL | **closed** | I re-derived S-F `:253-289` myself: eight numbered items, item 5 = AC-10 cross-shell byte-identity at `:264-266`, closing "Eighth and last — no ninth" at `:289`. §4 makes S-F the enumerating source and demotes `_qa_note_t13` to a contributing mirror that "never governs membership or ordinals"; §4.1's T13-5 row recovers the obligation and marks the mirror **silent**. |
| G-2 | FAIL | **closed as raised** | `01` E-6 now reads "seven source spans across four files" and names `hook-truth-spec/04_DEVELOPMENT.md:253-289`; AC-3 compares per ordinal against each of the seven and declares a mirror comparison non-discharging. See H-1 for the set being short again. |
| G-3 | MAJOR | **closed** | §4.1 T13-1 restores both `(template + repo)` scopes and the full type name; T13-2 restores `exit 0`, the Windows byte-forms, the FR-12 report and the idempotent re-run; K-4 field 4 now reads `not stated in source` only when **no** contributing span states an observable. |
| G-4 | MAJOR | **closed** | K-45 gives the widening lead-in R-12 kind 3 and §5 realises it as spans A and B with the clause byte-unchanged between them; K-24 makes the replacement clause follow its colon; V-6 anchors on it. No unit inside an excised span now takes a fourth disposition. |
| G-5 | MAJOR | **closed** | K-44 clause 3 excludes any token appearing in a unit K-23 or K-45 leaves byte-unchanged, which makes the two sets disjoint by construction — and I verified disjointness in fact against the note text (dimension 7). |
| G-6 | WARN | **closed** | R-1 is now satisfied by authority and permission, names the convenience index as the one restatement outside this task's permission, and AC-13 records its removal as an operator action. The closure introduced H-4. |
| G-7 | WARN | **closed, not latent** | K-4a states the form, and B-6 reduces to one executable command. That every entry ships `never` is not a re-run of G-7: G-7 was the absence of a *stated form*, and §3.1 puts discharging out of scope, so an unexercised form is the correct delivery state. RES-D9 names it honestly. |
| G-8 | WARN | **closed** | §9 now weighs mechanism 4 (an `I.6` banned-list entry), and it was re-verified rather than transcribed: `i6_banned` is a data-driven array at `verify_all.sh:640-655`, the scan walks `git ls-files` at `:742`, `baseline.json` is unexempted (`:666-679`), and `step "I.6"` is called once at `:744`/`:747` — so an added record genuinely costs no check id. Ground (a) is independently checkable and correct (`:636-638` and `:673-674` record the verbatim copy; `baseline.json:17-18` are both `58`). |
| G-9 | WARN | **closed** | K-32 states **four** and §9 lists four. |
| G-10 | WARN | **closed, and corrected against the gate — the architect is right** | S-A `:491-492` is "Re-run `pwsh -File .harness/scripts/verify_all.ps1` (expect **32/0/0**)", so id 7 does carry a check count. With S-B `:665` (`PASS 32 / WARN 0 / FAIL 0`), S-D id 14 (both forms plus `31/0/1`) and S-E id 17, §12.5's **four** is right and round 1's three was short by one. |
| G-11 | deferred | **PM ruling implemented correctly** | `01` R-2 and AC-4 require the delivery to name which of the eight the travelling note has not carried since T-13 and to state that the task retires nothing; `02` §4.1 T13-5 carries it in force, §12.4 states no retirement, §16 repeats it. Total stays 25 and I re-checked the arithmetic four ways (dimension audit / §B of the rationale). Not re-litigated. |
| G-12…G-16 | INFO | **all closed** | K-1 now says sixteen calls over eight pairs; K-2 says fourth file / fifth kind; §13 reads 114 and 52; K-29 binds B-9 to the `AI-GUIDE.md` line alone; V-14 pins the file at 31 lines. |
| G-17 | ruling | **BC-7 discharged** | The contract measures **510** lines — two fewer than round 1 — with §R-transcription withdrawn and the new argument in `02_RATIONALE.md`. The cap is not re-ruled. |

Binding conditions: **BC-1…BC-7 discharged** (BC-1 by §4.1, subject to H-1/H-2; BC-2 by E-6 + §4 with the
total re-derived at 25 and R-2/AC-4 not re-opened; BC-3 by K-45; BC-4 by K-44, verified in fact; BC-5 by
K-4a; BC-6 by K-32; BC-7 by the 510-line contract). **BC-8, BC-9 and BC-10 are dischargeable as
designed**: §13 states the pm-orchestrator arithmetic and that a case for a PM line is a rollback, with
AC-10/V-12 verifying (see H-5); V-11 names both search forms and requires the hidden-inclusive one to be
executed and stated, with RES-1 declining the totality claim; §12.5, RES-D8, the ledger header and the
delivery carry the check-count disclosure, now at four sites.

## Findings

| id | severity | owning upstream doc + section | finding |
|---|---|---|---|
| H-1 | **MAJOR** | `01` §0.1 E-6 and §5 AC-3/AC-5; `02` §4 source list, §4.1 rows T13-3 / T13-6 / T13-8, K-15 | `Test-InstallBootstrap` is **32** `Assert` calls is transcribed into three entries from S-F `:288-289` and `:449`, and the same task's archive amends that characterisation: `hook-truth-spec/07_DELIVERY.md:76` records "**n-9 — wording only**: the operator note calls `Test-InstallBootstrap` '32 `Assert` calls'; the source has **29**, one inside a four-tool loop, yielding **32 runtime rows**", corroborated at `06_TEST_REPORT.md:462,485-486`. Neither document is among E-6's seven spans or §4's source list, so AC-3 and AC-5 quantify over a set that excludes them and cannot detect the omission — the structural shape of round 1's G-2, recurring. An operator counting `Assert` calls finds 29 against a ledger that says 32 and cannot tell whether the obligation is discharged. R-5's "carries the amended statement only and cites the document that made the amendment" is the rule that is not applied here, though K-12 applies it correctly for id 2 and K-14 for id 9. Note the design already reads that section — K-16 cites `:73` and `:74`, two lines above. |
| H-2 | **WARN** | `02` §4.1 verdict column (rows T13-1 and T13-3) and §11 V-15 expected | §4.1 defines its own key: `differs↓` = "present but narrower", `differs↕` = "narrower in one place, wider in another". T13-1's fifth column is non-empty (`on Windows run`) and T13-3's is non-empty (the whole second-FC-4-row detail), so both are wider in one place by §4.1's own construction and both are labelled `differs↓`. V-15 then bakes the inconsistency into the expected string. QA doing AC-3 honestly — deriving before reading — will report `differs↕` for ordinals 1 and 3 and register a mismatch against `02` on the one verification row the design calls "this row's decisive risk". Under AC-3's actual three-value vocabulary (matches / differs / silent) every row is right; the ↓/↕ refinement is `02`'s own addition and two of its eight values are wrong. |
| H-3 | **WARN** | `02` §4 K-15, against `01` §2 R-4 and `02` §4.1 | K-15 says the widening is "allocated sentence by sentence, **never duplicated**", into "the lowest-numbered entry it names", and routes `Test-InstallBootstrap` = 32 to **T13-6**. R-4 requires each entry to preserve every expected figure present in **its** source text, and S-F states that figure inside item 8's own span (`:288-289`) and inside item 3's widening parenthetical (`:449`). §4.1 realises it in T13-3 and T13-8 and not in T13-6 — the opposite allocation. A developer following K-15 strictly drops it from T13-8, breaching R-4; a developer following §4.1 breaches K-15's "never duplicated". No precedence between the two keys is stated. |
| H-4 | **WARN** | `01` §5 AC-1 second sentence; inherited by `02` §11 (no row) | AC-1's new clause — "The same search over the tracked live tree returns hits only in the ledger and in the out-of-scope index of AC-13" — cannot pass as written. Fingerprint tokens for ids 1–11 and for the eight are drawn *from* the archive by construction, so the search is guaranteed to hit `_archived/guard-cmd-chain/04_DEVELOPMENT.md`, `_archived/hook-truth-verify-scope/04_DEVELOPMENT.md` and `_archived/hook-truth-spec/04_DEVELOPMENT.md`, all tracked. "Live" is used but never defined; §3.3 supplies the intent ("not a second authority") but not a search scope, and R-1's "One live document lies outside that permission" is false on the literal reading. Separately, §11 has **no** verification row for this half — V-5 runs over `.harness/scripts/baseline.json` only — so the criterion is both undefined and unexecuted. |
| H-5 | **WARN** | `01` §0.4 E-21 and §5 AC-10; `02` §13 and §11 V-12 | `agents/pm-orchestrator.md` reads a line **297** here, by the same Read-tool instrument whose 114 and 52 the architect adopted for `AI-GUIDE.md` and `40-locations.md` after round 1's G-14 — and my own round-1 note of "296" was the less careful reading. AC-10 requires the file "byte-unchanged at **296** lines" and V-12 expects `pm-orchestrator 296`, so if `wc -l` returns 297 both read FAIL against a file no stage touched. No cap is at risk either way (297 + 0 ≤ 300), and RES-D1 already owns the authoritative measurement; what is defective is hard-coding the integer into an acceptance criterion when the criterion is about byte-identity. |
| H-6 | **WARN** | `02` §3 K-46 and §4.1 rows T13-3 / T13-5 | K-46's load-bearing premise is that the mirror holds the reconcile leg at **its ordinal 5**. That premise is asserted in both places and derived in neither, and it is the whole of the falsifier: without it there is no clash with "Items 1, 2, 4, 5, 7 are unchanged". It is derivable and I derived it (rationale §A), but V-15 asks QA to re-run the comparison and hands them no route to it. |
| H-7 | INFO | `02` §4.1 row T13-8 fifth column, against `02` §3 K-16 and `02_RATIONALE` §R-mirror | §4.1 lists "the whole **KNOWN BOUNDS (i)/(ii)** paragraph" as text only the mirror carries, while K-16 two screens later locates (i) at S-F `:429-432` and `hook-truth-spec/07_DELIVERY.md:73` and (ii) at S-F `:418-419` and `07_DELIVERY.md:74` — all four of which I read and all four of which are correct. The paragraph *form* and its attachment to item 8 are mirror-only; the content is archived. `§R-mirror` states the flat universal "Four things exist **only** in the mirror … the whole `KNOWN BOUNDS (i)/(ii)` paragraph", which is both wrong on that member and undercounts: §4.1's fifth column names six distinct mirror-only bodies, not four. Harmless in direction — the union carries the text either way — but it feeds T13-8's `Origin`, and a universal negative with a stated count is exactly the clause an insight in this repo says to attack. |
| H-8 | INFO | `02` §3 K-45 and `02_RATIONALE` §R-K45 | K-45 says S-F `:444-453` "states it **verbatim**" and §R-K45 says "**word for word**". S-F states the same content in different words ("It adds **no ninth** operator item — it *widens three existing ones* … item **3** … item **6** … item **8**"); nothing in S-F reproduces `WIDENED in QA rework 3 (this adds NO ninth item - it widens items 3, 6 and 8):`. R-12 kind 3's actual test is "already has a **permanent home** in that task's archived stage documents", which S-F satisfies, so the classification holds and the disposition is right — but the verb overclaims, and the classification is being sold on the overclaim. |
| H-9 | INFO | `02` §4.1 row T13-3; `02` §3 K-9 | T13-3's widening span is cited `:449-450`; the widening's item-3 clause opens at `:448` ("…item **3** (the driver has one more"). And K-9's list of in-S-F ordinal citations (`:264`, `:284`, `:289`, `:449-453`) omits S-F `:241` — "**A-8 Carried** — operator item **4** / `_qa_note_t13`: `verify_all.ps1:290-291` hard-parses…" — which is the strongest one in the document, because it pins the *mirror's* ordinal 4 as well as S-F's. The claim "no document outside S-F and its mirror cites one" is unaffected; the enumeration under it is short. |
| H-10 | INFO | `02` §3 K-4a final sentence | "Entries whose artifact is generated rather than tracked (T13-2's settings file, **T13-5's pre-commit hook**) pin the **generator named in field 3**" is not realisable for T13-5: its S-F span `:264-266` names `settings.local.json`, the generated pre-commit hook and "step 2", and no generator literal, so field 3 (defined as "every path literal in every contributing span") holds no generator to pin. Unexercised at delivery, since every entry ships `never`. |
| H-11 | INFO | `02` §11 V-5, against §5 K-44 clause 2 | V-5 is labelled "**K-44 tokens only**" and its first token is `ParseFile` — one word, and shared by obligations 12, 13, 16, 17 and T13-1, so it satisfies neither limb of K-44 clause 2. The probe is stricter than the definition and passes in fact (I checked: every occurrence sits inside an excised span, and `_qa_note_t12`'s `ParserError` is not a substring match), so this costs nothing — but the row's label is inaccurate. |
| H-12 | INFO | `01` §0.4 E-19 | E-19 cites `verify_all.sh:823-825` for "exits **1** on `warns > 0`". Those lines are G.4's count-derivation comment; the behavior is at `:932-934` (`(( errors > 0 )) && exit 2` / `(( warns > 0 )) && exit 1`). `02` K-41 cites `:933` and is correct. |
| H-13 | INFO | `02` §13 line-count provenance note | "round 1's 113/51 were off by one (G-14)" is a claim about an earlier draft of this document, which `70-doc-size.md`'s boundary table row 8 sends to `PM_LOG.md`. The `**K-n**`-line and table-row instances (K-9's "Corrected from round 1", K-32's "round 1's 'two' was G-9", RES-D6's "superseded") are covered by the rule's own carve-out — row 2 matches a declared shape before row 8 is reached — but §13's sentence is a bare sentence and reaches row 8. |
| H-14 | INFO | `02` §4.1 row T13-2 | "`:284` requires re-running it after **the rework-2 patch**" narrows S-F: `:283-284`'s "re-run item 2 after this patch" refers to item 8's patch, which S-F `:280` labels "added in rework 2 … **widened in rework 3**", and `:450-451` records that the rework-3 widening added a second PS-side row to the same gate. The abridged column only; the transcription is from the span. |

## Binding conditions

BC-1…BC-7 are discharged and closed. BC-8, BC-9 and BC-10 remain in force as written, on stages 4/6/7.
New this round:

| id | condition | owner stage | discharged by |
|---|---|---|---|
| BC-11 | Before T13-3, T13-6 or T13-8 is written, `hook-truth-spec/07_DELIVERY.md:72-76` and `06_TEST_REPORT.md:462,485-486` are read as contributing spans, and each entry that states the `32` figure carries n-9's qualification with its citation — or the delivery records, in one sentence, why the amendment is not carried. AC-3's and AC-5's re-derivation runs over this widened span set, not over E-6's seven alone. | 4, re-derived 6 | AC-5 + delivery read |
| BC-12 | AC-3's per-ordinal verdict is reported in AC-3's own three-value vocabulary (**matches / differs / silent**). §4.1's `↓`/`↕` subscripts and V-15's expected string are advisory; where a QA-derived subscript disagrees with §4.1 for ordinals 1 or 3, §4.1 is wrong and the report says so rather than reconciling to it. | 6 | AC-3 |
| BC-13 | Where K-15's "never duplicated" would remove a figure, path or exit code from an entry whose **own** S-F span states it, R-4's per-entry union governs and K-15 yields. The developer records each place this arose. | 4 | AC-5 |
| BC-14 | AC-1's tracked-live-tree half is given a stated scope **before** it is claimed, the search is executed, and the exact form and scope are in the report. If the scope excludes `docs/features/_archived/`, the delivery says so and cites §3.3; if it does not, AC-1 cannot pass and this is a rollback to stage 1, not a developer judgement. | 4 and 6 | AC-1 |
| BC-15 | `wc -l agents/pm-orchestrator.md` is captured and reported. If it returns anything other than `296`, the file being **byte-unchanged** discharges AC-10 and V-12's integer is corrected in the report; the file is not edited to match the number under any circumstances. | 4 and 6 | AC-10, V-12 |
| BC-16 | QA records its eight per-ordinal verdicts, and its derivation of the mirror's ordinal for each leg, **before** reading §4.1's table or V-15's expected column. The route by which the mirror's ordinals are fixed is stated in the report (it is derivable from the mirror's own labels — `03_RATIONALE.md` §A). | 6 | AC-3 |
| BC-17 | K-23's kind-3 list ("round histories, label-set corroboration, the t13 `ARCHIVE CORRECTION`, the two `NEW NUMBERED …` sentences, the K-45 clause") is treated as **closed**. Read as an open category it makes K-44 clause 3 swallow anything left in the file and AC-1 becomes vacuous. Any unit a developer wants to keep that is not on that list is a rollback to stage 2. | 4 and 6 | AC-1 |
| BC-18 | The `_qa_note_t13` value takes **two** separate exact-string edits (§5 spans A and B) with the K-45 clause byte-unchanged between them. They are never collapsed into one replacement or one regex, and V-6's `WIDENED in QA rework 3 (this adds NO ninth item` anchor is run after the second edit, not after the first. | 4 | V-6 |

## Pre-answered developer questions

| id | question | answer |
|---|---|---|
| Q-1 | Which text do I transcribe for the eight? | §4.1, and §4.1 alone, is the map — it is the artifact this gate re-derived. Its enumerating source is `_archived/hook-truth-spec/04_DEVELOPMENT.md:253-289`, widened at `:444-453`; `_qa_note_t13` contributes the fifth column and never governs membership or ordinals. Two corrections travel with it: the ordinal-1 and ordinal-3 verdict subscripts are wrong (BC-12), and the `32 Assert calls` figure is qualified at `07_DELIVERY.md:76` (BC-11). Ids 1–10 (`_archived/guard-cmd-chain/04_DEVELOPMENT.md:467-492`, `:830-838`, `:1230-1238`) and id 11 (`_archived/hook-truth-verify-scope/04_DEVELOPMENT.md:662-676`) were checked span by span in round 1 and are unchanged and correct. |
| Q-2 | Is `.harness/operator-obligations.md` measured by any gate check? | Only `I.6`, at FAIL severity (`verify_all.sh:640-655` banned list, `:742` `git ls-files` walk, exemptions at `:666-679` do not cover it). Re-verified this round in both twins: `I.1`–`I.5` measure five fixed paths; `E.1` delegates to `sync-self.sh --check`, whose mapping is sixteen `sync_file` calls over eight script pairs (`:63-93`) with `sync_dir_of_md` (`:35-56`) never invoked; `E.2` walks `.harness/agents/` and `.harness/skills/` only; `F.1` is a fixed pair list; `G.4` is a fixed **eleven**-file array (`:841-853`, re-read this round — the ledger is not in it, so B-10 is not breached); `A.1` excludes `*.md`. A WARN is a hard failure: `verify_all.sh:933` is `(( warns > 0 )) && exit 1`. |
| Q-3 | Can the published count command match its own line, the header, or an example? | No, provided K-7 holds: the fenced line begins with `grep`, and no non-entry line may begin with `- Id: ` — which is why the header must name the seven fields as prose and render no example. On an empty ledger the command prints `0` and exits **1**, grep's documented no-match status. |
| Q-4 | May I use `git show HEAD:.harness/scripts/baseline.json` as AC-8's reference? | No. The file is already modified in the working tree. K-22's copy-aside before the first edit is the only valid reference, and V-4 runs against that copy. |
| Q-5 | The `guard-cmd-chain` archive says "item 11" — is that operator obligation 11? | No — that is that task's own design residual §10.2 item 11 (K-10). Obligation 11 originates in T-15 and its full five-leg text is the blockquote at `_archived/hook-truth-verify-scope/04_DEVELOPMENT.md:662-676`, so no entry takes B-2's `text not located` and the delivery states `0`. |
| Q-6 | Does editing `agents/qa-tester.md` change how this task's QA stage behaves? | No. A dispatched sub-agent loads the version-scoped plugin cache, not the working tree, so the edit governs no run until commit and publish. AC-9's second conjunct is verified by **reading** files: `grep -ln 'baseline\.json' agents/*.md` returns `qa-tester.md` and `developer.md`, and `developer.md:42` names `scripts/verify_baseline.json`, a different artifact. K-25's in-band sentence is the lever that works immediately. |
| Q-7 | Do I need a `templates/common/` twin, a `sync-self` mapping, or a `test-init` assertion? | No, and nothing in the gate would notice either way — `decision-rubric.md` and `rejected-decisions.md` already ship in both trees with no mapping and no check comparing them. All three are out of scope (§3.5, §12.1). |
| Q-8 | The retained K-45 clause says "it widens items 3, 6 and 8" but after the excision there are no items in the file. Is that a defect I should fix? | No — it is a ruling, K-42, applied uniformly: those sentences narrate what T-13, T-16 and T-20 *did*, which stays true, and §3.6 forbids pruning them. The same holds for `_qa_note_t16`'s "items 1-16 … are NOT renumbered" and `_qa_note_t20`'s "from 16 to 17". Do not tidy them; V-6 will fail if you do. |

## Verdict

**APPROVED FOR DEVELOPMENT** — conditional on BC-8…BC-18.

The defect that blocked round 1 is closed at the root rather than patched: the eight are re-issued from
the source that enumerates them, the recovered obligation is carried in force under the PM's ruling, the
mirror's contribution is preserved by R-4's union so the correction did not trade one loss for its
reverse, and the decisive instrument is no longer arithmetic. Nothing found this round loses an
obligation. What is open is one transcription-fidelity defect with a named span (H-1), two internal
contradictions inside the re-issued map (H-2, H-3), one acceptance criterion that needs a stated scope
before it can be run (H-4), and one hard-coded integer that can fail on an untouched file (H-5) — each
bounded, each located, and each dischargeable by a stage that can execute. A third design round would be
spent relabelling two subscripts and adding one citation, which is the oscillation this repository's own
insight index says to recognise rather than to fund.
