# PM Log — g4-version-decouple (T-010)

Mode: full (7-stage) · Started: 2026-06-06 · Slug: g4-version-decouple

## Pre-flight (PM)
- `.harness/intervention.md`: absent — clean start.
- Related: T-008 built G.4 (the count+version gate); T-009 flagged the version treadmill. This decouples the prose count claims from the version.
- insight-index lines surfaced: L13/L20 (PS/Bash symmetry on G.4 edits), L14 (count-claim drift class — note: decoupling must NOT reintroduce silent count drift; G.4 must still gate the COUNT), the T-008 exhaustive-sweep lesson (find ALL version-bearing count claims), the T-008 "new check = version-worthy" lesson (N/A — no new check).
- Ground truth (PM read): the 6 claims enumerated in INPUT, all "32 ... at v0.21.1", all living-current-state.
- Decision principle (user): UX / SE-standard / long-term maintainability (DRY: version has one SoT = plugin.json/G.3; prose count claims shouldn't duplicate it). Autonomy: PM decides+executes+commits/pushes.

## Stage transitions
- 2026-06-06 — Stage 1 (Requirement Analyst) dispatched.
- 2026-06-06 — Stage 1 COMPLETE (no user bounce). G.4 = 11 claim rows + CHANGELOG `[version]` check. Split: 6 version-bearing rows (1,2,3,4,5,10 — AI-GUIDE:36/69, dev-map:60/133, 40-locations:25, manual-e2e:3) → count-only; 5 already count-only (6,7 badges; 8,9; 11 baseline) unchanged; CHANGELOG check unchanged. Exhaustive sweep confirms set closed at exactly 6. Hard AC-2: G.4 must STILL FAIL on wrong COUNT (T-008 property — only version coupling removed). AC-3 proves treadmill gone (count-unchanged bump passes w/o touching the 6; G.3 still forces badges). AC-4 exhaustive removal. 4 decisions → SA. intervention.md: absent. Stage 2 (Solution Architect) dispatched.
- 2026-06-06 — Stage 2 COMPLETE (02_SOLUTION_DESIGN.md). D1=all 6 living → drop "at vX" (row-4 nuance: whole `(at v0.21.1)` parenthetical goes). D2=count-only shape+expect for rows 1-5,10 both shells, C-1/C-2 walk-through proves 32→31 still FAILs + no historical false-PASS (Contains short-circuits). D3=rows 6-9,11 + CHANGELOG check + `$version` derivation (kept, consumed by CHANGELOG) + tripwire byte-identical. D4=net strictly-better; v0.21.2 bump self-demonstrates AC-3. Set closed at 6 (grep); no templates/**/verify_all (parity non-issue); single-Dev. 2 impl traps: R-7 row-4 paren-orphan, R-3 PS/Bash asymmetry. intervention.md: absent. Stage 3 (Gate Reviewer) dispatched.
- 2026-06-06 — Stage 3 returned **GATE VERDICT: APPROVED FOR DEVELOPMENT** (no conditions; 03_GATE_REVIEW.md persisted by PM). All 8 dimensions PASS, no findings. AC-2 crux verified: 32→31 drift still FAILs all 6 decoupled rows both shells (each new expect keeps count + discriminator). No historical/HTML false-match (short-circuit order verified in live code + grep). Exhaustiveness confirmed (6, no 7th). Row-4 whole-parenthetical removal, `$version` retention (CHANGELOG), tally=32, PS/Bash symmetry all verified. intervention.md: absent. Stage 4 (Developer, single-Dev) dispatched — decouple ONLY (v0.21.2 bump is a separate delivery step).
- 2026-06-06 — Stage 4 returned **DEVELOPMENT COMPLETE** (04_DEVELOPMENT.md). 6 prose claims de-versioned (row-4 whole parenthetical removed, one-space collapse); G.4 count-only rows 1-5+10 both shells (every expect keeps count 32 → AC-2 holds); rows 6-9,11 + CHANGELOG check + `$version` derivation/guard + tripwire byte-identical; no Step added/removed (tally 32). Version NOT bumped (stays 0.21.1). **verify_all PS 32/32 SH 32/32, both 0W/0F, G.4 PASS.** AC-4 grep = zero version-bearing count claims (only `_archived/**` + this task's docs). intervention.md: absent. Stage 5 (Code Reviewer) dispatched.
- 2026-06-06 — Stage 5 returned **CODE REVIEW VERDICT: APPROVED** (05_CODE_REVIEW.md persisted by PM). AC-2 preserved all 6 rows both shells (every expect keeps count 32 + discriminator; 32→31 → FAIL verified); 6 prose edits clean (row-4 R-7 one-space, no orphan parens); rows 6-11/CHANGELOG/`$version`+guard/tripwire untouched; no Step added (tally 32); no version bump (correct); AC-4 independent grep zero; no HTML/history false-match; PS/Bash symmetric. 1 NIT: block comment still says "version-anchored" for decoupled rows (cosmetic) → PM folds into v0.21.2 ship pass. intervention.md: absent. Stage 6 (QA Tester) dispatched.
- 2026-06-06 — Stage 6 returned **QA VERDICT: FAIL (1 bug)** (06_TEST_REPORT.md). 5 of 6 AC-2 rows catch drift; AC-3 treadmill-gone proven (count-unchanged 0.21.1→0.21.2 bump → G.4 FAILs only on CHANGELOG/G.3, never prose); AC-4 grep zero; historical immunity OK. **D-1 (MAJOR, T-010 regression — Gate+CR both MISSED it):** dev-map:60 count drift silently UNCAUGHT — row-3 de-versioned `expect="32 checks"` is a substring of row-4's `runs all 32 checks` on dev-map:133 (same file, whole-file `.Contains`), so L60→`(31 checks)` still PASSes. Pre-decouple the expect `"32 checks at v0.21.1"` was L60-unique. Fix: row-3 `expect="(32 checks)"` + shape `\(\d+ checks\)`, both shells (L60 literally reads `(32 checks)`; L133 has no `(` before count post-decouple). D-2 NIT: stale "version-anchored" comment (fold in). **Rollback #1 at stage 4 (QA bug → developer).** intervention.md: absent. Re-dispatching Developer.
- 2026-06-06 — Stage 4 rework COMPLETE. D-1 reproduced then fixed: row-3 `expect`→`(32 checks)`, `shape`→`\(\d+ checks\)`, both shells (L60-unique; L133 has no `(` before count). Post-fix AC-2 dev-map pair: L60→`(31 checks)` FAILS, L133→`runs all 31 checks` FAILS, revert→PASS, both shells. Collision re-check: AI-GUIDE pair clean (`32/32` L36-only, `32 checks` L69-only); row-5 different file. D-2 comment de-versioned + same-file uniqueness invariant documented in-code. verify_all PS 32/32. NOTE: verify_all.sh full run intermittently times out on pre-existing I.6 `git ls-files×grep` MSYS fork-storm (env, not T-010) — dev/QA used verbatim G.4-block probe; bash G.4 PASS. intervention.md: absent. Re-dispatching Code Reviewer (re-review #1).
- 2026-06-06 — Stage 5 re-review returned **APPROVED** (appended to 05_CODE_REVIEW.md). D-1 fix verified both shells (row-3 file-unique `(32 checks)`; L60-alone drift now FAILs). Systematic same-file-uniqueness audit of all 11 rows: every multi-row file (AI-GUIDE/dev-map/README/README.zh-CN) has file-unique expects; cross-file same-expect non-interfering (file-pinned). D-2 comment fixed + invariant documented in-code. No regression, no bump. intervention.md: absent. Re-dispatching QA (re-verify #1).
- 2026-06-06 — Stage 6 re-verify returned **QA VERDICT: PASS** (06_TEST_REPORT.md, D-1 marked RESOLVED). D-1 closed both shells (dev-map:60→`(31 checks)` now FAILs); full 6-row AC-2 matrix all catch drift both shells; AC-3 treadmill-gone re-confirmed; no new false-match (row-3 `(32 checks)` file-pinned vs README:159); clean tree; verify_all PS 32/32. bash via verbatim-G.4-probe (full verify_all.sh wedged on pre-existing I.6 MSYS fork-storm, exit 124 — env, not T-010). 0 new defects.
- 2026-06-06 — PRE-DELIVERY: ship bump **v0.21.1→v0.21.2** (patch — internal G.4 refactor). Self-demonstration of T-010: the bump touches ONLY plugin/marketplace/README badges + CHANGELOG, NOT the 6 now-version-less prose claims. Dispatching developer.
