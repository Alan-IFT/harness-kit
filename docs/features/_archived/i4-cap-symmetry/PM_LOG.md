# PM Log — i4-cap-symmetry (T-009)

Mode: full (7-stage) · Started: 2026-06-05 · Slug: i4-cap-symmetry

## Pre-flight (PM)
- `.harness/intervention.md`: absent — clean start.
- Related: T-008 (just delivered v0.21.0) surfaced this as a pre-existing deferred item; CR/QA both flagged it out-of-scope-but-real.
- insight-index lines surfaced: L13/L20 (PS/Bash symmetry — `Measure-Object` vs `wc -l`/`grep -c` must agree), L14 (count/derive-from-source ethos), the new T-008 G.4 lesson (a new check is version-worthy — only relevant if SA adds a check).
- Ground truth (PM read): I.4 PS=`Measure-Object -Line` total lines (verify_all.ps1:398); I.4 SH=`wc -l` total lines (verify_all.sh:419); archive-task rotates on `^\s*-\s+` DATA lines (archive-task.ps1:71). Intent (AI-GUIDE:34) = evidence/data lines. File currently 23 data / ~33 total.
- Decision principle (user): UX / SE-standard / long-term maintainability. Autonomy: PM decides+executes+commits.

## Stage transitions
- 2026-06-05 — Stage 1 (Requirement Analyst) dispatched.
- 2026-06-05 — Stage 1 COMPLETE (no user bounce). Grounded: insight-index now 33 total / 25 data / 8 header (grew via T-007/T-008). Key finding: `wc -l` (newline terminators) vs PS `Measure-Object -Line` (line records) diverge by one on a no-trailing-newline final line → root of the L13 split, separate from the metric question. archive-task ALREADY uses the data-line regex (3 of 4 sites intent-aligned; only I.4 wording reads "total"). 4 decisions → SA (D1 metric DATA-vs-TOTAL, D2 rotate-now, D3 byte-identical cross-shell count incl. CRLF/no-trailing-newline edges, D4 baseline-vs-manual canonical). AC-7 conditional doc-sync guarded by I.6/E.4b/E.5. intervention.md: absent. Stage 2 (Solution Architect) dispatched.
- 2026-06-05 — Stage 2 COMPLETE (02_SOLUTION_DESIGN.md). D1=DATA lines (`^\s*-\s+`); D2=no rotation (25<30, WARN clears, zero index edits); D3=byte-identical filtered line-count reusing archive-task regex (`@(Get-Content|Where{...}).Count` ≡ `grep -c '^[[:space:]]*-[[:space:]]' ||true`), immune to trailing-newline/CRLF off-by-one; D4=baseline.json canonical, manual-e2e corrected from captured run (numbers deferred to Dev). archive-task NEEDS NO change (already data-line metric). Doc-sync "lines"→"evidence lines" confirmed safe (I.6 banned-phrases are CLAUDE.md-composition only; E.4b path-match; E.5 presence). No new check (count stays 32). Single-Dev mode. 2 DESIGN RISKs: R3 (D4 from fresh run not stale 251/227), R5 (verify I.4 Step-description not test-pinned before rename). intervention.md: absent. Stage 3 (Gate Reviewer) dispatched.
- 2026-06-05 — Stage 3 returned **GATE VERDICT: APPROVED FOR DEVELOPMENT** (with 5 dev-time conditions; 03_GATE_REVIEW.md persisted by PM). D3 byte-identical count VERIFIED sound; D1/D2/D4 verified; I.4 desc NOT test-pinned (rename safe); doc-sync safe (I.6/E.4b/E.5); no new check (count stays 32). Findings all WARN/cosmetic: F-1 design's `set -e` premise wrong (live `set -uo pipefail`; keep `||true` anyway), F-2 don't edit 3 `.tmpl` cap siblings (OOS F.4), F-3/F-4 cosmetic. Conditions C-1..C-5. intervention.md: absent. Stage 4 (Developer, single-Dev) dispatched.
- 2026-06-05 — Stage 4 returned **DEVELOPMENT COMPLETE** (04_DEVELOPMENT.md). I.4 both shells now count evidence lines (`^\s*-\s+`): PS `@(Get-Content|Where{...}).Count`, SH `grep -c '^[[:space:]]*-[[:space:]]' ||true`; labels "lines"→"evidence lines". Rule doc-sync 05-insight-index.md:5,25 + 70-doc-size.md:27 (NOT the 3 .tmpl siblings, C-3). baseline reconcile: fresh run PS 251/SH 213; manual-e2e:3 227/191→251/213; baseline.json already 251/213 canonical (no edit). C-2 grep confirmed no test pins I.4 desc. **verify_all PS 32/32 SH 32/32 — BOTH 0 WARN 0 FAIL; SH I.4 WARN RESOLVED, cross-shell split gone (both PASS count 25).** G.4 still 32; I.6/E.4b/E.5/J.1 untripped. No drift. intervention.md: absent. Stage 5 (Code Reviewer) dispatched.
- 2026-06-05 — Stage 5 returned **CODE REVIEW VERDICT: APPROVED** (05_CODE_REVIEW.md persisted by PM). I.4 counts 25 evidence lines (<30) identically both shells; WARN genuinely RESOLVED (right metric, `>30` threshold intact — not masked); tally held at 32; doc-sync + C-3 correct. 1 MINOR (BRE-vs-ERE "verbatim" wording, behavior identical) + 2 NIT (insight-index:3 header + dev-map:134/project-overview.html:299 stale 227/191 — all OOS). Verification ceiling: CR couldn't re-run test-init → QA to confirm 251/213 + both-shell parity on synthetic fixtures. intervention.md: absent. Stage 6 (QA Tester) dispatched.
- 2026-06-05 — Stage 6 returned **QA VERDICT: PASS** (06_TEST_REPORT.md): both shells 32/32 PASS 0W/0F; 7-fixture cross-shell parity matrix all byte-identical; QA PROVED the old metric diverged on no-trailing-newline (bash wc -l=31 vs PS=32 — the L13 bug); test-init re-run = 251/213 (matches baseline+manual-e2e); archive-task rotation now aligns with I.4 threshold; gate still bites (not masked). 0 defects. Clean tree.
- 2026-06-05 — PRE-DELIVERY: v0.21.0 already pushed → T-009 internal fix ships as patch **v0.21.1**. G.4 (current-version-anchored) requires count-claim versions move in lockstep. Dispatching developer for the v0.21.1 ship bump.
