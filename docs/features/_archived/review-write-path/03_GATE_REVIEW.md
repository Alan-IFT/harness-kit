> Contract portion. Rationale: 03_RATIONALE.md (absent = none written).

## Dimension audit

| # | Dimension | Result | Reason |
|---|---|---|---|
| 1 | Requirement completeness | PASS | R-1 … R-12 and B-1 … B-12 are unchanged and each still names a concrete falsifiable state, and AC-8's own stated purpose at `01:224-227` — it "distinguishes 'the three contracts now read consistently' from 'the arrangement holds when a review stage runs'" — is itself a usable rubric, so the criterion needed an executable reading rather than a rewording. |
| 2 | Design completeness | PASS | I re-ran AC-10's mutation on paper against the write-act rule: deleting P3 leaves `agents/pm-orchestrator.md` with P1/P2/P4–P7 anaphoric to a deleted antecedent and no sentence stating a disposition for the arrived body; deleting G1/K1 leaves G2/G3/G4/G8 naming no role for any of the four declared paths — the rule is now satisfiable simultaneously with P7/G3/G4/K3/K4 and the mutation bites (G-1 cleared). |
| 3 | Reuse correctness | PASS | Every site cited this round re-opens and resolves — `AI-GUIDE.md.tmpl:51,:53`, `docs/workflow.md:18` (and `:10`,`:12`), `pm-orchestrator.md:26-34,:84-88,:140-146,:181-182,:234-237,:241-242,:274-275`, `gate-reviewer.md:14-16,:29-30,:32-33,:36-37,:57,:74`, `code-reviewer.md:14-16,:31-32,:33-34,:36-39,:88,:95,:111`, `supervisor.md:97,:99`, `qa-tester.md:4`, `marketplace.json:12-15`, all five ⊕ template surfaces and `CHANGELOG.md:43` — and where the design corrects my round-1 line numbers (`60-tool-handoff` twin at `:32-33` not `:34`; `CHANGELOG.md:43` not `:42`) the design's figures are the accurate ones. |
| 4 | Risk coverage | PASS | R8 now carries rollout staleness with D-8's four-link propagation chain behind it, R1 × R2 is composed at the G8/K8 header instead of left as two separately-mitigated risks, and RES-1 … RES-5 each state what remains uncovered and where it would be detected rather than implying nothing remains. |
| 5 | Migration safety | PASS | Migration item 1 now states the change does **not** take effect for the next dispatched stage, which I re-established independently this round — `.claude-plugin/marketplace.json:12-15` sources the cache from the published GitHub repo, exactly one version dir `0.44.0` exists, and its `agents/gate-reviewer.md:12-32` is character-identical to the contract loaded into this session — and rollback stays `git checkout` with no data, no state file and no flag. |
| 6 | Boundary handling | PASS | B-4's third case is closed at write time by a bracket built entirely from text both schemas already carry (`gate-reviewer.md:14-15` plus its terminal `## Verdict`; `code-reviewer.md:14-15` plus its terminal `## Verdict`), and the residue — a loss strictly interior to the brackets — is bounded as RES-2 with its detection point named. |
| 7 | Test feasibility | WARN | AC-1 … AC-7 and AC-9 … AC-12 are executable, and AC-8's protocol is both executable and genuinely falsifiable — I confirmed the control artifact exists and must fail, since `~/.claude/plugins/cache/harness-kit-marketplace/harness-kit/0.44.0/agents/gate-reviewer.md:12-32` declares no opening line at all — but four protocol details are unspecified: the probe sub-agent's type, AC-9's `PM_LOG.md` target, the evidentiary bound on observable (d), and which sentences DEV-2's grep is expected to hit (G-13 … G-16). |
| 8 | Out-of-scope clarity | PASS | `01` §3, the design's out-of-scope clarifications and the now-total "files deliberately not changed" table give the same boundaries, the release-and-plugin-update acts are explicitly excluded ("this task edits the source, it does not ship it"), and the one over-build attractor (applying G6/K5 to `agents/supervisor.md`) is named and deferred as RES-5. |

## Findings

| id | severity | owning upstream doc + section | finding |
|---|---|---|---|
| G-1 | CLEARED | `02` D-3 "The write-act uniqueness rule", D-4/D-5, AC-10 table | The unsatisfiable constraint is gone: uniqueness is now scoped to a *write-act statement* (names/addresses the acting role **and** applies a create/write/overwrite/replace verb to one of four declared paths), P7/G3/G4/K3/K4 are rewritten as anaphors, `PM_LOG.md` is placed out of scope with a stated reason, and my own re-run of the three mutations leaves AC-1 and AC-2 unanswered in every case — the defect that made this BLOCKED ON DESIGN is repaired. |
| G-2 | CLEARED | `02` D-8, migration item 1, R8, AC-8 protocol, DEV-1 | Migration item 1 is inverted to the true statement, D-8 publishes the propagation chain (commit → push → marketplace publish → `/plugin` update → new session), the *cause* of the version gap is handed to a shell-holding stage as DEV-1 rather than asserted, and AC-8 no longer depends on the loader delivering the edited text; I re-verified every observation D-8 rests on. |
| G-3 | CLEARED | `02` D-4 G8 + "Why G8 carries fidelity and not only paths", R2, RES-1 | G8/K8 now carry verbatim/complete/add-nothing, replace-don't-append and return-don't-truncate alongside the target paths, and this does **not** collide with the C-1 rule: the header applies no write verb to a path while naming an acting role, so it classifies as a constraint, not a second write-act statement — and RES-1 states plainly that the header instructs without enforcing. |
| G-4 | CLEARED | `02` "Files deliberately not changed" (⊕ rows) | All four surfaces I named plus a fifth the architect found are now dispositioned, and I re-opened each: `70-doc-size.md.tmpl:38` (passive rationale-sibling definition), `00-core.md.tmpl:15` (filename set), `_ai-native-prompt.md:22-23` (`RESERVED_NAMES` list), `60-tool-handoff.md` twin `:32-33`/`:122` (read list + resume rule), the six `dev-*.md.tmpl` read lists, the two supervise fixtures, `docs/tasks.md:15,:29`, `CHANGELOG.md:43` — none carries a write attribution and none needs an edit. |
| G-5 | CLEARED | `02` "Audit-set derivation" | The headline is corrected to line granularity and is now exactly true: S1 returns `docs/workflow.md:10` and `:12`, S2 returns `:10`, and neither returns `:18` — the sentence that has to change — which I confirmed by reading `:8-21`. |
| G-6 | CLEARED | `02` D-3 C1/C2 rows, DEV-3 | C2's surviving authority is moved to `:181-182`, which I verified is mode-independent while `:274-275` sits inside a subsection `:241-242` orders `goal` mode to skip; and C1 is re-justified as content-preserving **by obligation** with DEV-3 requiring the before/after text — the correct instrument, since the fold's safety rests on the clause and not on the row-10 classification. |
| G-7 | CLEARED (bounded) | `02` API contracts / error envelope, RES-2 | P6's bracket check now covers the tail-truncation case B-4 forbids, using each schema's own first and last line and adding no protocol, and the remaining case — an interior loss preserving both brackets and every header-named portion — is named as RES-2 with its detection point (AC-8(b) at stage 6) rather than left implicit. |
| G-8 | CLEARED | `02` L-25/L-26, D-7; `agents/solution-architect.md:70` | Unchanged from round 1: both stage-2 production edits are inside the architect's own contract, are present in the established shape, and are carried as ledger rows marked done so the Developer does not re-apply them. |
| G-9 | MINOR (open, C-5) | `02` D-3 line budget; `01` E-15 | My round-1 basis for 294 does not reproduce: a read of `agents/pm-orchestrator.md` from `:290` with no limit terminates at `:293`, so the base is 293 or 294 and my earlier claim of a blank `:294` was not reproducible — the budget is safe either way (worst case 294 − 9 + 12 = 297 ≤ 300, best case 296), but no figure in this task is measured until C-5's live `wc -l` runs. |
| G-10 | CLEARED (recommendation accepted) | `02` D-8 "G-10 belongs here", RES-4; `01` §0.4 | I accept the recommendation not to route back: the second mechanism (a callee running a pre-schema build of its *own* contract) is an addition to §0.4 that falsifies no R-*, B-*, AC-* or ledger row and leaves §0.4's operative conclusion intact, it is a cross-task truth rather than a requirement statement, and it is not derivable from the codebase in ten minutes — so stage-7 insight harvest is its correct home, not an analyst round. |
| G-11 | CLEARED | `02` R7, S5; `01` E-17 | Unchanged from round 1: `verify_all.sh` names the two role identifiers once (`:74`) and carries no `tools:` matcher, so no check gates a tool grant in either direction. |
| G-12 | MINOR | `02` D-3 C1 row, C2 row | Two small imprecisions in the condensation justification: the literal token `harness-kit:developer` **does** appear at `:136` as the naming-convention example, so C1's "appears nowhere else in `:133-146`" is true of the branch *instruction* but false of the token; and `:236` carries "append a one-line entry referencing this folder", a specificity `:181` ("update `docs/tasks.md` with the delivery result") does not restate — the outcomes stay safe because DEV-3 publishes the fold and `:181` still orders the act, but the published reasons are looser than the sites. |
| G-13 | MINOR | `02` D-3 write-act rule conjunct (i) and DEV-2; AC-10 "Expected residue" column | The stated rule and DEV-2's operational check are not the same rule and the gap is not named: conjunct (i) "names **or addresses** the acting role" is vacuously true of every sentence in `agents/pm-orchestrator.md` (the whole file is second person), so uniqueness there rests entirely on conjunct (ii), while DEV-2's path-token grep will hit three sites the design does not pre-classify — the stage-table rows at `:30`/`:32` (which AC-10's expected-residue column omits, though they are the likeliest thing a mutation reader mistakes for an answer to AC-2), P8's new marker (whose wording, unlike G8/K8's, carries no "names no role" constraint), and `gate-reviewer.md:33`'s quoted opening line. |
| G-14 | MINOR | `02` AC-8 protocol steps 5-6, observable (d) | Observable (d) — "the file is byte-identical to the returned body" — is measured against an artifact the subject under test produces: the PM is both the writer being tested and the only party that can capture what the sub-agent returned, so a PM that reshapes both identically passes (d); the schema-absolute observables (a)–(c) are what actually carry the weight, and the protocol does not say so. |
| G-15 | MINOR | `02` AC-8 protocol steps 1 and 4 | The probe sub-agent's **type** is never named, and it changes what step 4 measures: if the probe is `harness-kit:gate-reviewer` it already loads the 0.44.0 build, so the differential control becomes partly a no-op and the precedence line's own effectiveness becomes an untested confound, whereas a general-purpose sub-agent carrying only the supplied contract text gives a clean control — the failure direction is conservative (NOT EXECUTED, not a false pass), which is why this is MINOR. |
| G-16 | MINOR | `02` AC-9 protocol | AC-9 requires observing "that the round record went to `PM_LOG.md` only" without naming a scratch `PM_LOG.md`; AC-8's outside-the-working-tree constraint is stated once and only in AC-8's preamble, and a probe that appends to the live `docs/features/review-write-path/PM_LOG.md` would corrupt the round-count evidence that `agents/pm-orchestrator.md:16`'s three-consecutive-rollback rule and the supervisor both read. |
| G-17 | MINOR (accepted shape) | `02` AC-8 protocol preamble; `01` §0.4, §3.5 | The design places on the PM an act it can learn only from `02_SOLUTION_DESIGN.md` — §0.4's own mechanism one level up — but this is the only available shape, since `agents/qa-tester.md:4` (`Read, Write, Edit, Glob, Grep, Bash, PowerShell`) grants no `Task` and `01` §3.5 puts every relevant tool grant out of scope; the failure is loud rather than silent (QA reports NOT EXECUTED, and `agents/pm-orchestrator.md:188` blocks stage 7 on a non-PASSing stage 6) and recoverable in-round. |
| G-18 | CLEARED | `02` AC-8 protocol step 4, R8; cross-task memory (T-16 QA D-1) | The must-fail control is real, not asserted: the artifact exists on disk and I read it — `0.44.0/agents/gate-reviewer.md:12-32` renders `## What you produce` as a four-item numbered list declaring **no** opening line and none of the schema's section names, so a run under it cannot produce observable (a) or (b) without inventing them; the bound worth stating is that this control is two minor versions behind and so validates the apparatus ("contract text drives the observable") rather than isolating *this task's* sentences — the necessity claim for those rests on AC-10's three mutations, and the two arms together satisfy the T-16 discipline that neither satisfies alone. |
| G-19 | CLEARED | `02` AC-8 protocol preamble; `.harness/scripts/verify_all.sh` | The probe cannot perturb the AC-6 tally or the freeze evidence: probe artifacts live outside the working tree, and even in-tree they would be nearly invisible — `verify_all.sh` touches `docs/features/` only at `:566-612` (a `find … -maxdepth 3 -name SUPERVISION_REPORT.md` scan) and at `:637`/`:677`, where the whole subtree is **exempt** from I.6, and no check greps `## Adversarial tests` at all. |

## Binding conditions

| id | condition | owner stage | discharged by |
|---|---|---|---|
| C-1 | **MET at round 2.** The uniqueness constraint is reconciled into the write-act rule; satisfiable simultaneously with P7/G3/G4/K3/K4, and the mutation re-run confirms it still bites. Nothing further is owed. | solution-architect | G-1 |
| C-2 | **MET at round 2.** D-8 states which build a dispatched stage loads and what makes edited text govern a run; the acceptance bar is settled as controlled reproduction and this gate rules that settlement admissible (`03_RATIONALE.md` §R7) — **no human ruling is required**. The empirical loader question is handed to DEV-1, not assumed. | solution-architect | G-2 |
| C-3 | **MET at round 2.** G8/K8 carry the fidelity constraints, name no role, and do not collide with C-1; RES-1 names what the header cannot enforce. | solution-architect | G-3 |
| C-4 | **CARRIED.** The stage-4 AC-5 publication is total over every derived site, including the five ⊕ rows, each with its disposition and live line numbers re-derived rather than inherited. | developer | G-4 |
| C-5 | **CARRIED, and now load-bearing.** Every line count is established by a live `wc -l` before and after and published in `04_DEVELOPMENT.md`. This gate's round-1 figure of 294 did not reproduce this round (a read from `:290` to end terminates at `:293`); treat no count in `01` or `02` as measured until this runs. | developer | G-9 |
| C-6 | **MET at round 2 (no-op confirmed).** OQ-1's direction is unchanged, so `.harness/rejected-decisions.md`'s `reviewer-write-grant` record and `CONTEXT.md`'s two terms stand unrevised. | solution-architect | G-8 |
| C-7 | Before running AC-8 step 1, state and publish the probe sub-agent's **type**, and use one that carries no competing build of the contract under test — otherwise step 4's differential control measures the loader rather than the supplied text. | pm-orchestrator (executed) / qa-tester (verified) | G-15 |
| C-8 | AC-9's second round runs against a **scratch** `PM_LOG.md` outside the working tree, on the same terms as AC-8's artifacts. No probe writes into `docs/features/review-write-path/PM_LOG.md`. | pm-orchestrator (executed) / qa-tester (verified) | G-16 |
| C-9 | QA records in `06_TEST_REPORT.md` that observable (d) is captured by the party under test, and states its verdict for AC-8 on (a)–(c) as the independent observables, with (d) and (e) as corroboration. | qa-tester | G-14 |
| C-10 | DEV-2's published classification covers every path-token hit including the stage-table rows at `:30`/`:32`, P8's new marker, and `gate-reviewer.md:33`'s quoted opening line; if P8's marker classifies as a second write-act statement it is reworded, not reclassified. | developer | G-13 |

## Pre-answered developer questions

| id | question | answer |
|---|---|---|
| Q-1 | Do I edit `agents/*.md` or a `.harness/agents/` copy? | Top-level `agents/*.md`, directly, no sync. A live glob of `.harness/agents/**` returns no files; `AI-GUIDE.md:13` and `.harness/rules/70-doc-size.md:27` both name the plugin-native top-level as the single source. This also confirms the single-Developer partition assignment. |
| Q-2 | How many lines do C1 and C2 actually free, and what is the base? | C1 is 7 physical lines (`:140` blank, `:141` fence open, `:142-144` body, `:145` fence close, `:146` blank), C2 is 3 (`:235` blank, `:236`, `:237`, with `:234` the closing fence that must survive) — gross 10, net 9 after the fold's one line. The base is **not settled**: 293 and 294 are both defensible from reads, and C-5 binds you to `wc -l`. Do not infer either figure. |
| Q-3 | Will new prose in `agents/pm-orchestrator.md` trip the I.6 retired-claim guard? | No. All fourteen banned entries at `verify_all.sh:640-655` concern CLAUDE.md composition/regeneration or the zh "全程中文" claim; none touches authorship or write attribution. `docs/features/` is exempt regardless (`:637`, `:677`). |
| Q-4 | Is `docs/workflow.md` kept in lockstep with its template twin by anything? | No. `sync-self.sh` carries mappings 2-9, all eight script pairs (`:62-90`); the agent mapping was removed at v0.30. `verify_all` E.5 tests presence of five doc paths only. Edit both copies in the same round — L-23 and L-24. |
| Q-5 | Which copy of the reviewer contract does a live stage-3 dispatch load? | **Resolved at round 2.** The version-scoped plugin cache: `~/.claude/plugins/cache/harness-kit-marketplace/harness-kit/<installed-version>/agents/<name>.md`, populated from the published GitHub repo (`.claude-plugin/marketplace.json:12-15`). Exactly one version dir exists here (`0.44.0`). `skills/harness/SKILL.md:36` dispatches `harness-kit:gate-reviewer`, so your edit does **not** govern a run until commit → push → publish → `/plugin` update → new session. DEV-1 asks you to establish the *cause* of the gap from a shell; if the cache turns out to already carry working-tree text, say so and AC-8 reverts to a direct dispatch. |
| Q-6 | Are L-25/L-26 mine to apply? | No. Both are already applied and verified present (`CONTEXT.md:54-64`, `.harness/rejected-decisions.md:282-308`). Re-applying would duplicate them. |
| Q-7 | How do I word G3/G4/K3/K4 so DEV-2's classification comes out with exactly one write-act statement per file? | Keep them anaphoric and actor-free. "The same transcription applies to the same path — its content is replaced, never appended to" satisfies the anaphor test; "the PM overwrites the same path" does not. The existing `PM_LOG.md` sentences at `gate-reviewer.md:30` and `code-reviewer.md:32` stay verbatim — they are outside the rule's scope by construction (different path, different act). |
| Q-8 | What do I do with the stage-table rows at `:30`/`:32`, which contain the path tokens? | Leave the "Output document" attribution as it stands and classify both as constraints, not write-act statements — that is the same standard the design applies to `skills/harness/SKILL.md:36`'s "Output: `03_GATE_REVIEW.md`". P8's marker must therefore be a **pointer** ("see the transcription block below"), never a write verb plus an actor, or you create a second write-act statement in the file and AC-10 stops biting. Publish all three rows in DEV-2 (C-10). |
| Q-9 | Do I add a `CHANGELOG.md` entry? | No. `CHANGELOG.md:43` is inside the cut `[0.46.0]` section, no version claim moves, and T-21/T-22 in this same drain added none. The design's class-level disposition is that no `CHANGELOG.md` line is edited by this task. |

## Verdict

APPROVED WITH CONDITIONS
