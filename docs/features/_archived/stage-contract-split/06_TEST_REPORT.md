# 06 — Test Report · T-18 `stage-contract-split`

> Contract portion. Rationale: 06_RATIONALE.md (absent = none written).

- **Mode**: `full` (`/harness-stream` drain, deferred-human mode)
- **Upstream**: `01` READY (481L) · `02` READY (**498L**) · `03` **APPROVED WITH CONDITIONS** (C-8, C-11, C-14 are mine) · `04` READY FOR REVIEW (156L) · `05` **APPROVED** (0 CRITICAL, 0 MAJOR)
- **Schema gap (boundary-rule row 5, per gate Q-9)**: this front-matter block fits no declared stage-6 shape; "upstream `05` is APPROVED" is a statement stage 7 must verify, so it is contract. Seventh instance of the F-34 class in this folder. Ledger row owed: none (`02` §14).
- **Contamination disclosure (C-8's intent, restated)**: `04_DEVELOPMENT.md` and `05_CODE_REVIEW.md` were mandated inputs, so the developer's classifications and the reviewer's walks were visible before my pass. Mitigation: every walk below is re-derived from the **shipped bytes** of `.harness/rules/70-doc-size.md` (174L) and `02` §3's schemas, and every matching row/step is recorded, not only the first (C-14).
- **Operator ruling in force**: **RES-QA7 / RES-QA8 are accepted published residuals, not defects.** Forced closure was declined and recorded. This report verifies that the accepted grade is stated correctly and that nothing regressed; it does not re-litigate either residual.
- **Verdict**: **APPROVED FOR DELIVERY, with named travelling residuals** (0 BLOCKER, 0 CRITICAL, 0 MAJOR, 2 MINOR) — §`## Verdict`.

## Test plan

| Acceptance criterion | Test case(s) | Where run |
|---|---|---|
| AC-1 total & deterministic rule | ladder/row structure re-counted from the shipped file (14 rows, 10 steps); the nine C-14/C-8 unit verdicts re-confirmed against byte-unchanged inputs; RES-QA7's own walk re-run against the **moved** E22 line | A-1, A-3c |
| AC-2 schema admits no blob | flagship 115-line body re-attacked: artifact bytes hashed, rows 5/6/9/10/14 re-read from the shipped file | A-2 |
| AC-3 no changelog section | `grep -rE '^## +(Round [0-9]\|.*[Cc]hangelog\|Superseded)'` over `agents/`, `.harness/rules/`, this folder | A-5 |
| AC-4 downstream consume contracts | trigger-id sweep over 7 framework agents + **all six** `dev-*.md.tmpl` (the QA-12 re-sweep) | A-4 |
| AC-5 gate keeps full view | `agents/gate-reviewer.md` dimension-row count + `:82-83` both-portions read | A-4 |
| AC-6 no stage removed / no gate relaxed | `bash .harness/scripts/verify_all.sh` ×3, count read from the run | `## verify_all result` |
| AC-7 one filename per stage | all-file-type sweep for `04a_DEVELOPMENT` / `04_IMPLEMENTATION` | A-5 |
| AC-8 provenance enumerated | same sweep; frozen set D1–D10 re-enumerated and checked for touches | A-5, A-14 |
| AC-9 ≥30 % stage-4 reduction | independent recompute of `(1649−1028)/1649` | A-6 |
| AC-10 size ceilings | `wc -l` all agents, all rule fragments, `AI-GUIDE.md`, `docs/tasks.md`, all task docs | `## Boundary tests added` |
| AC-11 single-sourced rule | authoring-site sweep for `## Stage-doc boundary rule` | A-7 |
| AC-12 degradation | FR-9 header + T4.1–T4.4 sweep over all six partition templates (**QA-12 fix verification**) | A-4 |
| AC-13 backwards compatibility | 40 archived folders carry **0** rationale siblings | A-4 |
| AC-14 lockstep & template parity | **true `diff`** of E1/E2 whole-file **and** md5 of the inserted section extracted from both | A-7 |
| Grade correctness (operator ruling) | nine-surface propagation sweep + negative sweep for a surviving unqualified structural claim | A-15 |
| Decline record durability | `.harness/rejected-decisions.md` key + reasoning + E19 ledger agreement | A-16 |
| No regression from the scoped edits | `find -newermt` change-set enumeration since the round-2 report, then per-file diffs | A-13, A-14 |

## Adversarial tests

One independent reproducer per criterion, hypothesis stated before running. Every reproducer was written from the criterion and the **shipped bytes**, not from `04_DEVELOPMENT.md`'s or `02` §13's own claims. Excerpts ≤5 lines (Rule 1).

| # | AC / target | Hypothesis — "I expect failure when…" | Reproducer (mine) | Outcome |
|---|---|---|---|---|
| A-1 | AC-1 | …the scoped `02` edit moved a normative byte and the ladder/table no longer has the shape my round-2 walks assumed | re-count rows and steps from the shipped file; hash the file; enumerate the whole change-set since 15:09 | **Survived.** 14 rows, 10 ladder steps, `70-doc-size.md` md5 `d80ba08b…` at **174L**, twin `35157e64…` at **173L**; neither appears in the change-set (A-13). Normative text is byte-frozen. |
| A-2 | AC-2 | …the flagship 115-line fence acquired a home because a row moved under it | hash `_archived/hook-truth-verify-scope/02:126-160`; re-read rows 9/10/14 and the row-5/6 rejection clauses | **Survived.** Fence bytes `d1a0bb62…`, 35 lines, unchanged; `| 9 | … destined **verbatim** … | **no home**`, row 10 `no home`, row 14 rationale; rows 5 and 6 both still carry "a fenced block or a blockquote is never a statement" (`:82`, `:83`). |
| A-3b | FR-6 (QA-3) | …the round-1 row-6 smuggle (`**OQ-3's answer**:` + a 115-line fence) reopened | re-read `:83` verbatim against the round-1 attack | **Survived — QA-3 stays closed.** Row 6 still inherits row 5's definition *and* the fence/blockquote clause in the same line. |
| A-3c | RES-QA7 | …RES-QA7's published citation is stale, because `02` shrank 499→498 and E22 moved | resolve **E22 by id**, then verify the cell's characters against the live artifact | **Reproduces exactly, at the published line.** E22 is at `02:252` — the cited line. `skills/harness-goal/SKILL.md:68` carries `(current-state contract; iteration history lives in goal_state.json + PM_LOG.md)`; the blockquote counterpart at `_archived/stream-defer-human/02:94` is present and still reaches `no home`. Destination of the E22 row is **determinate** (contract) — see QA-14. |
| A-4 | AC-4, AC-5, AC-12, AC-13 | …the QA-12 fix is cosmetic — a new coinage, or it broke the triggers it sits beside | sweep all six `dev-*.md.tmpl` for T4.1–T4.4, "contract portion", and the FR-9 header literal; then diff the literal across all six | **Survived — QA-12 is genuinely fixed, 6/6.** All six: `triggers=[T4.1T4.2T4.3T4.4] contractword=2 fr9=1`. The literal is byte-identical in all six (3× backticked instruction at `:51`/`:57`/`:65`, 3× inline in the schema block at `:76`) — the wording is `dev-api.md.tmpl:65`'s, not a coinage. Gate still has exactly **8** dimension rows and reads both portions of 1 and 2; 40 archived folders, **0** rationale siblings. |
| A-5 | AC-3, AC-7, AC-8 | …the new `### Residuals travelling` section or `04`'s three corrections introduced a changelog/round-record section | `^## +(Round N\|changelog\|Superseded)` sweep over `agents/`, `.harness/rules/`, this folder; `04a_`/`04_IMPLEMENTATION` sweep | **Survived.** Changelog sweep returns **0** hits. `04a_DEVELOPMENT` ×0 outside `docs/walkthrough.html:595` (frozen D6); `04_IMPLEMENTATION` only at `.harness/insight-index.md:16` (D1) and `baseline.json:25` (D2), both naming it as history. |
| A-6 | AC-9 | …37.66 % does not reconcile | recompute `(1649 − (169+725+134))/1649` | **Survived — 37.66 %**, 621 lines, 7.66 points over the floor. |
| A-7 | AC-11, AC-14 | …the two scoped edits drifted the E1/E2 twins, or a 6th divergence appeared | true `diff` whole-file; then extract `:33-113` / `:32-112` and compare md5 | **Survived.** Exactly **5** divergences, all at lines ≤28 (title, dogfood `I.*` note, WARN group, one "or similar", one "30 evidence lines"). Inserted section md5 `b15bd29d…` on **both sides — byte-identical**. Authoring sites for `## Stage-doc boundary rule`: exactly **2**. |
| A-8 | C-11 / V-12 | …the minimum `07_DELIVERY.md` no longer lands on 15 after the scoped round | rebuild the minimum schema-conforming delivery from `pm-orchestrator.md:199-232` and `wc -l` | **Survived — 15 exactly**, AP-2 stage-7 row still `| 7 | 07_DELIVERY.md | 15 | ## Summary, ## Verdict |`. Margin **0**, unchanged, no threshold touched. |
| A-9 | C-11 (byte-identity) | …the scoped round touched `supervisor.md` or `pm-orchestrator.md` | `git diff --stat HEAD` + change-set enumeration | **Survived.** `supervisor.md` 6 changed lines (4 ins / 2 del) — identical to round 2; `pm-orchestrator.md` 47 — identical to round 2; neither in the change-set. |
| A-10 | C-3 (byte-freeze) | …E29's `## Verdict` breaks `archive-task`'s harvest bound | run `archive-task.sh:51`'s awk verbatim over a constructed `## Summary` + `## Insight` + `## Verdict` delivery | **Survived** — harvest returns exactly **1** bullet; `DELIVERED` and the `Final verify_all result: PASS` line are not swept in. |
| A-13 | regression | …the scoped round touched more than the two files it claimed | `find . -type f -newermt '2026-08-01 15:09:22'` (my round-2 report's mtime) | **Survived — the change-set is exactly 8 files**: `02`, `04`, `PM_LOG.md`, `docs/tasks.md`, `.harness/rejected-decisions.md`, the two `dev-*.md.tmpl`, and `.harness/scripts/verification_history.log` (gitignored, untracked, appended by my own `verify_all` runs). **No script, no `baseline.json`, no `.ps1`, no `agents/*.md`, no `.harness/rules/*.md`, no archived folder.** |
| A-14 | AC-8 / frozen set | …an edit landed on a frozen-set member | check D1–D10 against the change-set; diff `docs/tasks.md` (D5, append-only) | **Survived.** Only D5's file appears, and its diff is **additions only** — one active T-18 row plus five sibling Completed rows; no Completed row mutated. `baseline.json` (D2) mtime 05:44, predates the task. |
| A-15 | grade correctness | …at least one of the nine claimed surfaces still carries an **unqualified** structural claim, which is the cross-surface contradiction this task's gate caught three times | grep every occurrence of `structural`/`structurally` in `02` and read each in context; then check each named surface individually | **Survived — 0 unqualified claims.** All 8 occurrences are qualified or explicitly negative. Exact required wording present verbatim at **§13 `:408`** and **§17 `:493`**; qualified restatements at §1 `:38`, §2 `:109`, §3 `:141-143`, §9-R9 `:334`, §11.1-V-2 `:364`, §12 `:400-401`, §13 row `:420`, §14 `:458`. `:409` states the reader test explicitly. See A-15 note. |
| A-16 | decline durability | …the decline record is a stub that a future reader reopens as an oversight | read the key's body; check the E19 ledger row agrees | **Survived.** `.harness/rejected-decisions.md:201` `## byte-form-subpart-classification` carries the ruling in full: the fix-one-reopens-the-other argument, the structure-cannot-perform-semantic-judgement argument, and the misclassification cost. File **222L**; the cap table declares **no cap** for it, so no ceiling is breached. E19 reads `199 → 222` and names all four keys. |
| A-17 | `04`'s corrections | …the three corrections the developer took from my byte-diffs are wrong in the other direction | resolve each against the shipped file, independently | **Survived, 3/3.** Row-4 bound occupies `70-doc-size.md:93-99` (blank at 92 and 100) — `:93-99` correct, `:94-100` was off by one. `pm-orchestrator.md:188` = "**Before stage 7 (delivery)**: Stages 5 and 6 must both PASS." — correct. Round-record: my wrap-tolerant sweep finds the literal in **15** files, of which 3 are this folder's own stage docs → **12 shipped files** (`70-doc-size.md` + twin, 7 framework agents, 3 inline partition templates) + 2 prose restatements at `docs/workflow.md:21` and its twin = **14 surfaces**. Exactly the corrected figures. My round-2 single-line grep missed 3 wrapped copies; the wrap-tolerant sweep is the correct instrument and the developer's count is the one that survives. |
| A-18 | RES-QA7 sharpness | …the residual was softened into "an edge case" on the way into `02` §13 | compare the published statement against my round-2 QA-7 text, clause by clause | **Survived — not softened.** RES-QA7 leads with the sharpest form, bolded: *"The same bytes with the same purpose reach opposite destinations depending on the markdown container they sit in."* Both artifacts cited (`02:252` E22 → `skills/harness-goal/SKILL.md:68`; blockquote counterpart `_archived/stream-defer-human/02:94`), "nine sibling §6 rows", "the class includes source code", and "no `## Byte-form specification` entry and no row-4 test result" all survive. The trailing "boundary of the mechanism, not an oversight in it" is the operator's reasoning appended, not a retraction of the finding. |
| A-19 | new text, self-application | …the architect's new `### Residuals travelling` table fits no declared shape — the F-34 class, self-inflicted by the fix | check §3's stage-2 schema row for the section and its shape | **Survived.** §3 `:148` declares `## Residuals travelling` `row` — `id \| statement \| must reach <stage/doc>`, and the shipped table's header is exactly `id \| statement \| must reach`. The `###` nesting under §13 rather than a top-level `##` is the folder-wide non-retrofit §14 already excludes, not a new gap. |

**A-15 note — the nine surfaces, individually.** §1 `:38` "Both bodies (b) removes are multi-line: the guarantee is structural for multi-line forms and compliance below that (§13)". §2 `:109` "It does **not** reach a byte-form that fits on one line inside a declared row's cell: §13's published residual **RES-QA7**". §3 `:141-143` states the property nobody had written down — "**Every declared shape below is line-shaped** … so a body needing more than one line has no container to hide in anywhere in the schema … it is also what fixes where §13's structural half stops". §9-R9 `:334` grades both remainders "**compliance below one line**". §11.1-V-2 `:364` publishes both exceptions and forbids tuning the probe around them. §12 `:400-401` "held for multi-line forms by rows 3/4 sitting **above** row 9 (below one line, RES-QA7)". §13 `:408-409` states the grade in full and adds the reader test. §14 `:458` records the declined forced closure. §17 `:493-496` restates the grade verbatim, forbids any other wording "here or in `07_DELIVERY.md`", and says **"AC-1 is met in outcome — 0 observed misroutes — and unmet in the guarantee"**. Applying the operator's own test to the whole document: a reader cannot come away believing the guarantee is total, because `:409` forecloses that reading by name. **The grade is correct, exact, and non-contradictory.**

## Boundary tests added

- **Empty / absent artifact**: `## Insight` absent → `archive-task` harvests **0**, exit **0** — silent false-green, which is why row 1 must freeze it. Re-verified with the shipped awk.
- **Heading-boundary mutation**: `## Verdict` placed after `## Insight` (E29) — harvest bounded to exactly 1 real bullet; `DELIVERED` not swept.
- **Minimum-size contract**: minimum schema-conforming `07_DELIVERY.md` = **15** lines = AP-2's threshold; margin **0**, reproduced, no threshold touched.
- **Maximum-size contract**: `pm-orchestrator` **291/300** (unchanged, tightest in the repo); `75-safety-hook.md` **200/200**; `70-doc-size.md` **174/200**, twin **173**; `AI-GUIDE.md` **113/200**; `docs/tasks.md` **61/300**; `02` **498/500** — one line of slack recovered by the scoped edit.
- **Uncapped-file boundary**: `.harness/rejected-decisions.md` grew 199 → **222** and has **no row in the cap table**, so the growth breaches nothing. Recorded so the absence is a measurement, not an omission.
- **`PM_LOG.md` pressure**: **265**/500 after the round-3 entries — grew 71 lines this round, still 235 of headroom. RES-5's ceiling concern remains dormant.
- **Absent-artifact boundary in this folder**: `01_REQUIREMENT_ANALYSIS.md` carries **no** FR-9 header line (5 of 6 contracts carry it) — excused by `02` §14's no-retrofit clause, recorded so it is not read as a counter-example. See QA-13.
- **Unicode / non-ASCII unit**: `_archived/lang-policy-split/02:67-91`, a fenced CJK block — rows {9, 13, 14} → `no home`, identical to its ASCII twin. The rule is not byte-encoding sensitive.
- **Concurrency**: not applicable — no runtime added; `0N_RATIONALE.md` is single-writer by construction (`02` §4).
- **Repeat runs**: `verify_all` ×3, stdout md5-identical.

## verify_all result

- Command: `bash .harness/scripts/verify_all.sh`, from `/home/alan/Programs/harness-kit`, **3 times**.
- **PASS: 32 · WARN: 0 · FAIL: 0 · exit 0**, identical on all three runs. Discharges **AC-6 / V-1 / C-1**.
- **Check count read from the run, not from a document**: `grep -cE '^\[[A-J]\.[0-9]+b?\]'` = **32** on each run, matching the `PASS: 32` summary. No check added, none removed.
- Total tests before → after: **32 → 32**. A documentation/agent-contract change; `01` §6.3 freezes the count and `03` C-2 forbids touching any script.
- New automated tests added: **0 by design** — R-3 / `01` §6.3 forbid a new `verify_all` check, and `02` §13 grades stage-doc size "Compliance only" precisely because no mechanism may be added here. My probes ran against copies of the shipped matchers in a sandbox outside the repository and are deliberately not persisted.
- **`.harness/scripts/baseline.json`: not updated.** The count did not increase; the file is frozen-set member D2 and C-2 forbids touching it. Baseline preserved, never lowered.
- PowerShell: **not run** — not executable by agents here, and this task created no PowerShell surface (`find -name '*.ps1' -newermt` since task start returns **0** files). PS-side re-run is **operator-pending**; the standing list stays at **sixteen** numbered items (four security), **not** reconciled and **not** renumbered.
- Version stamps unmoved: `plugin.json` / `marketplace.json` both `0.46.0`; `CHANGELOG.md`'s top heading is still the existing `## [0.46.0]` (NFR-4).
- C-4 / V-6 by `wc -l`: `pm-orchestrator 291 · supervisor 287 · solution-architect 169 · code-reviewer 166 · qa-tester 156 · gate-reviewer 113 · requirement-analyst 101 · developer 91` (cap 300). Rules: `75-safety-hook 200 · 70-doc-size 174 · 60-tool-handoff 131` (cap 200). `AI-GUIDE.md 113`. Task docs: `02` 498 · `01` 481 · `PM_LOG` 265 · `AC9_RECONSTRUCTION` 186 · `04` 156 · `06` (this) · `05` 102 · `05_RATIONALE` 98 · `03` 76 · `03_RATIONALE` 26 (cap 500).

## Defects found

Closed and not restated (no-changelog rule): QA-1…QA-6 were fixed in earlier rounds and I re-ran the attacks that produced them (A-2, A-3b, A-8, A-10) — all stay closed. **QA-7 and QA-8 are not defects**: they are accepted published residuals under the operator's ruling, verified present and correctly graded (A-15, A-18) and carried below as RES-QA7 / RES-QA8. **QA-12 is fixed and verified 6/6** (A-4). QA-9, QA-10 and QA-11 remain dispositioned "left with reason" at `04:126-128` and travel unchanged. Two new MINOR findings, both documentation-accuracy, neither blocking.

| id | Severity | Reproducer | file:line |
|---|---|---|---|
| **QA-13** | **MINOR** | **The scoped `02` 499→498 edit orphaned four upstream line-count citations, and one published count is off by one.** (a) `grep -rn '499L' docs/features/stage-contract-split/` returns **four** live stage docs citing `02` as 499L — `03_GATE_REVIEW.md:7`, `03_RATIONALE.md:6`, `04_DEVELOPMENT.md:6`, `05_CODE_REVIEW.md:8` — while `wc -l` on the shipped file is **498**. Each was accurate when written (all four mtimes precede the architect's 15:26 edit), so no agent introduced an error; the staleness is inherent to citing a live document by line count, which is exactly why RES-QA7 declares the ledger id and not the line number to be its stable key. My own front-matter is corrected to 498 above. (b) RES-QA8 (`02:446`) states "all six contracts in this folder carry the header correctly"; **five** do — `01_REQUIREMENT_ANALYSIS.md` carries no FR-9 header, excused by §14's no-retrofit clause. The substantive claim it supports ("observed misroutes: 0") is unaffected, since an absent header is not a misroute. Neither part touches normative text, a threshold, a check or a cap; both are one-token corrections available to stage 7 at zero risk. | `03_GATE_REVIEW.md:7`; `03_RATIONALE.md:6`; `04_DEVELOPMENT.md:6`; `05_CODE_REVIEW.md:8`; `02_SOLUTION_DESIGN.md:446` |
| **QA-14** | **MINOR** | **V-2 mis-grades RES-QA7 as a two-destination exception, which its own §13 row contradicts — and the error would mislead the next QA into looking for the wrong symptom.** `02:364`'s expected observable is "exactly one destination per unit and no unit with two", followed by "**Two published exceptions to 'exactly one': the FR-9 header line (RES-QA8) and a one-line byte-form inside a declared row's cell (RES-QA7)**". RES-QA8 genuinely is a two-destination case (step 1 → contract, step 3 → rationale). RES-QA7 is **not**: I re-walked E22 independently (A-3c) and it reaches exactly one destination — step 1 makes the row the unit, row 2 finds it is not offered as characters, matching rows {2, 14} → **contract**. The defect is one of *kind*, not of determinism, which is what §13's row `:420` grades ("Compliance only") and what §17 `:496` says ("AC-1 is met in outcome … and unmet in the guarantee"). A future QA executing V-2 literally would hunt a second destination for RES-QA7's class, fail to find one, and could wrongly report the residual as non-reproducible. The error is in the **conservative** direction — it overstates the AC-1 damage — so it does not soften the grade and does not fail the operator's reader test; it is a precision defect on the verification-plan surface. Corrective is one clause: name RES-QA7 as an exception to the destination's *correctness*, not to its uniqueness. | `02_SOLUTION_DESIGN.md:364` (against `:420`, `:445`, `:496`) |

**Verified clean (attacked and held) — stated positively, do not re-litigate.** AC-2 (A-2), AC-3/AC-7/AC-8 (A-5, A-14), AC-4/AC-5/AC-12/AC-13 (A-4 — **QA-12 confirmed fixed 6/6, wording inherited not coined**), AC-6 (run and counted, 3×), AC-9 (A-6, 37.66 %), AC-10 (`## Boundary tests added`), AC-11/AC-14 (A-7, **true byte-diff plus md5 of the extracted section**), C-3's byte-freeze (A-10), C-11's byte-identity and its measurement (A-9, A-8 at 15/margin 0). **The corrected grade is present, exact and non-contradictory on all nine surfaces (A-15); RES-QA7 is published unsoftened (A-18); the decline record is durable (A-16); all three of my round-2 byte-diff corrections landed correctly in `04` (A-17), and my own round-2 round-record count was the one that was wrong.** No normative byte moved: `70-doc-size.md` and its twin are absent from the change-set and md5-stable at 174/173 (A-1, A-13).

## Stability

- `verify_all` ×3: `PASS 32 / WARN 0 / FAIL 0`, exit 0, 32 step lines, **stdout md5-identical (`1fc35e9f…`) across all three runs**. No flakes.
- Every walk was re-derived a second time from the shipped bytes. All verdicts reproduced, including RES-QA7's at the moved E22 line and the 6/6 template sweep.
- The `archive-task` harvest test and the minimum-delivery construction reproduced on re-run.
- Environment note: the tree carries **89** uncommitted files from sibling tasks. Per insight-index L31 a claim about a dirty tree is not a proof, so every freeze and identity claim above rests on a real `diff`, `md5sum`, `find -newermt` or `git diff` against file **content** — never on `git status` alone. I did not commit, revert, stash or clean anything.
- **Footprint: zero.** Eight fixtures (three `verify_all` logs, two extracted rule sections, a constructed minimum `07_DELIVERY.md`, a constructed harvest-boundary delivery, a harvest output) were created under the session scratchpad **outside the repository** and have been deleted. The one repo-side artifact is `.harness/scripts/verification_history.log`, which `verify_all` appends to on every run; it is **gitignored and untracked** (`.gitignore:34`), and running the mandated tool is the whole of my mandate — disclosed rather than hidden. No other file under `/home/alan/Programs/harness-kit/` was created or modified by stage 6 except this report. No archived folder, frozen-set member, script, `baseline.json`, threshold, version stamp or shipped surface was touched to make any test pass.

## Verdict

**APPROVED FOR DELIVERY — with named travelling residuals** (0 BLOCKER, 0 CRITICAL, 0 MAJOR, 2 MINOR).

Both scoped changes are correct and nothing regressed. The developer's QA-12 fix is real: **6 of 6** partition templates now instruct the FR-9 header, in `dev-api.md.tmpl:65`'s own wording byte-for-byte, with T4.1–T4.4 intact beside it. The architect's re-grade is correct, exact, and carries the required wording verbatim on the two surfaces that own it and a faithful qualification on the other seven; §13 `:409` forecloses the total reading by name, so the operator's reader test is met. RES-QA7 is published in its sharpest form with both artifacts cited and nothing softened, and the decline is recorded under a stable key with the reasoning attached, so container-dependence cannot be reopened as an oversight. The normative surface is byte-frozen: `70-doc-size.md` and its twin never entered the change-set, the inserted section is md5-identical across the pair, `pm-orchestrator.md` is unchanged at 291/300, and no script, `baseline.json`, `.ps1` or frozen-set member was touched.

The two new findings are documentation-accuracy only, both one-token corrections, both available to stage 7 without reopening a stage. Neither blocks.

**Travelling to `07_DELIVERY.md`:**

- **RES-QA7** — the same bytes with the same purpose reach opposite destinations depending on the markdown container they sit in. Accepted, published, declined for closure by operator ruling. The delivery narrative must carry the grade **verbatim**: *"structural for multi-line forms, compliance for anything that fits on one line."* No other wording is authorised (`02` §17).
- **RES-QA8** — two ladder steps name the FR-9 header line with different destinations; **0 observed misroutes**. AC-1 is **met in outcome and unmet in the guarantee**, and the delivery must say both halves.
- **QA-13, QA-14** (MINOR, this round) — four orphaned `499L` citations plus RES-QA8's six-vs-five header count; and V-2's mis-grading of RES-QA7 as a uniqueness exception rather than a correctness one.
- **QA-9, QA-10, QA-11** (MINOR, dispositioned "left with reason" at `04:126-128`) — row 2's parenthetical is wrong about its own table (merges with RES-7); starvation demonstrated on live fail-closed guard bytes (merges with F-28/F-32); `harness-batch`'s `OR` fallback population is larger than the blocked-at-7 case, and E29 makes the matching literal the authored default (merges with RES-10).
- **Unchanged from stage 5**: RES-1 (margin 0, reproduced at 15), RES-3, RES-5, RES-6, RES-8, RES-9.

**My independent answer to the dominating question, restated against the accepted grade.** The unwanted output has nowhere to go **if and only if it needs more than one line** — and after three rounds of attacks I could not move that boundary in either direction. The structural half held every attack: the flagship 115-line body walks all fourteen rows to `no home` with row 2's guard present and removed; ladder steps 2 and 3 make a fence and a `>` run one unit each however long; rows 5 and 6 reject both syntactically, independent of what the author declares; and **every declared shape in `02` §3 is line-shaped**, so a multi-line body has no container to hide in anywhere in the schema. That last property is what actually carries AC-2, it was the thing nobody had written down, and §3 now states it once. The class the task was launched against — design documents embedding large verbatim blocks of implementation text — is that multi-line class, and it is genuinely homeless now, structurally rather than by compliance. What survives is a different and smaller class: single-line verbatim content inside a declared row's cell. The rule is a size filter on verbatim content below one line, not a kind filter, and the shipped documents now say exactly that in the same words on every surface that mentions it. That is a narrower guarantee than the pipeline first claimed and a true one, which is the trade the operator took.
