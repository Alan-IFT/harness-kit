# 05 — Code Review · T-18 `stage-contract-split`

> Contract portion. Rationale: 05_RATIONALE.md (absent = none written).
> Persisted verbatim by PM: the code-reviewer is read-only (`Read`/`Glob`/`Grep` only, no write tool
> by contract) and returned this review in its final message.

- **Mode**: `full` (`/harness-stream` drain, deferred-human mode) · **round 3**, current state only
- **Upstream**: `01` READY (481L) · `02` READY (499L, rewritten normative rows) · `03` **APPROVED WITH CONDITIONS** (round 3: C-1…C-4, C-7, C-8, C-11…C-15 bind) · `04` READY FOR REVIEW (143L) · `06` CHANGES REQUIRED (round 2), re-verification QA-owned
- **Schema gap (boundary-rule row 5, per gate Q-9)**: this front-matter block fits no declared stage-5 shape; "upstream `03` is APPROVED WITH CONDITIONS" is a statement stage 6 must verify, so it is contract. Ledger row owed: none — T-18's own folder is not retrofitted (`02` §14). Sixth instance of the F-34 class in this folder.
- **Tooling limitation, stated up front**: `Read`/`Glob`/`Grep` only. **No `Bash`, therefore no `git diff`, no `wc -l`, no `verify_all` run.** Line counts are `Read`-based (≥ `wc -l` by ≤1, always conservative — gate Q-3/G-12). Every claim of the form "byte-unchanged since HEAD" below is verified by **reading the current content against its recorded specification**, never by diff; a true byte-diff is QA-owned. AC-6's `verify_all` run and AC-1's unit probe are QA-owned (RES-4).
- **Finding ids are stable across rounds.** CR-1…CR-4 and CR-6 were round-2 findings and are **fixed** (evidence re-read: `pm-orchestrator.md:74-82`, `CHANGELOG.md:15`, `AC9_RECONSTRUCTION.md`, `qa-tester.md:108`, `CHANGELOG.md:14`); they are not restated, per the no-changelog rule. CR-5/CR-7/CR-8 persist; CR-9…CR-14 are new this round.

## Files reviewed

`.harness/rules/70-doc-size.md` (174L, full)
`skills/harness-init/templates/common/.harness/rules/70-doc-size.md.tmpl` (173L, full)
`agents/pm-orchestrator.md` (291L; `:60-109`, `:178-257`, `:280-291` in full)
`agents/code-reviewer.md` (166L)
`CONTEXT.md` (186L; `:50-79`, `:176-186`)
`CHANGELOG.md` (`:1-25`)
`agents/solution-architect.md:16-57` (C-7 guards, re-read not taken on report)
`agents/supervisor.md:88-115` (AP-2 table)
`.harness/scripts/archive-task.sh:38-72` · `.harness/scripts/archive-task.ps1:49` · both `templates/common/` twins (read-only, C-3 boundary check)
`skills/harness-batch/SKILL.md:45-86` · `skills/harness-stream/SKILL.md` `DELIVERED` sites (read-only, C-15 desk-check)
`docs/features/stage-contract-split/02_SOLUTION_DESIGN.md:1-248` · `04_DEVELOPMENT.md` · `03_GATE_REVIEW.md` · `03_RATIONALE.md` · `06_TEST_REPORT.md` · `PM_LOG.md:400-443`
`docs/tasks.md:1-14`
Repo-wide sweeps (`Grep`, all file types unless noted): retired stage-4 tokens; `three declared shapes`; `contract by construction` / `every declared shape contract`; `cannot disagree`; trigger enumerations `T2.1|T4.1|T5.1|T6.1|T7.1`; the round-record clause; `BF-1|FR-\d|BC-\d|§` inside both rule twins.

## Findings

| id | Severity | Axis | file:line | Finding |
|---|---|---|---|---|
| CR-9 | MINOR | Standards-conformance | `.harness/rules/70-doc-size.md:102-103`, `.tmpl:101-102`, `CONTEXT.md:63` | **The corrected row-2 restatement ships a round-relative comparative into three long-lived surfaces**: "…except for a byte-form, which row 2 **now** hands to rows 3/4/9 **rather than blessing**." A reader loading the shipped fragment fresh — the `/harness-init` twin's reader has no access to any earlier draft — cannot resolve "now" or "rather than blessing"; both are claims about a superseded version of this rule, which is the class the rule's own **row 8** routes to `PM_LOG.md` and which its own no-changelog paragraph (`:109-112`) forbids a stage doc to carry. The rule fails this test on itself. **Not the developer's to fix**: C-13 mandated transcription from `02` §2:131-133 and forbade authoring, and the transcription is byte-faithful (verified word for word). The defect is in the source wording; the corrective — delete "now" and "rather than blessing", leaving "…except for a byte-form, which row 2 hands to rows 3/4/9" — is a design edit. Travels as **RES-8**. |
| CR-5 | MINOR | Standards-conformance | `.harness/rules/70-doc-size.md:109-112` (+ `.tmpl:108-111`) and 10 further shipped files | **The round-record clause is a 12-copy hand-maintained invariant, so AC-11's sweep does not return "one authoring site + N pointer sites".** Re-measured this round: the literal clause `round N · what changed · why · which finding id` lives in both rule twins, all 7 framework agents and 3 inline partition templates = **12** shipped copies (the exact-phrase `Grep` returns 9 files; `70-doc-size.md`, its twin and `pm-orchestrator.md:85-86` wrap the clause across a line break and are the other 3), plus prose restatements at `docs/workflow.md` and its twin. Every by-name pointer AC-11 asks for is present and correct. **Adjudicated unchanged from round 2: the disposition "left, as a design call" is correct and does not block** — collapsing the copies trades AC-11 against AC-12/R8 (an agent must carry the duty for a project whose rule fragment predates T-18), which stage 4 may not decide unilaterally. Travels as **RES-6**. |
| CR-10 | MINOR | Spec/design-fidelity | `agents/pm-orchestrator.md:199-232` vs `02_SOLUTION_DESIGN.md:147-148` | **The stage-7 authoring template is the one contract template that emits no FR-9 header line**, while `02` §3 states without exception "Every contract opens with the FR-9 header line, which is itself a declared shape (unit-ladder step 1)". E29's ledger row does not add it, and the developer correctly declined to ship an unledgered line (`04:123`). Two consequences: `07_DELIVERY.md` is a declared contract that cannot conform to its own design's universal clause, and the two lines the header costs are exactly what would move AP-2's stage-7 minimum from **15 (= threshold, margin 0)** to 17 — the margin the round-2 desk-check wrongly assumed. This also falsifies the gate's dimension-5 basis (`03:18`, "the edit raises the minimum conforming stage-7 document above the 15-line threshold"): I rebuilt the minimum document from the **edited** template and reproduce the developer's **15** exactly (`# Delivery Summary`, blank, `## Summary`, blank, 7 non-optional `kv` fields, blank, `## Verdict`, blank, `DELIVERED`). E29 is still the right fix and the only one C-11 permits; its incompleteness is honestly published. Travels as **RES-1**. |
| CR-11 | MINOR | Spec/design-fidelity | `agents/requirement-analyst.md`, `gate-reviewer.md`, `developer.md`, `code-reviewer.md`, `qa-tester.md`, `pm-orchestrator.md` (each schema's closing sentence) | **After the row-2 amendment the agents' schema-fallback sentence is true but one-directional, and only the architect ships the other direction.** Each schema closes with "a unit that fits no declared shape here is classified by the boundary rule"; the amendment adds the converse path — a unit that **does** fit a declared shape leaves the contract when it is a byte-form (rows 3/4/9) — and only `agents/solution-architect.md:33-37` states it. A QA author whose `## Defects found` row quotes the exact bytes to insert will read its own contract, see a declared shape, and ship it; the rule ejects it at row 9. Exposure is bounded because each agent's degradation clause allows `70-doc-size.md` to be present and read. **Design-owned, not drift**: the fallback wording is mandated verbatim by `02` §3:144-146 and no ledger row covers the eight schemas, which is why the developer reported it (`04:125`) instead of editing them. Travels as **RES-9**. |
| CR-12 | MINOR | Spec/design-fidelity | `.harness/rules/70-doc-size.md:79,81,82,86` (+ twin) | **A marked one-sentence plain-prose byte-form that fits no declared shape reaches the contract through row 5, so `03_RATIONALE.md:14`'s "either way" is a two-outcome claim on a three-outcome walk.** Walk (full derivation in `05_RATIONALE.md`): unit = one sentence introduced by "replace `X` with:" but neither fenced nor blockquoted, fitting no declared shape → row 2 is inapplicable (it requires *fitting*), row 3 no, row 4 **rejected** when a shorter multi-satisfiable statement exists, row 5 **matches** (one sentence, not a fence, not a blockquote, binding) → **contract**, unlabelled, before row 9 is evaluated. The design predicts only "row 4 under `## Byte-form specification`, or row 9". **AC-1 is not violated** — the unit still reaches exactly one destination deterministically — and the harm is small because a one-sentence byte-form is materially indistinguishable from its own constraint, which is what row 9 tells the author to write anyway. It bounds the strength claim, not the mechanism: this is the same graded concession as `02` §13 row 4 / R9 ("Medium" for prose), and it should be graded for **marked** one-sentence prose too, not only unmarked. Travels as **RES-7**. |
| CR-14 | MINOR | Spec/design-fidelity | `skills/harness-batch/SKILL.md:81` (untouched, correctly) vs `agents/pm-orchestrator.md:208,229-231` | **F-30's residual is correctly left, but the developer's exposure bound understates it.** Verified directly: `:81` is `parses as DELIVERED (primary) **OR** contains 'Final verify_all result: PASS' (secondary fallback)`, and `:84` declares the tolerance deliberate ("keep the dual check if the delivery doc format evolves") — so touching it is genuinely a driver-semantics decision, and C-15's "change no skill file" is right. However `04:124` bounds the reachable population to "the blocked-at-7 case" via the stage gate at `pm-orchestrator.md:188`; in **deferred-human mode a stage-7 `BLOCKED: NEEDS-HUMAN` delivery is a designed, routine outcome** — T7.1 exists to compose exactly that — and such a delivery still carries `- Final verify_all result: PASS` from the template's own `## Summary` field list. E29 makes that document an authored form for the first time, so the batch driver will mark a needs-human task `done` and skip it on resume. Bounded, not exotic. Travels as **RES-10**. |
| CR-13 | NIT | Standards-conformance | `04_DEVELOPMENT.md:75`, `:124` | Two citation slips in otherwise sound rows. (a) Drift **D-2** states the row-4 bound moved "`:76-82` → `:94-100`"; the shipped bound is at **`:93-99`**, which is also what D-2's own "17 lines were inserted above it" arithmetic gives (76+17=93). The claim the row makes — byte-identical text, moved lines — is correct; only the destination range is off by one. (b) Item 4 cites `agents/pm-orchestrator.md:184-186` for the stage gate; `## Stage gates` opens at `:184` and the stage-7 clause is at **`:188`**. |
| CR-8 | NIT | Standards-conformance | `04_DEVELOPMENT.md:127` | The residual warning the next editor about the hand-maintained invariant still states the wrong count: "a **13**-copy hand-maintained invariant", then enumerates 2 rule twins + 7 framework agents + 3 partition templates + `docs/workflow.md` + twin = **14 surfaces**, of which **12** carry the literal clause. Neither is thirteen. Unchanged from round 2 (CR-8); a warning about an invariant that decays is itself the thing that decays. |
| CR-7 | NIT | Standards-conformance | `docs/tasks.md:9` | The active T-18 row still reads `stage: 1 — requirement analysis` at round 3. **PM-owned**, not the developer's (D5 freezes Completed rows only, and `agents/pm-orchestrator.md:181` assigns the update to stage 7 step 9). Noted so delivery updates it. |

**Standards axis, stated positively (not findings).** **C-4 holds by measurement** on the changed set: `70-doc-size.md` **174/200**, its twin **173/200**, `pm-orchestrator.md` **291/300**, `code-reviewer.md` **166/300**, `CONTEXT.md` **186** (uncapped). Untouched and re-confirmed as unchanged in content: `supervisor` 287–288/300, `solution-architect` 169, `qa-tester` 156, `gate-reviewer` 113, `developer` 91, `requirement-analyst` 101, `AI-GUIDE.md` 113, `75-safety-hook.md` 200/200. Worst agent headroom is now **9 lines** (`pm-orchestrator`), the tightest in the repo; D-8 publishes the 291-vs-287–289 miss rather than tuning, and 291 sits below E29's own `>295` condensation trigger, so no `I.*` WARN is reachable from this change and `verify_all`'s exit-1-on-WARN is not in play (execution QA-owned). **C-2/C-3's freeze holds**: the round-3 changed set is six `.md` files; a repo-wide sweep for this round's vocabulary over `*.sh`/`*.ps1`/`*.json`/`*.html` returns **zero**; all four `archive-task` matchers (repo + `templates/common/` twins, `.sh:51` / `.ps1:49`) are byte-identical in form and I read them rather than the report's claim about them; no `_archived/**` path and no frozen-set member (D1–D10) carries a token from this round — the retired-token sweep still returns only `.harness/insight-index.md:16` (D1), `baseline.json:25` (D2) and `docs/walkthrough.html:595` (D6). **Q-8's ship/don't-ship line is exact**: a `Grep` for `BF-1|FR-\d|BC-\d|§` across both rule twins returns **no matches**, so the row-4 note, the worked-walk table, BF-1 and every `01`/BC/FR pointer stayed out, and nothing normative was dropped — rows 1, 3, 4, 7–14, the destination paragraph, "Headings" and "Precedence" are present and match `02` §2:88-103 word for word.

## Requirement coverage check

| Criterion | Implementation | Status |
|---|---|---|
| AC-1 total & deterministic rule | `.harness/rules/70-doc-size.md:45-57` (six-step ordered ladder, first match names the unit, "**A paragraph is never a unit**"), `:59-60` (`**Not units.**`), `:62-66` (self-contained `**Verbatim byte-form.**`), `:73-74` (precedence + terminal reachable default), `:76-91` (14 rows) | ✅ **shipped and re-derived row by row against `02` §2:57-103 — byte-identical, including the ladder, "Not units", the byte-form definition and rows 2/5/6.** Two named precision residuals persist by gate disposition (F-29's colon-terminated fragment, which step 6's "one sentence" does not name; CR-12's row-5 path) — neither produced two destinations for one unit in any construction I built. The 25-unit probe and C-14's five added classes are **QA-owned** (RES-4) |
| AC-2 no blob in the schema | `agents/solution-architect.md:24` + `:33-37` (both guards, uncompressed, re-read not taken on report) **and now** `70-doc-size.md:79`'s own exclusion | ✅ **desk-verified independently, twice over.** The flagship 115-line fence walks to **row 9 → `no home`** with the row-2 guard *and* without it: ladder step 2 makes it one unit; row 2 excludes it as a byte-form; row 4 is disqualified because a shorter multi-satisfiable statement exists; rows 5/6 reject it as a fence. QA-3's smuggle (`**OQ-3's answer**:` + fence) is closed at row 6 by the inherited clause. Walks in `05_RATIONALE.md` |
| AC-3 no changelog section | No schema in any of the 7 declares one; `70-doc-size.md:109-112`; `pm-orchestrator.md:84-88` names `PM_LOG.md` and requires route-back on a `## Round N` heading; `04_DEVELOPMENT.md` was corrected **in place** (143L, no round section) and its round record is at `PM_LOG.md:404-420` | ✅ |
| AC-4 downstream consume contracts | 2 `solution-architect.md:66` (T2.1–T2.4) · 3 `gate-reviewer.md:82-83` · 4 `developer.md:38` (T4.1–T4.4) · 5 `code-reviewer.md:95` (**T5.1–T5.4**) · 6 `qa-tester.md:74,82` (T6.1–T6.3) · 6 partition templates · 7 `pm-orchestrator.md:74-82` (**T7.1–T7.3**) | ✅ **all six consumer stages; trigger counts now 4 at stage 5 and 3 at stage 7**, matching `02` §11.1. BC-5 and BC-6 wording present in every consumer, in the PM's own route-back form at `:82` |
| AC-5 gate keeps full view, 8 dimensions frozen | `gate-reviewer.md:82-83`, `:61-70`, `:20` | ✅ untouched this round; verified round 2 and not in the changed set |
| AC-6 no stage removed, no gate relaxed | 7 stages intact (`pm-orchestrator.md:26-34`); no `.sh`/`.ps1`/`.json` in the changed set; every AP-2 threshold in `agents/supervisor.md:93-101` reads exactly as `02` §3/§6 record it | ✅ structurally; the `PASS 32 / WARN 0 / FAIL 0` run and the byte-diff of `supervisor.md` are **QA-owned** (RES-4) |
| AC-7 one filename per stage | `60-tool-handoff.md:31-35` + twin; `solution-architect.md:61`; `pm-orchestrator.md:36-42`; `CHANGELOG.md:15` names the retirement **by class** | ✅ **sweep re-run this round and clean** — `04a_` ×0 and `04_IMPLEMENTATION` ×1 outside `docs/features/`, both frozen-set members; exactly one distinct live stage-4 filename |
| AC-8 provenance enumerated | Round-3 vocabulary sweep over all file types returns only `.md` surfaces, all ledgered (E1/E2/E7/E9/E29/E17/E20) | ✅ |
| AC-9 ≥30% stage-4 reduction | `AC9_RECONSTRUCTION.md:168,172` = **37.7 %** on `03` contract 134 | ✅ re-measured span-by-span in round 2 against the live archive; QA's A-6 recomputed 37.68 % independently; the artifact is untouched this round |
| AC-10 size ceilings | See the Standards-positive paragraph. Task docs: `01` 481 · `02` 499 · `03` 77 + `03_RATIONALE` 27 · `04` 143 · `06` 172 · `AC9_RECONSTRUCTION` 186 · `PM_LOG` **443** — all ≤500 | ✅ **with one live pressure**: `PM_LOG.md` is at 443/500 with stages 5, 6 and 7 still to log. Compaction is PM-owned (`70-doc-size.md` Rule 2, at a stage boundary, never mid-stage). Travels as **RES-5** |
| AC-11 single-sourced rule | One authoring site per channel (`70-doc-size.md` + `.tmpl`, byte-identical inside the section), by-name pointers in 6 agents, 4 templates, `CONTEXT.md:55`, `AI-GUIDE.md:32` + twin | ⚠️ **MINOR (CR-5)** — one paragraph restated in 12 shipped files; adjudicated non-blocking, travels as RES-6 |
| AC-12 degradation | Clauses present in all consumers incl. `solution-architect.md:51-52`; BC-5/BC-6 in all five consumers, the PM's own form at `pm-orchestrator.md:80-82`, all six partition templates | ⚠️ see **CR-11** — the clause is intact; the schema-fallback sentence beside it is now one-directional |
| AC-13 backwards compatibility | Contract keeps the existing filename; a pre-change folder is a valid contract with no sibling | ✅ |
| AC-14 lockstep parity | E1/E2 | ✅ **verified line by line, not on report.** The inserted section is byte-identical at a constant offset of 1 (`70-doc-size.md:33-112` ≡ `.tmpl:32-111`), **zero internal divergence**, and the offset originates in pre-existing divergence #2. Exactly the five standing divergences remain, all read directly: title (`:1`), the `I.*`-vs-`F.*` group sentence (`:5-8` / `:5-7`), the `F.*` WARN trigger (`:18` / `:17`), `concepts.md … or similar` (`:24` / `:23`), `30 evidence lines` vs `30 lines` (`:28` / `:27`). `agents/*.md` have no template twin, so E7/E9/E29 create no parity debt; no PowerShell surface created |

## Design fidelity check

| Design item | Implementation | Status |
|---|---|---|
| `02` §2's three constructs shipped into E1/E2 (D-1) | `70-doc-size.md:45-91` / `.tmpl:44-90` | ✅ **D-1 legitimate.** Compared token by token against `02` §2:57-103: the six-step ladder, `**Not units.**`, `**Verbatim byte-form.**` and all 14 rows are byte-identical; the only adaptations are the ones Q-8 authorises (cross-references dropped, `§3` → "the contract schema for the stage that authors it"). Nothing on Q-8's don't-ship list leaked (`BF-1|FR-\d|BC-\d|§` sweep: 0 hits) |
| C-7: row 5 amended, row 6 uncompressed, E4's guards verbatim | `:82`, `:83`; `agents/solution-architect.md:24`, `:33-37` | ✅ **verified in the shipped bytes, not on report.** Row 5 ships "(a *statement* is one sentence; a fenced block **or a blockquote** is never a statement)"; row 6 ships "— row 5's definition applies: one sentence, and a fenced block or a blockquote is never a statement", **plus** its own destination ("in the stage's resolved-question section") and the gap instruction. E4 needed no round-3 edit and I confirmed why: `:24` still carries "**Present only when boundary-rule row 3 or row 4 matched**" with both columns, and `:33-37` still repeats both guards naming the failure they prevent ("row 2 becomes an unconditional bypass of rows 3, 4 and 9"). All four clauses identical in the twin |
| Row-4 bound unchanged (D-2) | `70-doc-size.md:93-99` + twin `:92-98` | ✅ **D-2 legitimate; content re-derived, line range mis-cited (CR-13).** The paragraph reads exactly as round 2 recorded it — both conjuncts quantify over *statements of the constraint*, source code excluded because such a statement exists — and I re-ran each of the design's row-4 verdicts under it; none moved. A true byte-diff needs `git` |
| C-13 reconciliation, transcribed not authored (D-6, D-9) | `70-doc-size.md:101-103`, `CONTEXT.md:62-64`, `CHANGELOG.md:14` | ✅ **all three verified against their source, and the sweep re-run independently.** The rule's opening sentence is `02` §2:131-133 **verbatim**; `CONTEXT.md:63-64` carries the same clause verbatim; `CHANGELOG.md:14` now reads `T2.1–T2.4, T4.1–T4.4, **T5.1–T5.4**, T6.1–T6.3, **T7.1–T7.3**`, which matches the shipped agents exactly. My own sweeps for the falsified class return **zero** survivors: `cannot disagree` ×0 in shipped surfaces, `three declared shapes` ×0 outside task docs, and `contract by construction` / `every declared shape contract` returns only the three corrected sites. Finding the `CHANGELOG.md` instance the gate did not name is the right instinct and D-9 records it honestly (see CR-9 for the one defect in the transcribed wording) |
| E29 (QA-4/QA-6) | `pm-orchestrator.md:192-197`, `:202`, `:229-231` | ✅ **and the `archive-task` boundary checked directly, which is the part a report cannot substitute for.** Template order is `## Summary` (`:202`) → `kv` fields → `## Insight` (`:214`) → `## Verdict` (`:229`) → `DELIVERED` (`:231`). Against `archive-task.sh:51`'s `awk '/^##[[:space:]]+Insights?[[:space:]]*$/{flag=1;next} /^##[[:space:]]/{flag=0} flag && /^[[:space:]]*-[[:space:]]/'`: `## Verdict` does **not** match the insight pattern, and it **does** match the terminator, so the harvest is bounded to the one real bullet and cannot sweep in `DELIVERED`; the PS form `(?ms)^##\s+Insights?\s*$(.*?)(?=^##\s|\z)` is bounded by the same heading. The `## Summary` `kv` bullets sit **above** `## Insight`, so they are never harvested. All four matchers (repo + `templates/common/` twins) are unchanged. `:192` now says "**four** declared shapes" and names `## Verdict`'s token set and its written-last, never-omitted status |
| C-11: AP-2 published, never tuned | `agents/supervisor.md:93-101`; `04:123` | ✅ **and the unfavourable measurement is the right call.** Every threshold reads exactly as `02` §3/§6 record it (stage 7: 15 lines, `## Summary`, `## Verdict`); the round-2 E10 edits at `:96`/`:111`/`:236` are the only content differences from the pre-T-18 file and none touches a minimum-line column. I reproduced the developer's minimum-document construction and get **15** — margin **0** — from the *edited* template, so E29 closes the heading clause only. Publishing that instead of moving the threshold is exactly what C-11 and Q-13 required; the residue is CR-10 / RES-1. A byte-diff against HEAD is QA-owned |
| `02` §5 T5.4 / T7.3 (D-7) | `code-reviewer.md:95`, `pm-orchestrator.md:78-80` | ✅ **D-7 legitimate.** Both ship "a contract row **you** must act on cites an identifier (`R-n`, `OQ-n`, a finding id) that no contract portion defines (the rationale of the stage owning the identifier)" — **identical to each other**, which is the property §5 binds; the third-to-second person shift matches every sibling trigger and the agents' own voice. T5.4 says what §5 says, and E7 absorbed it into the existing single-line trigger block, so `code-reviewer.md` stayed at 166 |
| `02` §6 line projections (D-5, D-8) | Actuals | ✅ **D-5/D-8 legitimate and published, not tuned.** `70-doc-size.md` **174** vs Q-11's ~173 and E1's ~170; twin **173**; `pm-orchestrator` **291** vs the gate rationale's 287–289 (E9's hard-wrapped trigger block cost +2, `:192`'s shape sentence +1). No contingency fired; no cap breached; the 9-line headroom on `pm-orchestrator` is now the repo's tightest |
| C-15 disposition (F-30) | `04:124`; `harness-batch/SKILL.md:51,81`; `harness-stream/SKILL.md:67,126` | ⚠️ **Left correctly, bound understated — CR-14.** All four driver lines re-read: `:51` and `harness-stream:67` genuinely improve (before E29 the template emitted no token for the resume check to land on); `harness-stream:126` reads the returned verdict, not the file, and E29 coins no vocabulary. No skill file is in the changed set, which is right |
| Frozen set D1–D10 | No round-3 token in `insight-index.md`, `baseline.json`, `_archived/**`, the HTML snapshots, the supervise fixtures or `STREAM_LOG.md` | ✅ untouched, sweep re-run |

## Residuals travelling

| id | Statement | Must reach |
|---|---|---|
| RES-1 | **AP-2's stage-7 minimum is reachable at margin 0.** The minimum schema-conforming `07_DELIVERY.md` built from the **edited** template is exactly **15** lines = the threshold (I reproduce the developer's construction). E29 fixes the heading clause — the WARN that fired on 40 of 40 archived deliveries — not the line clause, so one further omission WARNs. C-11 was followed exactly and no threshold was touched. The cheapest true fix is already mandated elsewhere: `02` §3:147-148 requires an FR-9 header line on every contract and the stage-7 template omits it (**CR-10**); adding it costs one unledgered line and yields margin 2. **Not blocking; must not be silently dropped at delivery.** | `06_TEST_REPORT.md`, `07_DELIVERY.md`, follow-up pool |
| RES-3 | F-22 stands: `skills/harness-goal/SKILL.md:66-72` and `skills/harness-plan/SKILL.md:53-57` still close-enumerate the per-task document set. Non-blocking — both are output manifests, so neither misroutes a read — but they are the last two members of the family E25/E26/E27 de-enumerated. | follow-up pool |
| RES-4 | Three items are **unverified by this review** for want of `Bash`: AC-6's `verify_all` run (reported `PASS 32 · WARN 0 · FAIL 0`, exit 0, count 32); AC-1's probe, including C-14's five added unit classes and the C-8 disputed set, whose developer-side classifications are pre-recorded at `04:121-122` with **every** matching row listed so a disagreement cannot be reconciled away; and every byte-diff (E1/E2 whole-file, `agents/supervisor.md` vs HEAD, the row-4 bound). QA owns all three. If QA's independent classification differs on any of the nine units, that is an **AC-1 failure to report, not a disagreement to settle**. | `06_TEST_REPORT.md` |
| RES-5 | Two stage-doc size facts, honest and unmeasured by any check (`verify_all` measures nothing under `docs/features/` in either shell): the reconstructed `02` contract is 725 lines against the 500-line cap, and **`PM_LOG.md` is at 443/500 with three stages still to log**. Compaction is PM-owned, at a stage boundary only, never mid-stage (`70-doc-size.md` Rule 2). | `07_DELIVERY.md`, PM |
| RES-6 | The round-record clause is a **12-copy** hand-maintained invariant across shipped surfaces (2 rule twins + 7 framework agents + 3 inline partition templates), plus prose restatements in `docs/workflow.md` and its twin. Nothing mechanical keeps the copies in step, and the trade that would collapse them — AC-11 against AC-12/R8 — is a design decision. Whoever next edits the wording edits twelve files or accepts divergence. | `07_DELIVERY.md`, follow-up pool |
| RES-7 | **The prose concession is real and should be graded for marked prose too.** For a fenced or blockquoted body the mechanism is structural and I could not reopen it. For prose it remains compliance-with-a-citable-rule: an **unmarked** normative sentence is not a byte-form by the shipped definition and enters via row 2 (`02` §13 row 4 / R9 already grade this "Medium"), and a **marked** one-sentence byte-form fitting no declared shape reaches the contract via row 5 when row 4 rejects (**CR-12**) — a third outcome the design's "either way" does not name. Neither breaks AC-1's determinism. | `07_DELIVERY.md`, follow-up pool |
| RES-8 | The shipped rule and `CONTEXT.md` carry a round-relative comparative ("row 2 **now** hands … **rather than blessing**") that the rule's own row 8 and no-changelog paragraph would route out of a stage doc (**CR-9**). Transcription was mandated by C-13, so the corrective is a design edit: delete "now" and "rather than blessing" in all three sites at once. | `07_DELIVERY.md`, follow-up pool |
| RES-9 | Six of the seven agent schemas state only one direction of the boundary rule's relationship to a declared shape (**CR-11**); the converse — a byte-form leaves the contract even though it fits — ships only in `agents/solution-architect.md:33-37`. Adding it costs one sentence per agent and needs a ledger row. | `07_DELIVERY.md`, follow-up pool |
| RES-10 | `skills/harness-batch/SKILL.md:81`'s secondary fallback is an `OR`, so a delivery whose `## Verdict` is `FAILED` or `BLOCKED: NEEDS-HUMAN` but whose `## Summary` carries `Final verify_all result: PASS` is still marked `done` and skipped on resume — and E29 makes such a document an **authored** form for the first time. The reachable population includes every deferred-human `BLOCKED: NEEDS-HUMAN` delivery, which T7.1 exists to compose (**CR-14**). Correctly left untouched (C-15; `:84` declares the tolerance deliberate); the fix — gate the fallback on the primary being *absent* rather than *failed* — is a driver-semantics decision. | `07_DELIVERY.md`, follow-up pool |

**Closed this round, recorded so it is not re-litigated:** round 2's RES-2 (F-21 / QA-3, row 6's unqualified "statement") is **fixed in the shipped bytes** — row 6 now inherits row 5's definition explicitly and names both a destination and the gap instruction, and QA's smuggle reaches `no home`. It no longer travels.

## Axis status

- **Standards-conformance**: **5 findings (CR-5, CR-9 MINOR; CR-7, CR-8, CR-13 NIT), worst = MINOR.** Caps hold by measurement on all six changed files with 9 lines of tightest headroom; the E1/E2 twins are byte-identical inside the inserted section with exactly the five standing divergences elsewhere, verified line by line; the byte-freeze holds and I read all four `archive-task` matchers rather than the report about them; no script, JSON or HTML surface acquired this round's vocabulary; Q-8's don't-ship list leaked nothing and dropped nothing normative.
- **Spec/design-fidelity**: **4 findings (CR-10, CR-11, CR-12, CR-14 — all MINOR), worst = MINOR.** The three constructs AC-1 measures ship byte-identical to `02` §2, C-7's four load-bearing clauses are intact in both the rule and `agents/solution-architect.md`, C-13's reconciliation is transcribed from its authorised source and swept independently (the developer's third instance is real), and E29's `## Verdict` bounds `archive-task`'s harvest exactly as C-3 and Q-12 require. The four findings are boundary conditions the amendment newly exposed — each is design-owned, none changes a destination or a shipped mechanism, and each travels.

## Verdict

**APPROVED (0 CRITICAL, 0 MAJOR; 6 MINOR, 3 NIT as notes)** — proceed to stage 6 for independent re-verification of AC-1, AC-6 and the byte-diffs (RES-4). No finding routes back to stage 4: CR-9/CR-10/CR-11/CR-12/CR-14 are design- or driver-owned and travel as residuals, CR-5 is adjudicated non-blocking, CR-7 is PM-owned at delivery, and CR-8/CR-13 are one-number corrections inside `04`'s own prose.
