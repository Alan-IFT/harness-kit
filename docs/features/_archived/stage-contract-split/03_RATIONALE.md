# 03_RATIONALE.md — Gate Review · T-18 `stage-contract-split` (round 3)

> Rationale portion for 03_GATE_REVIEW.md. Non-binding.
> Persisted verbatim by PM: the gate-reviewer is read-only (no `Write`/`Edit` tool by contract).

**Files opened this round**: `02` (full, 499L); `06_TEST_REPORT.md` (full); `PM_LOG.md`; `.harness/rules/70-doc-size.md` (full, shipped round-2 text); `_archived/stream-defer-human/02_SOLUTION_DESIGN.md:80-134`; `_archived/hook-truth-verify-scope/02_SOLUTION_DESIGN.md:228-245`; `agents/pm-orchestrator.md:182-231` + heading scan; `agents/supervisor.md:88-115`; `agents/code-reviewer.md:88-101`; `agents/gate-reviewer.md:10-45`; the `fits no declared shape` clause in all six agents; `.harness/scripts/archive-task.sh:40-69`; `skills/harness-{stream,batch}/SKILL.md` `DELIVERED` sites; `CONTEXT.md:52-81,126-139`; `.harness/rejected-decisions.md` decline keys; `.harness/insight-index.md` (all 30); `verify_all.sh` `E.4b`.

**The three disputed units, classified before reading the architect's walk.**

- `stream-defer-human/02:94` — ladder step 3 (unbroken `>` run) makes the two-sentence quote **one** unit, so the sentence-count property the round-2 disagreement turned on is now irrelevant. Row 2 is excluded ("replace … with:" satisfies the byte-form definition); row 4 rejects (a shorter multi-satisfiable statement exists); row 5 rejects (blockquote); **row 9 → `no home`**. Agrees with §2.
- `stream-defer-human/02:98` — identical walk; my round-2/QA verdict of "row 2 → contract, because it is one imperative sentence fitting `## Constraints`" is now closed **twice over**, by row 2's exclusion and by row 5's blockquote clause. **Row 9 → `no home`**.
- `hook-truth-verify-scope/02:236-239` — step 6 splits the paragraph into three sentences; s1 → row 5, s2 ("§3.1 and §3.2 give full bodies…", a claim about this document) → row 10, s3 ("do not degrade this section to a diff") → row 5. **Contract / `no home` / contract**, one destination each. Agrees with §2, and it resolves QA's P-08 for the load-bearing sentences; the residual judgement on s1 alone is F-32.

**What actually carries the QA-2 fix.** The destination for verbatim content no longer depends on getting the unit granularity right: row 2 excludes a byte-form regardless of how it is chunked, and rows 5/6 exclude fences and blockquotes regardless of chunking. A marked ≤3-sentence prose byte-form now walks to row 4 — contract **once**, under `## Byte-form specification`, with the test result stated in the row and visible to the gate — or to row 9. Either way it no longer enters an arbitrary `stmt` section unlabelled, which is what QA measured at 27 of 38. The `## Byte-form specification` gate is now doubled: `agents/solution-architect.md:34` (C-7) and the rule's own row 2.

**QA-3's smuggle, re-run against the new row 6.** `**OQ-3's answer**:` + a 115-line fence → step 2 (one unit) → row 6 rejects (fenced block is never a statement) → row 9/13/14, never contract. Blockquote variant: rejected by the same clause. Plain-prose variant: step 6 shreds it into sentences, and only genuinely binding ones survive to row 5 — the blob does not ride in whole. Row 6 is now largely *dominated* by row 5 (row 5 fires first on any binding one-sentence answer that fits no declared shape), which §2's precedence clause pre-authorises as harmless redundancy; "Adequate" in §13 is therefore conservative rather than optimistic.

**§13's re-grade, judged row by row.** *Fenced — Strong*: earned, unchanged, and QA verified it against the shipped bytes. *Quoted — Strong (new)*: earned, and stronger than the design claims, because it does **not** depend on the author-declared predicate — row 5's "or a blockquote" is a syntactic property, so an unmarked quotation still cannot become a contract statement; §13 attributes the win mainly to row 2's exclusion, which slightly under-credits the clause doing the work. *Row 6 — Adequate*: conservative, see above. *Unmarked prose — Medium*: matches what the mechanism delivers, and the concession is graded in three places (R9, §13 row 4, §17) rather than buried, with QA's own sentence ("a reformat, not an elimination — QA's finding, and it stands") adopted verbatim. The one direction §13 does not grade is starvation (F-32).

**Attempts to build a unit the ladder routes two ways.** Fence inside a list item → step 2 beats step 5, fence is its own unit (desirable — it closes the bullet-wrapped byte-form). Fence inside a blockquote → different unit, same destination. Non-conforming content inside a declared section → step 1 matches the section but names no unit; every construction still lands identically, because row 2 requires *fitting* (F-27). Multi-sentence top-level bullet → determinate but routes whole where a paragraph would split (F-28). Colon-terminated lead-in → no step names a unit (F-29). None of these is the class QA broke, and none reopens AC-2.

**Tables audited after finding one imprecise ladder** (insight L21): §5 is total via its "any consumer, otherwise" row; §3 covers all seven stages, each with the fallback sentence, and its stage-7 rows are now honestly `(new — E29)` — I checked `## Entropy watch`'s `(kept)` separately and it is correct, the heading is emitted at `pm-orchestrator.md:254`; §14, §7's P/D tables and §10 reconcile. The 14-row table's amended rows 2/5/6 are total and terminal.

**Cap arithmetic.** `70-doc-size.md` 156 → **~173** against a projected ~170 (22 lines in, 5 out), 27 under the cap. `pm-orchestrator.md` 282 → **287–289** (E9's T7.3 +0/+1 into the single-line trigger block; E29 `## Summary`+blank = +2, `## Verdict` section = +3/+4), ≥11 under the cap, with a real contingency (`## Document size discipline` exists at `:112`). `code-reviewer.md` 166 → **166–167**, since T5.4 appends into the single physical line at `:95`; the design's ~168 is conservative. No projection puts a `wc -l` figure within 10 of a cap, so no `I.*` WARN is reachable and `verify_all`'s exit-1-on-WARN is not in play.

**Why this is not a third rollback.** Every open finding is either a transcription instruction with an authorised source already inside `02` (F-25 — the corrected sentence is in §2's own agreement paragraph), a disposition item, or a precision residual that changed no destination in any construction I could build. The one MAJOR is a one-clause reconciliation the developer can make without authoring normative text, which is the same authority boundary Q-8 drew and drift D-1 exercised. Blocking here would cost a stop-and-escalate for work that stage 4 can do under a named condition, and that is not the truthful reading of this evidence. Equally, I have not approved the construct on the architect's word: I classified the three disputed units before reading §2's walk, re-ran QA's row-6 smuggle against the shipped-form text, and tried five new attacks on the ladder. QA will re-run its 25-unit probe and its C-8 walk against a rule that is materially harder to break than the one it broke twice — and C-14 makes the probe record multi-matches, so the next failure, if there is one, surfaces as data rather than as a disagreement.
