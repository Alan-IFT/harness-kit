# PM Log — resilient-hooks (T-12)

> Mode: full · /harness-stream default pool · PM in main thread. Real consumer bug fix (per-turn Stop hook error). Independent.

- 2026-06-20 · Task created (folder + INPUT.md). Mode=full. Root cause confirmed this session (Stop hook brittle + relative path; plugin dogfood-settings hygiene latent). claude-code-guide gave authoritative plugin-hook facts.
- 2026-06-20 · intervention.md check: absent.
- 2026-06-20 · insight-index read; surfaced: J.1 schema + T-020 E.4b/D.4b congruence + test-init hook-path asserts + test-harness-upgrade dangling/congruence probes (the assertion ripple); guard-rm must stay fail-CLOSED (safety) while sync/ambient go fail-open (convenience); $CLAUDE_PROJECT_DIR anchoring; cross-shell parity.
- 2026-06-20 · Stage 1 dispatch: harness-kit:requirement-analyst — scope the resilient hook form (Unix+Windows), the template+dogfood+upgrade application, the dogfood-settings relocation, and enumerate every verify_all/test assertion that parses the hook command; decompose if needed; keep guard-rm fail-closed.
- 2026-06-20 · Stage 1 RETURN: **READY**. 14 in-scope behaviors (slice A A1–A9, slice B B1–B5). 4 open questions, ALL proceedable (recommended-answer under deferred-human). Resilient form: convenience hooks (Stop/sync + 2 ambient) → `$CLAUDE_PROJECT_DIR`-anchored + fail-open (no-op exit 0 if script absent); guard-rm → SAME anchor but NO `|| exit 0` (fail-CLOSED — OQ-1a). NOT decomposed (A+B share `.claude/settings*.json` + same J.1/F.2 surfaces; A9 writes resilient form into the very settings.local.json B creates). Version 0.43.0→0.44.0, no count flip. **Load-bearing call flagged to SA (OQ-3):** the E.4b/D.4b path-extraction ERE boundary class is `["' =]` (no `/`) + cwd-relative existence check — a `$CLAUDE_PROJECT_DIR/`-prefixed path can break BOTH extraction and resolution. RA recommends choosing a command form whose extractable token stays the cwd-relative `.harness/scripts/<name>` substring. Assertion-ripple: 7 surfaces (J.1; E.4b/D.4b; test-init path-exists+OS-variant+`*_COMMAND` literals+delete-dangling; test-harness-upgrade fixtures A/H/I/P/P2/M1/M2/M3+`t20_*` literals; upgrade-project S3/S6 + migrate rewire/scan; harness-status §3b/§3c; harness-init 10b + harness-adopt 6). Doc: `docs/features/resilient-hooks/01_REQUIREMENT_ANALYSIS.md`.
- 2026-06-20 · Stage 2 dispatch: harness-kit:solution-architect — design the exact resilient command strings (both shells), the guard-rm anchored-but-fail-closed form, and SOLVE the OQ-3 ERE-vs-$CLAUDE_PROJECT_DIR extraction tension; enumerate the exact lockstep edits across all 7 ripple surfaces; sequence A-before-B; honor deep-module vocab + design-over-guards (no new check).
- 2026-06-20 · Stage 2 RETURN: **READY**. Resilient form = **split anchor from invocation**: convenience hooks `sh -c 'cd "$CLAUDE_PROJECT_DIR" 2>/dev/null && [ -f .harness/scripts/<name>.sh ] && exec bash .harness/scripts/<name>.sh || exit 0'` (fail-OPEN); pwsh twin `Set-Location $env:CLAUDE_PROJECT_DIR; if (Test-Path -LiteralPath ... -PathType Leaf){ & pwsh -NoProfile -File ... }; exit 0`. guard-rm = SAME anchor, NO `|| exit 0` (fail-CLOSED, OQ-1a confirmed). **OQ-3 = option (a) with correction:** the dispatch's `bash "$CLAUDE_PROJECT_DIR/.harness/..."` form would be INVISIBLE to the congruence ERE (the `/` before `.harness` is outside boundary class `["' =]`) → silent T-020 regression; SA instead `cd`-anchors at runtime and leaves a **space-preceded bare relative token** `.harness/scripts/<name>.<ext>` that the UNCHANGED ERE grabs + UNCHANGED cwd-relative `[[ -f ]]` resolves → **E.4b/D.4b ×6 tmpls are 0-edit verify-only** (the payoff; avoids option-b's ~12-file regex rewrite). **22 lockstep edit points** (J.1×2, test-init×2, test-harness-upgrade×2, upgrade/migrate×4, status×1, init+adopt×2, F.2×2, dogfood settings.json×1, new settings.local.json×1, .gitignore×1, version fan-out×4+CHANGELOG×1). Version 0.44.0, no count flip. **Residual risks flagged to Gate:** (1) F.2 is the ONE real check edit — must read guard-rm evidence from `.claude/settings.local.json` (fallback) since committed settings.json loses hooks; (2) A8 new brittle→resilient rewrite transform (over-eager rewrite of user-custom hooks risk; mitigated by C1 left-bounded ERE + harness-tool-name gate); (3) cross-shell byte-parity of JSON-escaped `\"` strings (QA cmp); test fixtures encoding OLD form (t20_pick, Fixture A/H/M2, Fixture P's `##* ` last-token extraction breaks on resilient string ending in `0`/`}`) must move to resilient form. Doc: `docs/features/resilient-hooks/02_SOLUTION_DESIGN.md`.
- 2026-06-20 · Stage 3 dispatch: harness-kit:gate-reviewer (read-only; PM persists its 03 doc) — holistic completeness/feasibility/risk review of RA spec + SA design. Scrutinize: the OQ-3a `cd`-anchor claim (does the bare token REALLY stay scanner-visible + cwd-resolvable in BOTH shells?), guard-rm fail-closed integrity, F.2 settings.local.json fallback, the brittle→resilient rewrite safety (no clobbering user-custom hooks), the 22-edit completeness vs the 7 ripple surfaces, and any I.3/line-cap or count-flip breach.
- 2026-06-20 · Stage 3 RETURN: **APPROVED WITH CONDITIONS**. PM persisted gate text verbatim to `03_GATE_REVIEW.md`. **OQ-3a CONFIRMED true** on both shells (hand-traced live ERE: `verify_all.sh.tmpl` L207-210 + `verify_all.ps1.tmpl` L172 `Groups[2]`+Test-Path) — the `cd`-anchor + space-preceded bare `.harness/scripts/<name>.<ext>` token IS scanner-visible + cwd-resolvable; `||`/`&&`/brace punctuation does NOT ride along; E.4b/D.4b ×6 tmpls genuinely 0-edit verify-only. **2 missed lockstep surfaces caught (the design under-counted):** F1/C1 — `upgrade-project` + `migrate-scripts-layout` are an **8-file** lockstep (4 repo + 4 `templates/common/.harness/scripts/`); `sync-self.sh` mappings 6/7 enforce byte-identity and verify_all **E.1** runs `sync-self --check` → editing only repo copies = guaranteed E.1 FAIL (edit template-common source + run `sync-self`). F2/C2 — `test-init.{sh,ps1}` `test_migrate` block (~L558-565: Stop/PreToolUse/`_doc_sync_hook`/`-NoProfile`≥2 count) is a third unenumerated test surface beyond the `*_COMMAND` literals; A8 migrate-rewrite changes its output. Blocking C3 — `t20_pick` + all `*_COMMAND` test literals must hold the **JSON-escaped** resilient bytes (`\"`) for the exact `grep -qF '"command": "<lit>"'` matches. Blocking C4 — F.2 must add `.claude/settings.local.json` fallback (both shells) since committed settings.json loses hooks. Advisory C5 — Fixtures P/H `eval` exits 0 via fail-open not real-run (QA set `CLAUDE_PROJECT_DIR` first); optional `75-safety-hook.md` dogfood wording refresh. `.gitignore` `*.local` does NOT cover `settings.local.json` (B3 explicit add required). No I.3 breach, no count flip.
- 2026-06-20 · Stage 4 dispatch: harness-kit:developer — implement the approved design with ALL gate conditions C1–C4 baked in (8-file upgrade/migrate lockstep via sync-self → E.1 green; test_migrate update; JSON-escaped test literals; F.2 settings.local.json fallback both shells). Edit both shells symmetrically; verify on Bash (verify_all.sh must reach 32/0/0); PS twins green-by-symmetry (operator-pending). A-before-B sequencing.
- 2026-06-21 · Stage 4 RETURN: **IMPLEMENTED**. verify_all.sh **32/0/0**. 25 files (24 tracked + new gitignored `.claude/settings.local.json`). Regression drivers green: test-init.sh 278/0, test-harness-upgrade.sh 85/0 (+6 resilient asserts), test-supervisor.sh 45/0, test-verify-i6.sh 58/0, test-language.sh 39/0, test-real-project.sh 90/0. **C1 E.1 GREEN** after editing the 4 template-common sources + `sync-self.sh` propagate (`--check` = "In sync."). C2/C3/C4 done. Runtime-verified: AC-1 missing-script rc=0 no-stderr; AC-2 subdir anchor runs script; AC-5 guard-rm missing rc=127 (fail-CLOSED). **2 items flagged for CR:** (1) minor design-drift — `cd ""` on EMPTY `$CLAUDE_PROJECT_DIR` stays in cwd on this bash (vs OQ-2a's assumed fail) — still safe fail-open, guard half unaffected; (2) added an ampersand-safe `str_replace_all` helper (NOT in design edit list) to defeat bash 5.2 `${var//}` `&`=matched-text corrupting the literal `& pwsh` in resilient JSON — also the harvest Insight. PS twins green-by-symmetry NOT executed (PowerShell denied) → operator-pending: verify_all.ps1 / test-init.ps1 / test-harness-upgrade.ps1. Doc: `docs/features/resilient-hooks/04_IMPLEMENTATION.md`.
- 2026-06-21 · Stage 5 dispatch: harness-kit:code-reviewer (read-only; PM persists its 05 doc) — review implementation vs requirement+design+gate-conditions (completeness + design-fidelity, two-axis). Scrutinize the 2 dev-flagged items: (1) is the `cd ""` empty-var drift truly safe fail-open in BOTH shells (does pwsh `Set-Location -LiteralPath ''` behave equivalently)? (2) is the `str_replace_all` ampersand-safe helper correct + applied everywhere the resilient `& pwsh` JSON is built (upgrade+migrate, both shells)? Verify C1–C4 actually landed, guard-rm fail-closed integrity, no count-flip/cap breach, version 0.44.0 fan-out complete.
- 2026-06-21 · Stage 5 RETURN: **APPROVED-WITH-NITS**. PM persisted CR verbatim to `05_CODE_REVIEW.md`. No CRITICAL/MAJOR. **Guard-rm fail-CLOSED verified INTACT in all 4 forms + across the empty-`$CLAUDE_PROJECT_DIR` drift in BOTH shells** (bash `&&` short-circuit / pwsh `-Command` Continue-pref both still RUN the guard or fail non-zero — never silently skip). Both dev-flagged items adjudicated: (1) empty-var drift SAFE (only convenience hooks, fail-open regardless; safety half unaffected); (2) `str_replace_all` CORRECT + applied at every `&`-bearing build site (grep confirms no live `${//}` on `&`-value remains). C1 byte-identity confirmed, C4 F.2 still fails-closed on truly-missing guard, no count-flip, 0.44.0 fan-out consistent. **3 MINOR test-fidelity nits:** `test-real-project.{sh,ps1}` + `test-init.ps1 Test-ZhOverlay` still author the BRITTLE `*_COMMAND` form → AC-7 "no surviving brittle authoring form" not fully met + weakens test-real-project dogfooding. CR routing note: non-blocking, PM discretion fix-now vs merge-then-fix. Doc: `docs/features/resilient-hooks/05_CODE_REVIEW.md`.
- 2026-06-21 · PM decision: **fix-now** the 3 brittle-fixture nits (cheap literal swaps; makes AC-7 truly met + restores test-real-project dogfooding fidelity). NOT a same-stage rollback (CR APPROVED) — a targeted fidelity touch-up routed back to the same developer agent, then re-verify, then QA. Honors verify-before-done + design-fidelity. (SendMessage unavailable in this env → fresh dev agent given the exact reuse instructions.)
- 2026-06-21 · Fidelity-fix RETURN: 3 sites fixed (test-real-project.{sh,ps1} + test-init.ps1 Test-ZhOverlay) → resilient literals byte-identical to canonical test-init.sh block (clean diff). guard-rm stayed fail-CLOSED in the new literals (ends `guard-rm.ps1\"`, no `|| exit 0`); convenience end `}; exit 0\"`. `& pwsh` survived literal replace (no `${//}` corruption). verify_all.sh **32/0/0**, test-real-project.sh **90/0**. AC-7 now fully met. PS twins green-by-symmetry (operator-pending). Note appended to 04 doc.
- 2026-06-21 · Stage 6 dispatch: harness-kit:qa-tester — validate user-observable behavior of the ORIGINAL bug fix end-to-end (missing/unresolvable script → NO per-turn Stop error; subdir launch → anchored resolve; guard-rm STILL blocks destructive cmd when present, fails-closed when absent), run the full Bash suite, and consider codifying the AC-5 fail-closed runtime mutation probe the CR flagged as missing. Verify regressions across all drivers. PS-side operator-pending.
- 2026-06-21 · Stage 6 RETURN: **PASS — APPROVED FOR DELIVERY**. All 4 user-observable behaviors verified with real Bash repros: (1) absent Stop script rc=0/no-stderr (control: brittle form gives exact consumer error rc=127); (2) subdir launch anchored-resolve+run; (3) guard-rm fail-CLOSED both present (rc=2 BLOCKED, benign ls rc=0) + absent (rc=127); (4) empty-var guard still runs/fails non-zero, never silent-skip. Suite: verify_all.sh 32/0/0 · test-init.sh 278/0 · test-harness-upgrade.sh 89/0 · test-real-project.sh 90/0 · test-supervisor.sh 45/0 · test-language.sh 39/0 · test-verify-i6.sh 58/0. **AC-5 probe ADDED** as Fixture Z (+4 bash / +3 ps) — no new check, baseline preserved, 5/5 stable. Baseline bumped: test_harness_upgrade_bash 79→89, test_init_bash_no_python3 276→278, last_verify→2026-06-21 (PS keys operator-pending). No defects. Doc: 06_QA_REPORT.md.
- 2026-06-21 · Stage 7 (PM, main thread): wrote 07_DELIVERY.md (Verdict DELIVERED, Final verify_all PASS 32/0/0). Final PM regression gate: verify_all.sh **32/0/0**. archive-task.sh --task resilient-hooks → moved to docs/features/_archived/resilient-hooks/, harvested 1 insight (bash 5.2 patsub_replacement `&`-corruption), rotated 1 (2026-05-23 GNU-grep line → insight-history.md). Post-archive re-verify **32/0/0**. Pool artifacts updated (BATCH_PLAN T-12→done, STREAM_LOG delivery+exit, tasks.md row). **T-12 DELIVERED v0.44.0. Pool drained → entropy-watch cadence checked on exit.**
- 2026-06-21 · **POST-DELIVERY operator PS verification (release gate).** Operator ran the full PS suite on Windows; "green-by-symmetry" FAILED and exposed 2 real PS-only defects (no `.sh` run could catch — PS denied to agents). **DEFECT-2 (parse):** bash-form `else` branch authored as a double-quote concat → PS reads `"\"` as close+stray-token → ParserError; PS parses the whole file first, so the never-taken branch was fatal — broke 3 test drivers AND the shipped upgrade-project.ps1 + migrate-scripts-layout.ps1 (the /harness-upgrade repair path would not load on Windows). Fixed: single-quoted PS literals / `-f $tool`, 7 files. **DEFECT-3 (runtime):** `Get-ResilientCmd($tool,$isWindows)` param collided with the read-only automatic `$IsWindows` → threw on first call (moved scripts, then aborted before settings rewrite). Fixed: param `$isWindows`→`$forWin` (repo + template twins); scanned all other funcs/params — no further collisions. **PS re-run all green:** verify_all.ps1 32/0/0, test-init.ps1 316/0, test-harness-upgrade.ps1 exit 0 (all fixtures incl Fixture Z), test-real-project.ps1 90/0, supervisor 49/language 39/verify-i6 58/guard-rm 17. `[Parser]::ParseFile` sweep = all ok. bash re-confirmed: sync-self In sync (E.1), verify_all.sh 32/0/0. Ledger: baseline test_init_ps 314→316 + README badge; insight harvested (PS unexecutable-by-agent → operator PS run is a mandatory release gate). **T-12 now verified green on BOTH shells.**
