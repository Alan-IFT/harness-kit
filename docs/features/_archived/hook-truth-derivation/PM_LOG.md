# PM Log — T-16 `hook-truth-derivation`

- **Mode**: `full` (stages 1-7)
- **Dispatched from**: `/harness-stream` drain — **final row in the pool**
- **Decision mode**: Mode 2 (balanced) · **deferred-human mode: defer, do not ask**
- **Developer routing**: no `.harness/agents/dev-*.md` present → **single Developer mode** (`harness-kit:developer`)
- **Gate baseline captured before dispatch**: `verify_all` **PASS 32 / WARN 0 / FAIL 0** (bash)

## Goal

Retire the hand-maintained byte-identical copies of the hook command strings across the
project-creation, adoption, upgrade-repair and layout-migration flows by having each derive
its commands from the single source of truth (`hook-spec.{sh,ps1}`, delivered by T-13), so
adding or changing a hook is a one-place edit instead of a multi-file lockstep. Also folds in
the three prose consumers that still assert a stale settings-file location.

## Pre-dispatch checks

| Check | Result |
|---|---|
| `.harness/intervention.md` present? | **No** — nothing to consume |
| `.harness/agents/dev-*.md` | none → single Developer |
| `docs/tasks.md` related history | T-13 (`hook-truth-spec`, the source), T-14 (`hook-truth-status`), T-15 (`hook-truth-verify-scope`), T-12 (`resilient-hooks`, the origin of the byte-forms) |
| `.harness/insight-index.md` read | yes — 9 entries surfaced into dispatch prompts (see below) |

## Insight-index entries surfaced downstream

1. bash 5.2 `patsub_replacement` — literal `&` in a `${var//a/b}` replacement expands to the match (2026-06-21).
2. PowerShell is agent-unexecutable here; whole-file parse, `$IsWindows` collision, bash-literal idiom does not port (2026-06-21).
3. PS binary `-join` binds below `+` — silent wrong string (2026-07-31).
4. `archive-task`'s insight harvester drops continuation lines of a wrapped bullet (2026-08-01).
5. `verify_all` exits 1 on `warns > 0` — the 200-line rule cap is a hard gate (2026-08-01).
6. Editing a live fail-closed `PreToolUse` hook has one safe sequence (stage in the unwired template copy) (2026-08-01).
7. Cross-check a reported tally against the artifact that produced it, not against arithmetic (2026-07-31).
8. On a dirty tree, absence from `git diff --name-only` is not a freeze proof — needs mtime ordering (2026-08-01).
9. An unanchored whole-file `grep` for a JSON key is vacuous when the file documents itself (2026-08-01).

## Stage transitions

| # | Stage | Round | Timestamp | Decision |
|---|---|---|---|---|
| 0 | PM setup | — | 2026-08-01 | Folder created, insight-index read, no intervention. Dispatching stage 1. |
| 1 | requirement-analyst | 1 | 2026-08-01 | **READY**. No BLOCKED marker; no human-reserved point engaged. 8 Open Questions each with a binding `Recommended:` answer. Notable: the analyst **derived** the call-site inventory rather than inheriting it and found the T-13 hand-off header **incomplete** (`test-real-project.{sh,ps1}` omitted, each carrying a full 8-cell copy); all three stale prose claims confirmed live; a **circularity trap** identified (once the repair helpers delegate to the spec, `test-init`'s T-13 byte-identity assertions compare the spec with itself — tautological and still green). Inherited T-15 containment residual decided **IN SCOPE** (OQ-2) with reasons. Intervention re-check: none present. **Advance to stage 2.** |
| 2 | solution-architect | 1 | 2026-08-01 | **READY**. Spec-adapter design (memoised, ≤9 spawns/run, `resilient_cmd`/`Get-ResilientCmd` survive as 3-line delegations so both call sites stay byte-untouched); byte-identity chain traced through `printf`/`$( )`; `&`/`patsub_replacement` hazard made **unreachable** (adapter does zero substitution) with the existing `str_replace_all` mandated as preservation; containment fix via a leading-whitespace-delimited window, no JSON parser, **32 checks held**; circularity repaired by re-anchoring the oracle to the frozen `EXP_*` with 16 rows in / 16 rows out in both shells (bash 355 and frozen PS 316 both hold); 28-row change ledger incl. the two files the T-13 header omitted. Operator PS list 11 → 16 (security-marked 2 → 4). Two items raised **for the gate**: (a) the session git snapshot reports a **clean** tree, contradicting requirement B-11's dirty-tree premise → design mandates S0 be re-derived, not inherited; (b) `insight-index.md` is at 30/30 with the harvester defect out of scope → each T-16 insight must be one physical line. Intervention re-check: none present. **Advance to stage 3.** |
| 3 | gate-reviewer | 1 | 2026-08-01 | **CHANGES REQUIRED** (dimension 7 FAIL, dimensions 2+6 WARN). 11 findings, **all routed to solution-architect; none to requirement-analyst** (`01` stands as written). Blocking: **F-2** AC-7's anti-vacuity direction is specified against `git show HEAD:`, but HEAD on this tree is v0.44.0 — it predates both T-13 and T-15's anchoring fix, so it measures a check T-16 does not modify; **F-3** the containment-window terminator rule is **non-total** (matches zero lines when `"PreToolUse"` is the last key in `hooks`, then reports it as "unterminated" → gate red on a valid template); **F-1** the adapter's stated placement rule does not cover `dst_dir`/`$dstDir`, which its own candidate list reads — 3 of the 4 flow files would abort at load or first call. Plus **F-4** (`ph_o` unbound → exit 1 instead of the contracted exit 4), **F-5** (`"${!hs_keys[@]}"` on an empty array breaks the bash-3.2 target the choice was justified by), **F-6** (standing flow-byte coverage silently drops 8 cells → 1, unrecorded as a residual), and minors F-7…F-11. Gate **independently re-derived the change ledger** (54-file idiom sweep) and confirmed it **complete** — no missing lockstep surface; also confirmed positively: the `&` hazard genuinely unreachable, fail-closed preserved on every degradation branch, mutation M-C correctly predicted with a real anti-vacuity direction, all cap measurements, and **adjudicated the freeze contradiction in favour of B-11** (the "clean" git snapshot is stale; developer must still re-derive S0). Intervention re-check: none present. **ROLLBACK 1 → stage 2 (solution-architect), round 2.** Consecutive rollbacks at stage 2: **1 of 3**. |
| 2 | solution-architect | 2 | 2026-08-01 | **READY**. New §18 answers F-1…F-11 individually. **F-2**: all pre-change claims moved onto an **S0 working-tree capture** (new Rule 2 covering C-1/C-3/C-4 and §7.2), admissibility proven by hash + `mtime < T0` + a converse live-file check; the architect did **not** accept the gate's "C-1/C-4 are probably safe" guess and found C-4 was **impossible as written** (it diffed against `hook-spec.sh` at HEAD, where that file does not exist). **F-3**: terminator rule made total ("first non-blank line with leading width **≤ IND**", terminator excluded, `term = N` when none), with a three-row verification table; every other table re-audited — confirmed the gate on §5/§3.5/§7.1/§15/§14/§8 and **corrected** §3.5 (total over `resilient_cmd` call sites but not over the two PS `$IsWindows` host-OS reads). **F-1**: candidate resolution made **lazy**, so no binding order changes in any of the four files. While fixing F-1 the architect found **two defects the gate did not flag**: the bash cache would have been silently discarded by the `$( … )` call sites (13 spawns vs NFR-1's 9) → out-variable convention (D-6), and the wrapper became a pass-through → **retired** (D-5), which dissolves F-9 at the root. **F-4** fixed by hoisting `ph_o`/`ph_c` to S3 scope, with a **narrowing disagreement argued from the file** (the PS twin was never broken — `.ps1:254` is already at S3 scope). **F-5** C-style counter loop, no `[@]` expansion. **F-6**: chose **both** — two standing 4-row scans (zero new checks, zero pin movement) **and** named residual RES-1 so it reaches delivery. Minors: operator register restated (11 numbered → 16; 19 → 24 total), F-8 symptom corrected to 9 loud-red rows, F-10 mandate+grep widened, F-11 routed to operator items 12(f)/13(e). Also newly caught: `hook-spec.{sh,ps1}` provenance sentences and a `test-init.ps1` comment name a helper that will no longer exist. 3 autonomous calls logged with revert costs. Intervention re-check: none present. **Advance to stage 3, round 2.** |
| 3 | gate-reviewer | 2 | 2026-08-01 | **APPROVED FOR DEVELOPMENT** — all 8 dimensions PASS, F-1…F-11 all **CLOSED** and each re-verified against the real files rather than against the design's account of them. The gate **adjudicated both architect disagreements in the architect's favour and said so plainly**: `upgrade-project.ps1:254` is already at S3 scope so round-1 F-4 overreached on the PS half (and "fix both shells" would have moved a correct line), and round-1's §3.5 totality reading missed the two `$IsWindows` host-OS reads. It also **withdrew** its round-1 "probably safe" characterisation of C-1/C-4 as an unverified inference. The three previously-ungated autonomous calls were audited from scratch and all **ACCEPTED** (D-5 hardest: both name-anchored extractors deleted, no third exists repo-wide, 17-in/17-out so no pin moves). Ledger re-swept after the retirement — **complete after one addition** (`hook-spec.ps1:94`). Two **binding conditions, not a rollback** (both mechanically dischargeable by the developer): **F-12** the design's "both scans green pre-change — measured, not assumed" is **false** for the idiom scan (16 non-comment hits, 4 of 4 files red pre-change — the correct and desirable state, but the claim must not be transcribed into `04`/`06`); **F-13** ledger row 11 omits `hook-spec.ps1:94`. Plus **8 empirical captures** the gate could only verify by inspection and explicitly refuses to certify — notably the `test-init` 355 tally, which it states is **unverified by it** because 17-in/17-out is arithmetic and this repo's rule is to cross-check a tally against the artifact that produced it. Intervention re-check: none present. **Stage gate satisfied → advance to stage 4 (single Developer).** |
| 4 | developer | 1 | 2026-08-01 | **READY FOR REVIEW**. Declare-done gate captured: `verify_all.sh` **PASS 32 / WARN 0 / FAIL 0**, exit 0; `sync-self --check` → In sync. `test-init` **391/0** on this host and **355/0** under a `python3`-absent shim — the condition the pin's *name* describes, so `test_init_bash_no_python3_assertions = 355` is transcribed from a real run rather than from arithmetic. All other pinned drivers green from real runs (real-project 90, harness-upgrade 89, verify-i6 58, supervisor 46/45-with-shim, language 39, guard-rm 87). Mutation **M-C** on the post-change gate produced **exactly the two predicted tokens** and whole-gate 31/0/1; the **anti-vacuity** direction PASSed from the **S0 working-tree capture** with both admissibility assertions recorded (S0 hash + `mtime 1785522577 < T0 1785532976`; live file hashing differently at proof time) and the capture deleted in the same step. `git cat-file -e HEAD:.harness/scripts/hook-spec.sh` independently confirmed the gate's premise that the spec does not exist at HEAD. Both binding conditions applied: **F-12** true pre-change state recorded (**16 non-comment idiom hits, all four flow files red**; substitution scan 0 → post-change 0/0) with the scan **not** narrowed; **F-13** `hook-spec.ps1:94` fixed alongside its bash twin. **F-4's `ph_o` hoist measured load-bearing** — the counterfactual exits **1** with no `SUMMARY\|` line and `ph_o: unbound variable`, the shipped build exits **4**. Freeze re-derived: the tree was **dirty at S0 (38 modified + 10 untracked)** — the session snapshot's "clean" claim is **false**, as the gate predicted; every frozen path passes all three §13 conditions. `75-safety-hook.md` still exactly **200**. **3 self-reported deviations** for the reviewer (A′ scans score `missing` rather than `0` for an absent flow file; `60-tool-handoff.md` landed at 131 vs the design's "≤130", cap 200; `dev-map.md` edited without a ledger row). Entire PS surface unverified — operator items 12-16 (two security), numbered list **11 → 16**. Intervention re-check: none present. **Stage gate satisfied (`verify_all` PASSED) → advance to stage 5.** |
| 5 | code-reviewer | 1 | 2026-08-01 | **APPROVED** — 0 CRITICAL, 0 MAJOR, 4 MINOR, 2 NIT. Both axes clean of blocking findings (Standards worst MINOR, Fidelity worst MINOR; masking invariant satisfied). The reviewer re-ran the greps itself rather than inheriting them and confirmed the central premise adversarially: no post-processing of the captured value anywhere in either shell (no `printf "$v"`, no bare `echo`, no unquoted expansion, no `-join`, no pattern substitution), the two spec twins' four literals character-identical, the `&` hazard **unreachable** (2 hits, both comments), and `guard-rm` fail-closed **structurally** — all seven failure branches enumerated into the single "return nothing" path and each followed to its terminal exit code to confirm no residue the write gate or a second run blesses at 0. Audited the two repair helpers **as a pair** in four files: they diverge in exactly one place, the `ph_o` hoist, which is where they are supposed to. All three self-reported deviations adjudicated **SOUND** (and DRIFT 3's ledger omission attributed to the design, not the developer). MINORs: **#1** a *fourth* stale provenance comment in `hook-spec` (`sh:162-163` + 3 twins) that was in neither the ledger nor gate F-13; **#2** an undisclosed one-sided PS host-OS *selection* delta on Windows PowerShell 5.1 (a strict improvement, but it falsifies §3.6's D-1 justification) → record-only, architect; **#3** case-insensitive `-match` at `verify_all.ps1:335,338` and `test-init.ps1:950-952,968-969` — a **cross-shell verdict divergence in the one check this task tightened**; **#4** the `04` record mis-locates the surviving R-1 divergence. Intervention re-check: none present. **PM routing decision: ROLLBACK 2 → stage 4 (developer), round 2, over the reviewer's non-blocking rating.** Rationale: MINOR #3 is a real verdict divergence in a gate check, and all three developer-routed MINORs are mechanical and bash-dischargeable — fixing them now means QA measures the final artifact rather than one with a known cross-shell gap. Consecutive rollbacks at stage 4: **1 of 3**. |
| 4 | developer | 2 | 2026-08-01 | **READY FOR REVIEW** (fix-forward). All three developer-routed MINORs discharged; 7 files touched, mtime-verified, nothing else moved. **#1** stale provenance sentence reworded in all four `hook-spec` copies, edited at the `templates/common/` source and propagated (`sync-self --check` → In sync; 0 hits for the old sentence outside the review doc's own quotation). **#3** case-sensitivity fixed at 4 sites / 7 operators — **and the developer flagged a fifth site it deliberately did not touch** (`verify_all.ps1:315`, the `{{GUARD_COMMAND}}` *presence* check) after proving from `git show HEAD:` that it is **pre-existing, not T-16's**. **#4** record corrected — **and the reviewer's offered "cheap R-1 closure" was DECLINED on a measurement**: GNU grep 3.11 does **not** read `\t` inside a bracket expression, so `verify_all.sh` → `[ \t]` would have matched a literal `t` and a literal `\` while **missing a real tab**; awk does read it as a tab, which is why the shipped asymmetry is correct rather than sloppy. The developer also surfaced a compounding trap: this host's *interactive* grep is **ugrep 7.5.0**, which accepts `[ \t]` — so a by-hand check in a login shell endorses the broken form while the non-interactive script run gets GNU grep. Documented in the gate itself (NIT #2 taken, comment-only); NIT #1 declined with reason (it would force a third operator re-parse for half a line of prose). Captures: `verify_all` **32/0/0**, `test-init` **391** (python3 present) and **355** under the python3-absent shim matching the pin's named condition, verify-i6 58, real-project 90, harness-upgrade 89, guard-rm 87 — every run reaching its own summary line; all 18 `baseline.json` numeric keys unchanged and the file not re-touched; **0 pins moved**. `verify_all.ps1` and `test-init.ps1` were re-touched **after** the operator list was written → **operator items 14(a)/15(a) must be re-run**; a round-1 `ParseFile` result for either is now stale. MINOR #2 left untouched for the architect. Intervention re-check: none present. **Advance to stage 5, round 2 (narrow — verify the round-2 delta and adjudicate the declined suggestion).** |
| — | infrastructure | — | 2026-08-01 | **Non-task fault, recorded for audit.** The PM run was terminated mid-dispatch by an API 403, then the sub-agent dispatch classifier became intermittently unavailable. Neither is a defect in this task's work; no stage result was affected and no stage was re-run. PM resumed at the queued stage-5 round-2 dispatch and retried until it succeeded. Per the coordinator's standing instruction: if the declare-done gate could not be run reliably, PM must return `BLOCKED` rather than claim a pass. **Approach changed on the coordinator's instruction: each stage artifact is now written to disk the moment that stage returns, with its PM_LOG row appended in the same step — no batching to the end.** |
| 5 | code-reviewer | 2 | 2026-08-01 | **APPROVED** (operative verdict) — 0 CRITICAL, 0 MAJOR, 2 MINOR, 4 NIT. Round-1 MINOR #1/#3/#4 **CLOSED**, NIT #2 **TAKEN**, NIT #1 **closed as declined-with-reason**. **The declined `[ \t]` harmonization is adjudicated CORRECT and the reviewer withdrew its own round-1 suggestion without reservation** — it verified the claim against POSIX bracket-expression semantics (inside a bracket expression `\` has no special meaning, so GNU grep reads `[ \t]` as `{space, backslash, t}`, while awk resolves `\t` in its string-escape layer *before* the regex engine sees it) and found its own round-1 NIT #2 and MINOR #4 were **in tension with each other** — the developer caught that tension by measuring rather than by assuming the two bullets were consistent. Had the "cheap closure" been taken, the gate would have gained a false FAIL on a valid tab-indented template **and** a false PASS on `"command"t:`. The surviving asymmetry passes all three soundness tests (intentional, per-tool-correct, recorded where the auditor looks). The `verify_all.ps1:315` scope call is upheld **and shown safer than the developer argued**: post-MINOR-#3 both shells still FAIL a lowercase placeholder, so what survives is a **diagnostic-token-set divergence, not a verdict divergence** — `:338` backstops it. Reviewer re-measured every invariant itself rather than inheriting: 32 `Step` calls with `G.4` last, 18 baseline keys frozen, `75-safety-hook.md` 200/200, zero `T-16\|hook-spec` in any `*guard*` path, both `hook-spec` twin pairs identical **at identical line numbers** (the signature of a source-side edit propagated, not a repo-side edit back-copied), and — the check that mattered most — that neither `verify_all.ps1` nor `test-init.ps1` has a `templates/common/` twin, so the propagation surface is exactly the four `hook-spec` copies. **One new MINOR (R2-1)**: the `:315` residual lives only in narrative prose and is absent from `04`'s "Open issues" list — the artifact stage 6/7 actually harvests — so it would not survive to delivery. **MINOR #2 carried to the architect, now enlarged**: `02_SOLUTION_DESIGN.md:369-370` quotes the sentence MINOR #1 just deleted and mis-cites its lines twice over. Intervention re-check: none present. **PM: discharging R2-1 directly as a one-line record edit (no code, no design decision), then advancing to stage 6.** |
| 5b | PM record edit | — | 2026-08-01 | **R2-1 discharged.** Added Open-issues **item 5** to `04_DEVELOPMENT.md` (the `verify_all.ps1:315` residual), attributed to PM and marked record-only. Reviewer's reasoning accepted: a residual living only in a fix section's narrative does not survive the stage-6/7 harvest, which reads that list. No code, no design decision, no count touched. |
| 6 | qa-tester | 1 | 2026-08-01 | **PASS WITH DEFECTS** — 0 BLOCKER / 0 CRITICAL / 0 MAJOR, 1 MINOR (record) + 3 NIT. Approved for delivery conditional only on record edits. `## Adversarial tests` section present (required). QA **re-measured rather than transcribed**, as instructed: `verify_all` **32/0/0** exit 0 with the count read from the run (`grep -cE` over the run output → 32), all pinned drivers reproduced (test-init **391**/**355**-under-shim, real-project 90, harness-upgrade 89, verify-i6 58, supervisor 46/45, language 39, guard-rm 87), every driver reaching its own summary line, suite run **3× end-to-end with zero flakes**, all 18 baseline numerics reproduced, `sync-self --check` In sync. **AC-7** M-C → exactly two tokens, 31/0/1, exit 2; pre-change S0 gate PASSed the same mutant with admissibility proven **four ways including a content-level one** (the capture carries T-15's anchoring and none of T-16's three tokens). **AC-1** mutation in 4 spec files propagated to all four flows with `hits=0` for the marker in every flow file; reverted byte-exact. Adversarial value beyond the pipeline: **AC-2 re-proved against a genuinely non-circular oracle** (`cb0ed57` predates `hook-spec` entirely) 8/8 both flows; a **partial-answer probe** (six of eight cells resolve, `guard-rm` alone does not) → exit 4, no permissive value, no residue blessed on re-run; the **`&` hazard measured** — the naive `${var//…}` writes `{{GUARD_COMMAND}}` into the *middle of the live guard command*, the shipped helper is immune; **NFR-1 measured at exactly 9 spawns**. **The MINOR is the notable one: a false measurement claim in the record.** `04:202-204` and `02:604-607`/§7.1 row 3 claim round-1's `== IND` rule "would have FAILed" a block-form `PreToolUse` moved to last key — QA measured that **it would have PASSed too** (the array's own `]` sits at width exactly 4), so the stated evidence does not discriminate between the two rules. QA then **built the fixture that does**: an **inline** `PreToolUse` as last key, where the shipped rule PASSes and `== IND` FAILs; it also confirmed the terminator-**exclusion** tightening with a new mutant `M-D`. The shipped rule is genuinely more total — only the evidence cited for it was wrong. QA left **zero footprint**: `git status --porcelain` byte-identical to QA start, 19/20 S0-frozen paths hash-unchanged, `75-safety-hook.md` 200/200, stray sweep empty; no test added and `baseline.json` untouched, reasoned (a bash-only assertion would move `test_init_bash_no_python3_assertions` without its unreconcilable PS twin, which OQ-8(b)/NFR-3 forbid). Intervention re-check: none present. **Stage gate satisfied (stage 5 APPROVED + stage 6 PASS, 0 blocking) → correcting the record, then stage 7.** |
| 2 | solution-architect | 3 | 2026-08-01 | **READY (record corrected)** — record-only, **not a rollback**: no design decision, mechanism, interface, ledger row or AC changed; count stays 32; frozen set untouched. Discharged the three items CR and QA routed to the architect. (a) §3.6's D-1 justification no longer claims the spec carries "the exact discrimination the existing derivation flows already use" — it now records the measured split: **zero** delta in bash, a **one-sided host-OS selection delta on Windows PowerShell 5.1** in PS, with three separate safety arguments (strict improvement; cannot fail-open, since both selected forms are spec-authored fail-closed strings; AC-2 untouched because OS is a *parameter* of the 8-cell comparison). (b) The dangling quotation was **withdrawn rather than re-pointed** — a sharper call than PM asked for: re-quoting the reworded comment would be **circular**, because this task wrote that sentence, so D-1 now rests on the measured duplication in the *pre-change* flows. (c) QA's D-1 folded in — the non-discriminating fixture is struck and QA's discriminating inline-last-key fixture plus mutant `M-D` transcribed in its place, conclusion kept. The architect also found the same false clause in **§12 operator item 14(e)** and struck it there (obligation unchanged in kind and count; `baseline.json` never carried the clause, so no doc/artifact divergence). New `## 20` section records each correction with what was false, what is true, and the evidence. |
| 6b | PM record edit | — | 2026-08-01 | **QA D-1, developer-side half corrected.** Annotated `04_DEVELOPMENT.md:202-204` in place: the sentence is struck and a PM correction block records that QA **measured** the block-form fixture to PASS under **both** rules (`start=68 IND=4 term=78`), so it never discriminated; the shipped rule's greater totality stands on QA's inline fixture and mutant `M-D` instead. Attributed, record-only, no code. Both halves of the falsified claim (design §7.1/§12 and development AC-7) are now corrected and cross-referenced. |
| 7 | PM delivery | 1 | 2026-08-01 | **DELIVERED.** `07_DELIVERY.md` written; `docs/tasks.md` updated (active row removed, completed row added). Stage gate honoured: stage 5 **APPROVED** and stage 6 **PASS** (0 blocking) before delivery. Final gate **PASS 32 / WARN 0 / FAIL 0**, count read from the run, re-confirmed by the coordinator after the last edit. 5 insights authored as **single unwrapped lines** (the harvester silently drops continuation lines — T-15 lost 4 bullets to this). **Entropy-watch cadence deliberately NOT run** — the stream owns that boundary and will run it when the pool drains. Next: `archive-task`. |
