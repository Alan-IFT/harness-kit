# 03 — Gate Review · T-022 `stream-defer-human`

- Mode: **full** · Reviewer: gate-reviewer · Date: 2026-06-13
- Upstream verdicts verified: `01` = READY FOR DESIGN, `02` = READY FOR GATE.
- Decision policy: Mode 2 — judgment calls adjudicated per rubric; only red lines escalate (none arose).
- Verification depth: every cited file:line read against the live tree; verify_all G.3/G.4/I.3/I.6 blocks read in source; tree-wide greps for `AskUserQuestion`, stop/continue prose, and the discriminator token.
- (Materialized verbatim by PM — the gate-reviewer thread had no Write tool; content authored by gate-reviewer.)

## Audit checklist (8 dimensions)

| # | Dimension | Result | One-line reason |
|---|---|---|---|
| 1 | Requirement completeness | **PASS** | All 4 stop-reason buckets + the (c)/(d) bright line are testable; boundary table (§5) covers empty/all-defer/sibling/re-defer/mixed/tie-break/Mode-1. |
| 2 | Design completeness | **PASS** | Every in-scope behavior (1-12) maps to a verbatim normative edit with file:line in §4-§9 fan-out table. |
| 3 | Reuse correctness | **PASS** | Reuse audit (§8) verified: STREAM_LOG step d/e, dependents-rule SKILL:125, resume SKILL:118, enum BATCH_PLAN:27 all exist as cited. |
| 4 | Risk coverage | **PASS** | §10 a-e cover the real risks; I add residue F-1 (encoding premise) and F-2 (batch D-4 inaccuracy) below. |
| 5 | Migration safety | **PASS** | No data migration; new enum value is additive, no gate pins the enum (verified), resume absorbs it. No flag needed. |
| 6 | Boundary handling | **PASS** | Empty queue → "None."; all-defer → normal exit not halt; sibling scoping `Depends on`-only; (c)+(d) tie-break d-wins. |
| 7 | Test feasibility | **PASS** | AC-1..AC-9 are doc-consistency + adversarial-grep gates, each mechanically checkable; no "feel fast"-class criterion. |
| 8 | Out-of-scope clarity | **PASS** | §13 + RA §4 explicit: batch untouched, no column, no parallelism, hard-safety stops unweakened. One advisory below (F-2) on the *rationale* for batch, not the boundary. |

## Findings

### F-1 — Console-encoding premise is FALSE, but immaterial to this task. (ADVISORY)
**Evidence:** SA §6 (`02:110`) states "the 2026-06-12 console-encoding fix already neutralizes the pwsh mojibake risk for the existing chars." I verified: `.harness/scripts/ambient-prompt.ps1` has **no** `[Console]::OutputEncoding` / UTF-8 line (read whole file, 67 lines; `$ErrorActionPreference` at :24, emits at :65 with raw codepage). `docs/features/_archived/stream-auto-decompose/07_DELIVERY.md:10` records the fix as **DEFECT-1, BACKLOG, explicitly unimplemented** ("Backlog fix: set `[Console]::OutputEncoding`…"); `.harness/insight-index.md:38` records it as a *fix class to apply*, not an applied fix. **(a) The premise is false** — the existing em-dashes/`≡` in the ambient block (ps1:47,53,57,62; sh:44,50,54,59) still mojibake under GBK pwsh today.
**(b) Does it matter for THIS task?** No. SA's actual edit replaces ONE sentence with deliberately ASCII-only text (colon-separated, no `--`, no `≡`, no `{{` — `02:108`), and explicitly does NOT touch the surrounding non-ASCII chars (`02:107,207`). An Edit that swaps an ASCII-only sentence neither introduces new mojibake nor depends on the fix being present; the pre-existing mojibake of the *surrounding* chars is unchanged either way. The wrong premise changes nothing about the diff's correctness — it is a stale justification, not a defect in the edit.
**Required action (binding on Developer):** Your ambient-hook edit MUST be ASCII-only and MUST NOT depend on the console-encoding fix being present (it is not). Verify the replacement sentence with `grep -nP '[^\x00-\x7F]'` across all 4 carriers = 0 hits (AC-5). Do NOT "while you're here" rewrite the surrounding em-dashes — that is out of scope (DEFECT-1 is a separate backlog task) and would balloon the diff / risk I.6. Strip the false "already neutralizes" clause from the carried rationale if you transcribe it.

### F-2 — D-4 "batch keeps stopping on it" is inaccurate; harmless because batch is untouched. (ADVISORY)
**Evidence:** RA §4 / SA §13 (D-4) claim batch "keeps stopping on" the richer `BLOCKED: NEEDS-HUMAN` message. I read `skills/harness-batch/SKILL.md:55-65`: batch's strong-signal stop keys on a **`FAILED`** verdict (`:62`), `verify_all` FAIL, `STOP`, or `guard-rm` — **not** on a `BLOCKED` verdict. Step f (`:57`) maps `BLOCKED`→`blocked`; step h (`:59`) only blocks-dependents-and-stops on `FAILED`. So a `BLOCKED:` return to batch would mark the row `blocked` and **continue**, not stop. Separately, the new pm-orchestrator structured return (`02 §5A`) fires only when the dispatch prompt carries `deferred-human mode: defer, do not ask` — which SA adds to the **stream's** dispatch prompt only (`SKILL.md:120`), never batch's (`batch SKILL.md:54`). Under batch (no signal), pm-orchestrator takes the interactive branch — the same pre-existing condition as today. Net: **batch behavior is unchanged**, which is the actual non-goal; only the *stated reason* ("keeps stopping on it") is wrong.
**Required action (binding on Developer):** Do NOT add the `deferred-human mode` signal to `harness-batch/SKILL.md`'s dispatch prompt — that would pull batch into scope. Do not edit `harness-batch/SKILL.md` at all. If you transcribe the D-4 rationale into CHANGELOG/README, drop or correct "batch keeps stopping on it" (batch simply receives a richer message; its halt policy is unchanged and key on FAILED, not BLOCKED).

### F-3 — Fan-out ledger omits `docs/getting-started.md:45`; no edit required. (ADVISORY)
**Evidence:** `docs/getting-started.md:45` carries `harness-stream — drain a living task pool you keep topping up mid-run (best-effort)`. RA §7 / SA §9 do not list it. Like `dev-map.md:57` and `AI-GUIDE.md:93`, it is a one-liner whose "(best-effort)" stays accurate (a needs-human defer is a flavor of best-effort), so it needs no edit by the same rule SA applied elsewhere. The SA's "grep-verified list" claim is thus slightly incomplete but causes no stale/broken text.
**Required action:** None mandatory. If the Developer wants belt-and-suspenders, getting-started.md:45 may receive the same "needs human input is deferred" half-clause as `README.md:21`, but it is not required for correctness.

### F-4 — Snapshot-exempt HTML ruling is sound; flag for awareness. (ADVISORY)
**Evidence:** `docs/harness-stream.html:130,181` and `docs/parallel-stream-design.html` contain `best-effort` stream prose and are NOT in RA/SA's edit list. They are NOT in verify_all's I.6 exempt-file list (only CHANGELOG/architecture.html/walkthrough.html + the i6 drivers are exempt, `verify_all.sh:516-519`), but they contain no I.6 banned anchor (the banned list `:521-536` is entirely about harness-adopt/CLAUDE.md-composition/zh — unrelated), so leaving them unedited trips nothing. They are dated design snapshots, not normative skill docs — not syncing them is defensible.
**Required action:** None. Leave both HTML files untouched; confirm they don't carry a banned anchor (they don't) and move on.

## High-probability developer questions (pre-answered)

1. **"Is removing `AskUserQuestion` from SKILL.md:4 safe — any blocking-ask path left?"** YES, safe. Tree-wide grep confirms the literal token appears in `harness-stream/SKILL.md` ONLY at `:4` (the decl) and `:115` (Procedure 3a call site). The `:76` ambient path is a prose "ask before creating a row" (no token) and is also rewritten by SA §6. No third blocking-ask path exists in the skill or in any file the skill references. Uniform-defer (RA D-2) is correct — there is no genuinely-attended ambient immediate-ask to retain (ambient runs unattended by definition; the hook never blocks, ps1:14/sh:15). `test-supervisor.{ps1,sh}:138-144,164-169` match `AskUserQuestion` against `supervisor.md`/`harness-supervise/SKILL.md` only — **not** the stream — so removal pins no test driver.

2. **"Will the `BLOCKED: NEEDS-HUMAN —` token collide with existing BLOCKED markers?"** No. The existing `BLOCKED ON PARTITION` (pm-orchestrator.md:99) is a partition-routing marker; `BLOCKED:` at pm-orchestrator.md:145 is a *parsing instruction* (PM scans returns for a `BLOCKED:` prefix), not a verdict literal. The new token IS a `BLOCKED:` marker with the `NEEDS-HUMAN` discriminator token immediately after the colon; the stream switches on that prefix token only (`02 §4`), routing non-`NEEDS-HUMAN` BLOCKED to `blocked`. A dependency-blocked task carries a plain `BLOCKED` (no `NEEDS-HUMAN`) so cannot be misclassified, and vice-versa. Unambiguous.

3. **"Does a `needs-human` row resume?"** Yes, free. SKILL.md:118 resume semantics re-evaluate every non-DELIVERED status (`pending/in-progress/failed/blocked`); SA §2 adds `needs-human` to that set verbatim. No new resume mechanism. The user supplies input via pool edit / `ADD` / `/harness-intervene` / chat (all existing channels, 65-intervention.md:53,56).

4. **"Will the version bump break G.3/G.4?"** No. G.3 (`verify_all.sh:350-372`) is 4-way version equality across plugin.json + marketplace.json + 2 README badges — bump all four to 0.33.0. G.4 (`:686-786`) reads version from plugin.json and requires a `[0.33.0]` CHANGELOG heading + count claims = `g4_count` (stays 32). G.4's `g4_files` list does NOT include README milestone tables, BATCH_PLAN.md, or pm-orchestrator.md, so SA's edits there don't trip count checks. Counts stay 15 skills / 32 checks (no new skill, no new check) — verified correct.

5. **"Does the new prose trip I.6?"** No. The I.6 banned anchors (`verify_all.sh:521-536`) are entirely about harness-adopt automation / CLAUDE.md-composition / zh-全程中文 — none relate to stream stop/halt/defer/best-effort. "Best-effort never halts" wording is NOT a banned anchor; it is extended, not contradicted. No 4-file I.6 lockstep edit needed (insight 2026-06-08). Still: run the AC-3 re-check to confirm no accidental anchor resurrection.

## PM-flagged items — explicit rulings

1. **Console-encoding claim** → F-1: premise FALSE (no `[Console]::OutputEncoding` in ambient-prompt.ps1; DEFECT-1 is backlog/unimplemented), but immaterial — SA's edit is ASCII-only and non-destructive. Binding instruction issued (F-1).
2. **Status model D-1** → Confirmed: BATCH_PLAN.md:27 is the only enum definition (`pending|in-progress|done|failed|blocked|skipped`); `docs/batches/README.md:19` names only the column shape (no inline enum). No verify_all check pins the Status enum (G/I/E/F families verified, no enum assertion). No batch-plan validator lists allowed values (batch SKILL.md:45-49 validates columns/cycles/uniqueness, not enum values). Resume (SKILL.md:118) re-runs `needs-human` exactly like `blocked`/`failed`. **Safe.**
3. **allowed-tools removal** → Safe (developer Q1). Uniform-defer correct; no attended-ambient immediate-ask must be retained.
4. **Discriminator token** → Composes cleanly (developer Q2). Unambiguous vs `BLOCKED ON PARTITION` and the `BLOCKED:` parse instruction. No misclassification path.
5. **pm-orchestrator contract + I.3** → §5A forbids (a) interactive asking under a stream ("an interactive ask is unavailable… Do NOT attempt an interactive ask"), (b) auto-deciding a reserved point ("Do NOT silently auto-decide… defer-and-surface instead" + new Hard rule 6 §5B), (c) mandates the structured `BLOCKED: NEEDS-HUMAN —` return. File is 200 lines + ~12 = ~212, under the I.3 300 cap (verify_all.sh:402-414 globs top-level `agents/*.md`, WARN-only). Composes with 25-decision-policy.md: policy = WHO decides; this feature = WHEN to surface (end vs mid-drain). Red lines unchanged. **No contradiction.** Note: PM dispatch said "190-197"; the actual section is at `pm-orchestrator.md:193-200` — SA cited it correctly.
6. **Bright line (c)/(d)** → Unambiguous. SKILL.md:134-142 edit + pm-orchestrator §5A both state verbatim: a *request for* a safety-critical action **defers** (not performed, not halted); a `guard-rm` *block of an attempted destructive command* **halts**; verify_all FAIL + intervention STOP still halt. §5A adds "a hard-safety event… is NOT a needs-human deferral." Tie-break (d wins) preserved (RA §5). A future agent cannot defer a real hard-stop nor halt on a mere request.
7. **Fan-out completeness** → Independently grepped README.md:21/275, README.zh-CN.md, CHANGELOG.md:1-11, AI-GUIDE.md:93, docs/batches/README.md:19/28, BATCH_PLAN.md:27, docs/dev-map.md:57, docs/concepts.md (grep-clean for stream-stop), docs/workflow.md (clean), evals/golden-tasks.md (zero matches), skills/harness-batch|intervene|goal/SKILL.md, .harness/rules/65-intervention.md (keywords only, not taxonomy), verify_all.{ps1,sh} G.4 claim ledger, the 2 HTML files. All 4 ambient carriers covered (`02 §6,§9`). No test driver pins the touched stop/continue prose (only count/version via C.1/G.1/G.2/G.3/G.4). Misses: getting-started.md:45 (F-3, no edit needed), HTML snapshot ruling sound (F-4).
8. **Version/G.3/G.4 + I.6** → Bump set verified against G.3 (4-way) + G.4 (plugin.json version + `[0.33.0]` heading). Counts 15/32 NOT moved (correct). New normative sentences + planned CHANGELOG touch no I.6 banned anchor (`:521-536` reviewed). **All correct.**
9. **Feasibility/risk residue** → F-1 (false encoding premise), F-2 (batch D-4 rationale inaccuracy). Neither blocks; both have binding/awareness actions.

## Verdict

**APPROVED FOR DEVELOPMENT.** 0 BLOCKING, 4 ADVISORY (F-1..F-4). The design is implementable as written with no further design decisions. The single PM-flagged risk (console-encoding premise) is a stale justification, not a defect.

### Binding conditions the Developer must carry
1. **(F-1)** Ambient-hook edit MUST be ASCII-only (verify all 4 carriers with `grep -nP '[^\x00-\x7F]'` on the new sentence = 0); MUST NOT depend on the console-encoding fix (unimplemented); MUST NOT rewrite the surrounding existing non-ASCII chars (out of scope). Drop the false "already neutralizes" clause.
2. **(F-2)** Do NOT edit `harness-batch/SKILL.md` or add the `deferred-human mode` signal to batch's dispatch prompt — batch stays out of scope. In CHANGELOG/README, do not assert "batch keeps stopping on the NEEDS-HUMAN message" (batch keys stop on FAILED, not BLOCKED; it simply receives a richer message, policy unchanged).
3. **(AC standing)** verify_all 32/0/0 both shells; G.3 4-way = 0.33.0; G.4 `[0.33.0]` heading + counts 32/15 unchanged; ambient 4-file byte-lockstep (ps1↔sh CR-strip identity, dogfood↔template per-extension byte-identity, no `{{` token); pm-orchestrator.md ≤300 (≈212); I.6 clean (no banned anchor touched).
