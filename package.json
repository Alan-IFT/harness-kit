{
  "name": "harness-kit",
  "version": "0.50.0",
  "private": true,
  "description": "Build tooling for harness-kit's TypeScript scripts. Not published; the plugin ships the compiled output under .harness/scripts/.",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "shim": "bash tools/make-shims.sh"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^4.1.10"
  }
}
