[project]
name = "todo-backend"
version = "0.0.1"
description = "Minimal backend todo list fixture for Harness integration tests."
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.pytest.ini_options]
testpaths = ["tests"]
