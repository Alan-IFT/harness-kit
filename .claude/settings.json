{
  "$schema": "https://json.schemastore.org/claude-code-settings",
  "_comment": "Dogfood: this repo uses the same Stop-hook auto-sync that harness-init ships to new projects (v0.9.0).",
  "permissions": {
    "allow": [
      "Bash(pwsh:*)",
      "Bash(bash scripts/harness-sync.sh:*)",
      "Bash(git status:*)",
      "Bash(git diff:*)",
      "Bash(git log:*)",
      "Bash(git add:*)",
      "Bash(git commit:*)"
    ],
    "deny": [
      "Bash(git push --force:*)",
      "Bash(git push -f:*)",
      "Bash(rm -rf /:*)"
    ]
  },
  "hooks": {
    "_doc_sync_hook": "Auto-runs harness-sync at session end so .harness/ edits flow to CLAUDE.md + .github/copilot-instructions.md without manual sync. On macOS/Linux without pwsh, change to: bash scripts/harness-sync.sh",
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "pwsh -NoProfile -File scripts/harness-sync.ps1"
          }
        ]
      }
    ],
    "_guard_hook": "Auto-runs guard-rm before every Bash tool call. Blocks destructive commands targeting paths outside this repo. Override per-call with HARNESS_ALLOW_OUTSIDE_RM=1. See .harness/rules/75-safety-hook.md to fully disable.",
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "pwsh -NoProfile -File scripts/guard-rm.ps1"
          }
        ]
      }
    ]
  }
}
