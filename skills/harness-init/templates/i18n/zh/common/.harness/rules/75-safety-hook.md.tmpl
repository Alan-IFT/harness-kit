# 75 — 破坏性命令安全 Hook

## 这是什么

`.claude/settings.json` 里的一个 `PreToolUse` hook，在 Claude Code 每次调用 Bash
工具前先跑 `.harness/scripts/guard-rm.{ps1,sh}`。当任何破坏性动词的目标路径解析到
**当前工作目录最近的 `.git/` 祖先之外** 时，guard 阻断该调用。

`/harness-init` 和 `/harness-adopt`（带合并提示）会自动装这个 hook。默认 **始终
开启**；有完整记录的一行禁用方式（见下面）。

## 什么时候读

- 运行、观察、或禁用破坏性命令防护时。
- 一次工具调用意外被 BLOCK，想搞清楚原因时。
- 编辑 `.claude/settings.json` 的 `PreToolUse` 块前。

## 触发动词

| 类别 | 动词（可选的 `sudo` 之后的第一个 token） |
|---|---|
| POSIX `rm` 家族 | `rm`、`rmdir`、`unlink`、`shred`、`srm` |
| Windows / pwsh | `Remove-Item`、`del`、`erase`、`Clear-RecycleBin` |
| `find` 删除 | `find … -delete`（`-delete` 出现在参数任意位置） |
| 嵌套 pwsh | `pwsh -c "<cmd>"`、`pwsh -Command "<cmd>"`、`powershell -c …` —— 引号里的命令会被重新分词，套用相同规则（递归深度上限 2；更深嵌套 → 用 parse-failure 消息 BLOCK） |

其他命令（`mv`、`cp`、`>` 重定向、`git`、构建工具……）**不**在本 hook 防护范围内，
本 hook 只抓破坏性动词。

## 路径解析规则

- **绝对路径**：按原样使用。
- **相对路径**：先 join 到 Bash 工具的 cwd，再折叠 `..` / `.`。
- **波浪号** (`~`、`~/foo`)：展开为 `$HOME` / `$env:USERPROFILE`。
- **符号链接**：**只看叶子** —— 检查写下来的链接路径本身；不跟随链接目标。
  这符合用户心智模型（你写了仓库内的路径，就意味着要删这个路径），同时避免在
  合法整理符号链接时被意外 block。
- **glob** (`/tmp/*.log`)：字面对待 —— guard 不展开。被检查的就是字面前缀，
  所以 `/tmp/*.log` 解析到 `/tmp/` 下，落在 repo 之外。
- `$repoRoot` 是 cwd 最近的 `.git/` 祖先（手工 walk —— guard 不会自己 spawn
  `git`，为了守住 50 ms 墙钟预算）。如果向上找不到 `.git/`，guard 会向 stderr
  打 WARN 然后 exit 0，工具调用继续 —— "大声拒绝保护" 好过在 CI 临时 shell 里
  误 block。

## Override（单次逃生口）

为单次调用设置环境变量 `HARNESS_ALLOW_OUTSIDE_RM=1`。

Bash：

```
HARNESS_ALLOW_OUTSIDE_RM=1 rm -rf /tmp/some-thing
```

PowerShell：

```
$env:HARNESS_ALLOW_OUTSIDE_RM=1; Remove-Item -Recurse C:\some\external\path
```

Override 激活时，guard 向 stderr 打一行 INFO（`override active …`），不解析直接
exit 0。Claude Code 把这行 INFO 记到 tool transcript 里，所以 override 可审计。

Override 刻意做成 **单次 + 可见**。它不能被持久化到任何被提交的文件里 ——
`.claude/settings.json` 不接受它，而且 verify_all 在 release 检查时会抓
任何在 tracked 文件里硬编码它的尝试。

## 完全禁用

编辑 `.claude/settings.json`，删掉 `hooks.PreToolUse` 数组（或者只删那个
matcher==="Bash"、指向 `guard-rm` 的条目）。要重新启用：重跑 `/harness-adopt`
（它会把 PreToolUse 块合并回去），或者从
`skills/harness-init/templates/common/.claude/settings.json.tmpl` 复制。

## 故障模式

| 故障 | 表现 | 修复 |
|---|---|---|
| 磁盘上 guard 脚本缺失 | PreToolUse hook 本身失败 → Claude Code 用 hook 错误 BLOCK 调用，错误显示在 transcript 里 | 重装：`/harness-adopt`，或者从 harness-kit 模板复制 `.harness/scripts/guard-rm.{ps1,sh}` |
| 嵌套 pwsh 解析失败 / 引号不闭合 | 用明确的 "could not parse …" 消息 BLOCK | 换种不嵌套引号的写法，或者你确认命令安全时用 override |
| cwd 向上没有 `.git/` | 向 stderr 打 WARN，exit 0（工具调用继续） | 确认在真实项目里，或者 `git init` |
| 性能超 50 ms | 不算故障，但记录 | guard 是直行代码、不 spawn 子进程；如果遇到慢 block，提 issue 附上耗时 |

## 边界

- **项目内删除允许。** 在仓库内跑 `rm -rf node_modules`、`rm -rf .next/`、
  `rm -rf build/` 是合法的 —— 设计如此。guard 的作用域是 "rm 项目 root 之外"。
- **不在 v0.15 范围**：`mv` / `cp` / 重定向覆盖（`> file`）—— 常见合法用法，
  本版本不处理。出事才会重评。
- **不在范围**：Write/Edit、Cursor、Copilot 或除 Claude Code 之外的任何 AI
  工具发出的工具调用。PreToolUse 只管 Claude Code 的工具调用。其他工具靠
  文档化的最佳实践；`.claude/settings.json` 里的 `Bash(rm -rf /:*)` deny 行
  和 git pre-commit hook 是部分兜底。
- **嵌套在其他 git repo 的子目录**：`$repoRoot` 是**最近**的 `.git/` 祖先。
  你如果在嵌套 clone 里运行，guard 的作用域是内层 repo，不是外层 monorepo。
  迁出去，或者为那个 session 设 override。
