# {{PROJECT_NAME}} — bootstrap 规则

输出语言：**中文**。

完整项目规则在 `AI-GUIDE.md`（根目录）+ `.harness/rules/*.md`。**开始任何任务前先读 `AI-GUIDE.md`，根据它每条的"什么时候读"描述选择性加载相关片段，不要全读**。

红线（永不违反）：
- 别手改 `.claude/` —— 它是 agent 运行时配置：`settings.json` 是活的启动配置（改动请提议、由用户应用），`agents/`+`skills/` 由 `.harness/` 同步生成（要改去改 `.harness/` 源文件）。
- 禁编辑本文件和 `.github/copilot-instructions.md` —— init 时写一次的静态 stub。
- 声明完成前必须跑 `.harness/scripts/verify_all` PASS
- 子 agent 不自路由；PM Orchestrator 派发

本文件是**静态**的 —— init 时写一次，刻意精简，永不膨胀持久 context 预算。其余全在 `AI-GUIDE.md` 或 `.harness/`。
